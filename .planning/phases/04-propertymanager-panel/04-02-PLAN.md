---
phase: 04-propertymanager-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/Gui/FreeWorks/FwLayout.h
  - src/Gui/FreeWorks/FwLayout.cpp
  - src/Gui/FreeWorks/FwPropertyManagerHeader.h
  - src/Gui/FreeWorks/FwPropertyManagerHeader.cpp
  - src/Gui/FreeWorks/FwWorkbench.cpp
  - src/Gui/FreeWorks/CMakeLists.txt
  - tests/src/Gui/FwPropertyManagerWidget.cpp
autonomous: true
requirements:
  - PROP-01
user_setup: []

must_haves:
  truths:
    - "FwLayout::mountPropertyManager() HOSTS Control's TaskView content in the EXISTING Fw_PropertyManager LEFT dock (mirroring mountFeatureManager FwLayout.cpp:117-146, which REPLACES the Fw_FeatureManager placeholder with the real FwFeatureTree): in FreeWorks mode FwWorkbench::setupDockWindows() (FwWorkbench.cpp:95-124) returns ONLY Fw_FeatureManager/Fw_PropertyManager/Fw_TaskPane — NOT Std_TaskView — so the framework never builds a live 'Tasks' dock and Control().taskPanel() is null until this mount runs (F1 VERIFIED). The mount resolves the REGISTERED Tasks TaskView widget (the one MainWindow::setupTaskView() registers under key 'Std_TaskView' whose WIDGET objectName is 'Tasks' — MainWindow.cpp:609-630) via Gui::DockWindowManager::instance()->findRegisteredDockWindow(\"Std_TaskView\"), and hosts it into a left dock via addDockWindow(\"Tasks\", taskView, Qt::LeftDockWidgetArea). Because addDockWindow copies the WIDGET objectName onto the new QDockWidget container (DockWindowManager.cpp:290; same mechanism setup() uses at DockWindowManager.cpp:532), the live container objectName becomes 'Tasks' — so Control::taskPanel()'s getDockWindow(\"Tasks\") lookup (Control.cpp:55-61; matches the container objectName at DockWindowManager.cpp:325) RESOLVES after hosting, and OverlayWidgets' findChild<TaskView*> (OverlayWidgets.cpp:2850) keeps resolving. The inner TaskView widget objectName 'Tasks' is NEVER changed (D-01/D-02; F2/F3 VERIFIED; committed by the 04-01 SPIKE A1 verdict 'reuse-and-rehost committed')"
    - "mountPropertyManager() does NOT hardcode a getDockContainer(\"Std_TaskView\") guess (F3 VERIFIED: the live container objectName is 'Tasks', not 'Std_TaskView', so that lookup may return nullptr): it resolves the host ROBUSTLY by (a) findRegisteredDockWindow(\"Std_TaskView\") for the registered TaskView widget, and (b) Gui::Control().taskPanel() walk-up to its parent QDockWidget (qobject_cast<QDockWidget*>(taskView->parentWidget())) for the already-hosted case — getDockWindow(\"Tasks\") is a registry/dock-list lookup by name and is PARENT-INDEPENDENT, so hosting the widget preserves the lookup. It is IDEMPOTENT find-or-reuse mirroring mountFeatureManager: if Control().taskPanel() is already non-null AND its parent QDockWidget is already in Qt::LeftDockWidgetArea, the call is a no-op; reachability is guarded (no-op if Gui::getMainWindow() / DockWindowManager::instance() / the registered Tasks widget are not yet available)"
    - "mountPropertyManager() makes Fw_PropertyManager the SINGLE coherent left PropertyManager surface (F2 VERIFIED: FwLayout::install() registers a labeled 'PropertyManager' placeholder under Fw_PropertyManager shown Left — FwLayout.cpp:162-175 — and ignoring it would give the user TWO left surfaces): mirroring mountFeatureManager's placeholder drop (FwLayout.cpp:138-143), the mount consumes/removes the Fw_PropertyManager placeholder (unregisterDockWindow + deleteLater on the prior placeholder) when it hosts the Tasks TaskView left, so exactly ONE left PropertyManager exists, not a placeholder PLUS a hosted dock"
    - "unmountPropertyManager() reverses the host so stock (non-FreeWorks) workbenches keep their default behaviour: it releases the hosted Tasks TaskView's left QDockWidget so the registry re-yields the Tasks widget for the framework's standard right-dock setup, and restores the Fw_PropertyManager placeholder — mirroring restoreStockChrome reversibility (FwWorkbench::deactivated, Pitfall 3); the host is FreeWorks-scoped and reversible, never an unconditional left-pin"
    - "The host SURVIVES the PartDesign workbench round-trip (F4 VERIFIED): PartDesign ViewProvider::setEdit() calls assureWorkbench(\"PartDesignWorkbench\") (ViewProvider.cpp:164-175) BEFORE Control().showDialog(), which activates another workbench (Command.cpp:851-864), and signalInEdit fires only after startEditing() (Document.cpp). Because Fw_PropertyManager and the hosted Tasks QDockWidget are MainWindow-level QDockWidgets (NOT recreated per workbench), the host is inherently robust across the round-trip. The reveal/host wiring is therefore NOT torn down in unmountPropertyManager() merely on deactivated() mid-edit: FwWorkbench::deactivated() must NOT drop the host/reveal while a Control dialog is active (Control().activeDialog() != nullptr); and the signalInEdit reveal consumer RE-ASSERTS the left host idempotently when the edit lands so a tree double-click of a PartDesign feature opens LEFT, not in the wrong place"
    - "FwWorkbench::activated() calls FwLayout::mountPropertyManager() AFTER FwLayout::mountRibbon() and with hideStockChrome() (FwWorkbench.cpp:61-63 ordering), and FwWorkbench::deactivated() calls FwLayout::unmountPropertyManager() in the reverse-order teardown alongside unmountRibbon() (FwWorkbench.cpp:70-71) — GUARDED so an active Control dialog (mid-edit, e.g. during the assureWorkbench round-trip) is NOT torn down; the panel left-host is wired into the same workbench lifecycle as the ribbon and tree"
    - "FwPropertyManagerHeader is a thin FreeWorks QWidget chrome band (mirrors FwRollbackBar discipline) whose green-✓ control calls Gui::Control().accept() and red-✗ control calls Gui::Control().reject() (Control.h:98-100) — it introduces NO new commit/transaction logic (each hosted TaskDialog already wraps openTransaction/commitTransaction — D-04/D-10); the ✓/✗ map to the SAME accept/reject the hosted TaskEditControl QDialogButtonBox drives (TaskView.cpp:644-686)"
    - "FwPropertyManagerHeader color is QPalette-ROLE-derived only: NO hex literal and NO setStyleSheet color literal anywhere in the header source (mirror FwFeatureTreeDelegate.cpp:154-162 palette derivation); the header is a fixed 32px band (UI-SPEC § Spacing exception); tooltips read 'Accept (Enter)' / 'Cancel (Esc)' and the copy contains NO 'SolidWorks' identifier string (UI-SPEC § Copywriting; LOCKED fork rule)"
    - "The panel is NON-MODAL: mountPropertyManager() never calls QDialog::exec() and the hosted left dock remains a QDockWidget-embedded Gui::TaskView::TaskView, so geometry stays pickable while the panel is shown and the model previews live via the hosted panel's PreviewExtension (PROP-01, D-07; a fully-modal panel is explicitly REJECTED — REQUIREMENTS Out-of-Scope)"
    - "Panel reveal is event-driven (Pitfall 1: Control().activeDialog() has NO change signal — never poll it): a single scoped boost::signals2 consumer subscribes to Gui::Application::signalInEdit (Document.cpp:313) to re-assert the idempotent left host and reveal/raise the left Tasks dock on command/edit start (covering the A2/F4 assureWorkbench round-trip), owned for the FreeWorks-mode lifetime via a unique_ptr static and reset() in unmount* so the subscription drops before teardown — mirroring the s_ribbonContext lifetime discipline (FwLayout.cpp:59/273-286, FwRibbonContext scoped_connection pattern)"
    - "After mountPropertyManager() in an offscreen QTEST, Gui::Control().taskPanel() is non-null AND its parent QDockWidget is in Qt::LeftDockWidgetArea AND a save/restore of the QMainWindow state round-trips with the 'Tasks' container objectName + the 'Tasks' inner-widget objectName preserved (saveState/restoreState — Runtime State Inventory: the hosted dock must round-trip); the green-✓/red-✗ header fires Control().accept()/reject() against a stub TaskDialog; collapsible TaskBox/TaskGroup rollouts are still present and expandable after the re-host (PROP-01 rollouts intact, D-04)"
    - "tools/fw-string-leak-grep.sh is clean over src/Gui/FreeWorks/ — no bare 'SolidWorks' token in FwPropertyManagerHeader.{h,cpp} or the FwLayout additions; FreeWorks stays additive (target_sources(FreeCADGui PRIVATE ...), no MainWindow.cpp / Control.cpp / TaskView.cpp body edit)"
  artifacts:
    - path: "src/Gui/FreeWorks/FwPropertyManagerHeader.h"
      provides: "FwPropertyManagerHeader: thin QWidget chrome band declaration — green-✓ accept / red-✗ cancel controls driving Gui::Control() (no commit logic), palette-only, 32px"
      contains: "class FwPropertyManagerHeader"
    - path: "src/Gui/FreeWorks/FwPropertyManagerHeader.cpp"
      provides: "Header impl: ✓→Control().accept(), ✗→Control().reject(); palette-role tint (no hex/setStyleSheet color literal); tooltips 'Accept (Enter)'/'Cancel (Esc)'; no 'SolidWorks' string"
      contains: "Gui::Control().accept"
    - path: "src/Gui/FreeWorks/FwLayout.cpp"
      provides: "mountPropertyManager()/unmountPropertyManager(): idempotent find-or-reuse HOST of the registered Tasks TaskView into the left Fw_PropertyManager dock (resolve via findRegisteredDockWindow + Control().taskPanel() walk-up, never a hardcoded getDockContainer guess; consume the placeholder; preserve the 'Tasks' objectName); signalInEdit reveal consumer that re-asserts the host idempotently, owned via a unique_ptr static, reset() on unmount; deactivated()-guarded against mid-edit teardown"
      contains: "mountPropertyManager"
    - path: "src/Gui/FreeWorks/FwLayout.h"
      provides: "mountPropertyManager()/unmountPropertyManager() static declarations + the reveal-consumer member + Doxygen mirroring the mountRibbon/mountFeatureManager contract"
      contains: "mountPropertyManager"
  key_links:
    - from: "src/Gui/FreeWorks/FwLayout.cpp"
      to: "Gui::DockWindowManager"
      via: "findRegisteredDockWindow(\"Std_TaskView\") to get the registered Tasks TaskView widget, then addDockWindow(\"Tasks\", taskView, Qt::LeftDockWidgetArea) — host the widget left so the new container objectName is 'Tasks' and getDockWindow(\"Tasks\") resolves; never hardcode getDockContainer(\"Std_TaskView\")"
      pattern: "findRegisteredDockWindow|addDockWindow"
    - from: "src/Gui/FreeWorks/FwLayout.cpp"
      to: "Gui::ControlSingleton"
      via: "Control().taskPanel() walk-up to its parent QDockWidget resolves the already-hosted host robustly (idempotency + reveal re-assert)"
      pattern: "Gui::Control\\(\\)\\.taskPanel"
    - from: "src/Gui/FreeWorks/FwPropertyManagerHeader.cpp"
      to: "Gui::ControlSingleton"
      via: "✓→Gui::Control().accept(), ✗→Gui::Control().reject()"
      pattern: "Gui::Control\\(\\)\\.(accept|reject)"
    - from: "src/Gui/FreeWorks/FwWorkbench.cpp"
      to: "src/Gui/FreeWorks/FwLayout.cpp"
      via: "activated() calls mountPropertyManager() after mountRibbon(); deactivated() calls unmountPropertyManager() (guarded against mid-edit teardown)"
      pattern: "mountPropertyManager"
    - from: "src/Gui/FreeWorks/FwLayout.cpp"
      to: "Gui::Application::signalInEdit"
      via: "a scoped boost::signals2 consumer re-asserts the left host + reveals the panel on edit start (never polls Control().activeDialog()); survives the assureWorkbench round-trip"
      pattern: "signalInEdit"
---

<objective>
Deliver the PROP-01 core vertical slice: when a feature/sketch command or edit starts, the PropertyManager appears **left-docked** with a green-✓ accept / red-✗ cancel header, and the hosted task panel (rollouts, fields, live preview) runs **unmodified** inside it — end to end, non-modal. The keystone is `FwLayout::mountPropertyManager()`: at FreeWorks activation, **host Control's TaskView content in the existing `Fw_PropertyManager` LEFT dock**, mirroring `mountFeatureManager`'s discipline of replacing the `Fw_FeatureManager` placeholder with the real `FwFeatureTree`. In FreeWorks mode `FwWorkbench::setupDockWindows()` (FwWorkbench.cpp:95-124) never returns `Std_TaskView`, so the framework builds **no** live "Tasks" dock and `Control().taskPanel()` is null until this mount runs (F1 VERIFIED). The mount resolves the **registered** Tasks `TaskView` widget (registered under key `"Std_TaskView"`, widget objectName `"Tasks"` — MainWindow.cpp:609-630) via `findRegisteredDockWindow("Std_TaskView")` and hosts it left via `addDockWindow("Tasks", taskView, Qt::LeftDockWidgetArea)`; because `addDockWindow` copies the **widget** objectName onto the new container (DockWindowManager.cpp:290, the same path `setup()` uses at :532), the live container objectName becomes `"Tasks"` and `Control::taskPanel()`'s `getDockWindow("Tasks")` lookup (which matches the **container** objectName, DockWindowManager.cpp:325) RESOLVES (F2/F3 VERIFIED). The host is resolved **robustly** — never a hardcoded `getDockContainer("Std_TaskView")` guess (the live container is named `"Tasks"`, not `"Std_TaskView"`) — and the `Fw_PropertyManager` placeholder is **consumed** so there is ONE coherent left surface, not a placeholder plus a hosted dock (F2 VERIFIED). On top, add a thin `FwPropertyManagerHeader` chrome band whose ✓/✗ controls call the existing `Gui::Control().accept()`/`reject()` slots — no new commit logic (D-04/D-10). Reveal is event-driven via `signalInEdit` (never poll `activeDialog()` — Pitfall 1), and that consumer **re-asserts the left host idempotently** so the PartDesign `assureWorkbench("PartDesignWorkbench")` round-trip (F4 VERIFIED) does not land the edit in the wrong place. `deactivated()` is **guarded** against tearing down the host while a Control dialog is active.

Purpose: This is the slice that satisfies ~80% of the phase success criteria (04-RESEARCH § Summary) — the moment Control's TaskView is left-hosted, SC4 ("existing PartDesign panels work unmodified") and TREE-03's left-landing come for free; this plan wires the host + the ✓/✗ header + the reveal trigger, the foundation Plans 04-03 (pink box + keys) and 04-04 (tree double-click + sketch→feature flow) build on.
Output: `FwPropertyManagerHeader.{h,cpp}` (new), `FwLayout::mountPropertyManager()`/`unmountPropertyManager()` + the `signalInEdit` reveal/re-assert consumer (added to `FwLayout.{h,cpp}`), the `FwWorkbench` lifecycle wiring (guarded against mid-edit teardown), additive CMake, and the extended offscreen QTEST asserting left placement + `taskPanel()` reachable + accept/reject fire + rollouts intact + saveState round-trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-propertymanager-panel/04-CONTEXT.md
@.planning/phases/04-propertymanager-panel/04-RESEARCH.md
@.planning/phases/04-propertymanager-panel/04-PATTERNS.md
@.planning/phases/04-propertymanager-panel/04-UI-SPEC.md
@.planning/phases/04-propertymanager-panel/04-VALIDATION.md
@.planning/phases/04-propertymanager-panel/SPIKE.md
@.planning/phases/04-propertymanager-panel/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- **This plan (04-02):** class `FwPropertyManagerHeader` (namespace `FreeWorksGui`, `src/Gui/FreeWorks/FwPropertyManagerHeader.{h,cpp}`) — thin ✓/✗ header band driving `Gui::Control().accept()`/`reject()`. New `FwLayout::mountPropertyManager()`/`unmountPropertyManager()` static methods + the `signalInEdit` reveal/re-assert consumer (a `unique_ptr` static, e.g. `s_propertyReveal`) added to `FwLayout.{h,cpp}`. `FwWorkbench::activated()`/`deactivated()` wiring (guarded against mid-edit teardown). Extended `tests/src/Gui/FwPropertyManagerWidget.cpp` (left-placement, `taskPanel()` reachable, accept/reject fire, rollouts intact, saveState round-trip). CMake edits to `src/Gui/FreeWorks/CMakeLists.txt` (`FwPropertyManagerHeader.cpp`/`.h` into `FreeWorks_CPP_SRCS`/`FreeWorks_HPP_SRCS`).
- **Plan 04-03 (later):** `FwReferenceBoxStyler.{h,cpp}` (pink active-box restyle), `FwPropertyKeyFilter.{h,cpp}` (D-10 Tab + Enter/Esc container guarantee) — both installed on the host this plan establishes.
- **Plan 04-04 (later):** the FLOW-01 sketch-exit handler + TREE-03 double-click→left-panel confirmation — both rely on this plan's left-hosted `Control` panel.

> **Gate:** This plan executes only after `SPIKE.md` records `reuse-and-rehost committed`. If it records `thin-adopt committed`, re-scope `mountPropertyManager()` to the thin-adopt path (a `FwPropertyManager` QWidget under `Fw_PropertyManager` that adopts the running `TaskView` for the edit lifetime) before executing. NOTE: 04-PATTERNS § "Idempotent find-or-reuse mount" still describes the OLD "move the Std_TaskView container" strategy — that mechanism is SUPERSEDED here (F1/F2/F3 VERIFIED: there is no live Std_TaskView container to move in FreeWorks mode; host the registered Tasks widget into the Fw_PropertyManager left dock instead). Follow this plan's `<behavior>`, not the stale PATTERNS snippet.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FwLayout::mountPropertyManager()/unmountPropertyManager() — host the registered Tasks TaskView in the existing Fw_PropertyManager LEFT dock (resolve robustly, consume the placeholder, preserve "Tasks" objectName) + the signalInEdit reveal/re-assert consumer; wire into FwWorkbench lifecycle (guarded against mid-edit teardown)</name>
  <files>src/Gui/FreeWorks/FwLayout.h, src/Gui/FreeWorks/FwLayout.cpp, src/Gui/FreeWorks/FwWorkbench.cpp</files>
  <read_first>
    - src/Gui/FreeWorks/FwLayout.cpp lines 117-146 (`mountFeatureManager` — the EXACT placeholder-REPLACEMENT mount discipline to mirror: reach the registry only through `Gui::DockWindowManager`, be idempotent via qobject_cast on the existing registered widget, DROP the prior placeholder with unregisterDockWindow + deleteLater (:138-143), preserve objectName) and lines 162-175 (`install()` registers the `Fw_PropertyManager` placeholder via `ensureDock` — the placeholder this mount must CONSUME) and lines 50-59/182-209/273-286 (the `s_ribbonContext` `unique_ptr` static lifetime + `mountRibbon` reachability guards + `reset()` on unmount — the reveal-consumer ownership pattern)
    - src/Gui/FreeWorks/FwLayout.h lines 55-116 (the class surface: `mountRibbon`/`unmountRibbon`/`hideStockChrome`/`restoreStockChrome` declarations + the `s_ribbonContext` member — add `mountPropertyManager`/`unmountPropertyManager` + the reveal-consumer member in the same style + Doxygen)
    - src/Gui/FreeWorks/FwWorkbench.cpp lines 44-74 (`activated()` order `StdWorkbench::activated(); mountRibbon(); hideStockChrome();` — add `mountPropertyManager()` after `mountRibbon()`; `deactivated()` `restoreStockChrome(); unmountRibbon();` — add `unmountPropertyManager()` in the reverse teardown, GUARDED) and lines 95-124 (`setupDockWindows()` returns ONLY Fw_FeatureManager/Fw_PropertyManager/Fw_TaskPane — confirm Std_TaskView is NOT returned, so no live Tasks dock exists in FreeWorks mode — F1)
    - src/Gui/DockWindowManager.cpp lines 251-314 (`addDockWindow(name, widget, pos)`: creates a QDockWidget container, sets its objectName from the `name` arg at :290, pushes to `_dockedWindows` — host the registered Tasks widget with name "Tasks" so the container objectName becomes "Tasks"), lines 321-331 (`getDockWindow` matches the CONTAINER objectName and returns its widget — the lookup Control().taskPanel() uses), lines 484-512 (`registerDockWindow`/`unregisterDockWindow`/`findRegisteredDockWindow` — the staging registry; findRegisteredDockWindow("Std_TaskView") returns the Tasks TaskView widget), lines 515-551 (`setup()` :530-535 calls `addDockWindow(jt.value()->objectName()..., jt.value(), pos)` — proves the live container objectName == the WIDGET objectName, i.e. "Tasks")
    - src/Gui/MainWindow.cpp lines 609-633 (`setupTaskView()`: creates the `TaskView`, sets WIDGET objectName "Tasks" (:623), registers it under key "Std_TaskView" (:628), NOT added to _dockedWindows — the names to preserve)
    - src/Gui/Control.h lines 54,68,76 (`Control()`/`taskPanel()` — the lookup that must keep resolving after the host) and src/Gui/Control.cpp lines 55-61 (`getDockWindow("Tasks")`) and lines 149-195 (`showDialog` — the dialog the reveal raises)
    - src/Mod/PartDesign/Gui/ViewProvider.cpp lines 164-175 (`setEdit` calls `assureWorkbench("PartDesignWorkbench")` BEFORE `Control().showDialog()` — the A2/F4 WB round-trip the host must survive)
    - src/Gui/Command.cpp lines 851-864 (`assureWorkbench` activates another workbench — confirms the mid-edit WB switch)
    - src/Gui/Application.h lines 154,156 (`signalInEdit`/`signalResetEdit`) and src/Gui/Document.cpp lines 313,750 (emit sites — the event-driven reveal/re-assert trigger; signalInEdit fires AFTER startEditing, i.e. after the WB round-trip)
    - src/Gui/FreeWorks/FwRibbonContext.cpp lines 65-71,80-129 (the `boost::signals2::scoped_connection` member + idempotent `connect()`/explicit `disconnect()` in dtor + `QPointer` widget guard — the reveal consumer reuses this scoped-lifetime discipline)
    - .planning/phases/04-propertymanager-panel/SPIKE.md (the A1 verdict the host strategy depends on; the A2/F4 disposition for re-asserting on signalInEdit and not tearing down mid-edit)
    - .planning/phases/04-propertymanager-panel/04-PATTERNS.md § "Idempotent find-or-reuse mount" (NOTE: describes the SUPERSEDED container-MOVE; follow this plan's host-the-registered-widget behavior instead — F1/F2/F3) + § "Mount/teardown wiring" + § "Event-driven signal consumer"
  </read_first>
  <behavior>
    - `FwLayout::mountPropertyManager()` (static): reachability-guarded (no-op if `Gui::getMainWindow()` or `Gui::DockWindowManager::instance()` is null). Resolve the host ROBUSTLY: first check whether `Gui::Control().taskPanel()` is already non-null and its parent QDockWidget (`qobject_cast<QDockWidget*>(taskPanel()->parentWidget())`) is already in `Qt::LeftDockWidgetArea` → no-op (idempotent). Otherwise obtain the registered Tasks `TaskView` widget via `Gui::DockWindowManager::instance()->findRegisteredDockWindow("Std_TaskView")` (its widget objectName is "Tasks"); if non-null, host it left via `Gui::DockWindowManager::instance()->addDockWindow("Tasks", taskView, Qt::LeftDockWidgetArea)` and `dock->show()` — the new container's objectName becomes "Tasks" (addDockWindow :290), so `getDockWindow("Tasks")` resolves. Do NOT reparent the inner TaskView out of its host; do NOT change the "Tasks" objectName; do NOT hardcode `getDockContainer("Std_TaskView")` (the live container is named "Tasks", F3). CONSUME the `Fw_PropertyManager` placeholder: `unregisterDockWindow("Fw_PropertyManager")` and `deleteLater()` the returned placeholder (mirror mountFeatureManager :138-143) so the Tasks host is the single left PropertyManager. Then bind the reveal consumer (below).
    - The reveal/re-assert consumer: a single `boost::signals2::scoped_connection` (held by a `unique_ptr` static, e.g. `s_propertyReveal`, mirroring `s_ribbonContext`) subscribes to `Gui::Application::signalInEdit`; on fire it RE-ASSERTS the idempotent left host (re-run the mountPropertyManager body) and raises/shows the hosted Tasks dock — covering the A2/F4 case where `assureWorkbench` round-trips the WB mid-edit and signalInEdit fires only after the edit starts. The subscription is idempotent (reassigned, never stacked) and dropped in `unmountPropertyManager()`.
    - `FwLayout::unmountPropertyManager()` (static): GUARDED — if `Gui::Control().activeDialog() != nullptr` (a Control dialog is mid-edit), do NOT tear down the host (the deactivated() during the assureWorkbench round-trip must not strand the edit). Otherwise release the hosted Tasks left QDockWidget so the registry re-yields the Tasks widget for the stock right-dock setup, restore the `Fw_PropertyManager` placeholder (re-`ensureDock`), and reset the reveal consumer so its subscription drops before teardown. No-op if nothing mounted. FreeWorks-scoped and reversible.
    - `FwWorkbench::activated()`: insert `FwLayout::mountPropertyManager();` immediately after `FwLayout::mountRibbon();`. `FwWorkbench::deactivated()`: insert `FwLayout::unmountPropertyManager();` in the reverse teardown next to `unmountRibbon();` — unmountPropertyManager's own active-dialog guard handles the mid-edit case.
    - Add the matching declarations + Doxygen to `FwLayout.h` and the `s_propertyReveal` member.
  </behavior>
  <action>
Add `mountPropertyManager()`/`unmountPropertyManager()` to `FwLayout` mirroring `mountFeatureManager`'s placeholder-REPLACEMENT discipline (FwLayout.cpp:117-146), but HOSTING the registered Tasks TaskView into the Fw_PropertyManager left dock rather than moving a (nonexistent) Std_TaskView container — because FwWorkbench::setupDockWindows() (FwWorkbench.cpp:95-124) never returns Std_TaskView, no live Tasks dock exists in FreeWorks mode until this mount creates one (F1). In mountPropertyManager(): reachability-guard on Gui::getMainWindow() / DockWindowManager::instance(); resolve the host robustly via (a) Gui::Control().taskPanel() + qobject_cast<QDockWidget*>(taskPanel()->parentWidget()) for the already-hosted/idempotent case (no-op if already left), else (b) Gui::DockWindowManager::instance()->findRegisteredDockWindow("Std_TaskView") to get the registered Tasks TaskView widget; host it left with addDockWindow("Tasks", taskView, Qt::LeftDockWidgetArea) + show() so the new container objectName is "Tasks" (DockWindowManager.cpp:290) and getDockWindow("Tasks") (Control.cpp:58) resolves. Do NOT reparent the inner TaskView, do NOT change the "Tasks" objectName, and do NOT hardcode getDockContainer("Std_TaskView") (the live container is "Tasks", F3). Consume the Fw_PropertyManager placeholder (unregisterDockWindow("Fw_PropertyManager") + deleteLater, mirror :138-143) so exactly one left PropertyManager exists (F2). Add a reveal/re-assert consumer as a unique_ptr static (s_propertyReveal) holding a boost::signals2::scoped_connection to Gui::Application::signalInEdit, reusing the s_ribbonContext ownership + FwRibbonContext scoped-connection discipline: on signalInEdit re-assert the idempotent left host and raise the dock (covers the A2/F4 assureWorkbench round-trip — ViewProvider.cpp:164-175, Command.cpp:851-864), reassign (never stack) the connection, and reset() it in unmountPropertyManager(). Make unmountPropertyManager() GUARDED: if Gui::Control().activeDialog() != nullptr (mid-edit), do NOT tear down the host; otherwise release the hosted left QDockWidget, restore the Fw_PropertyManager placeholder (re-ensureDock), reset the consumer (reversible, FreeWorks-scoped — mirror restoreStockChrome). Wire FwLayout::mountPropertyManager() into FwWorkbench::activated() right after mountRibbon(), and FwLayout::unmountPropertyManager() into deactivated() next to unmountRibbon(). Declare both statics + the s_propertyReveal member + Doxygen in FwLayout.h matching the existing mount-method style. Touch NO MainWindow.cpp/Control.cpp/TaskView.cpp body.
  </action>
  <verify>
    <automated>if [ -d build ]; then cmake --build build --target FwPropertyManagerWidget_Tests_run && ctest --test-dir build -R "FwPropertyManagerWidget" --output-on-failure; else echo "NO-BUILD-TREE: compile-intended; CMake/grep verification only (Phases 1-3 precedent)"; fi && grep -q "mountPropertyManager" src/Gui/FreeWorks/FwLayout.h && grep -q "mountPropertyManager" src/Gui/FreeWorks/FwWorkbench.cpp && grep -Eq "findRegisteredDockWindow|addDockWindow" src/Gui/FreeWorks/FwLayout.cpp && grep -q "signalInEdit" src/Gui/FreeWorks/FwLayout.cpp && ! grep -n 'getDockContainer("Std_TaskView")' src/Gui/FreeWorks/FwLayout.cpp && echo "MOUNT-WIRED-OK" && bash tools/fw-string-leak-grep.sh</automated>
    <human-check>No build tree (Phases 1-3 precedent): the host + reveal/re-assert consumer are authored compile-intended; the live left-placement + the assureWorkbench WB-switch round-trip (panel stays left, edit lands left) are deferred to SPIKE_LIVE_CHECKLIST.md Phase 4.</human-check>
  </verify>
  <done>`FwLayout::mountPropertyManager()`/`unmountPropertyManager()` exist and HOST the registered Tasks TaskView in the Fw_PropertyManager left dock (robust resolution via findRegisteredDockWindow + Control().taskPanel() walk-up, never a hardcoded getDockContainer("Std_TaskView"); placeholder consumed; "Tasks" objectName preserved); the signalInEdit reveal consumer re-asserts the host idempotently (survives the assureWorkbench round-trip) and is owned via a unique_ptr static and reset() on unmount; unmountPropertyManager() is guarded against mid-edit teardown (Control().activeDialog()); FwWorkbench::activated()/deactivated() are wired; the `<automated>` block builds FwPropertyManagerWidget_Tests_run (when a build tree exists) and prints `MOUNT-WIRED-OK`; no MainWindow.cpp/Control.cpp/TaskView.cpp body is edited; leak-grep clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FwPropertyManagerHeader — thin green-✓/red-✗ header band driving Gui::Control().accept()/reject() (palette-only, 32px, no SolidWorks string); add it above the left-hosted panel; extend the offscreen QTEST</name>
  <files>src/Gui/FreeWorks/FwPropertyManagerHeader.h, src/Gui/FreeWorks/FwPropertyManagerHeader.cpp, src/Gui/FreeWorks/CMakeLists.txt, tests/src/Gui/FwPropertyManagerWidget.cpp</files>
  <read_first>
    - src/Gui/FreeWorks/FwRollbackBar.h + FwRollbackBar.cpp (the thin-Fw-widget analog: a small QWidget chrome band; ctor, palette use, QPointer to the host, NO transaction logic of its own — mirror its structure for the header band)
    - src/Gui/Control.h lines 98-100 (`accept()`/`reject()`/`closeDialog()` slots — the ✓/✗ targets) and src/Gui/Control.cpp lines 212/227 (what accept/reject do — confirm no FreeWorks commit logic is needed)
    - src/Gui/TaskView/TaskView.cpp lines 644-686 (the existing `TaskEditControl` `QDialogButtonBox` accept/reject wiring — the ✓/✗ map onto the SAME accept/reject)
    - src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp lines 154-162 (palette-DERIVED color: `palette.color(QPalette::Disabled, QPalette::Text)` — the header tint derives roles the same way; NO hex, NO `setStyleSheet` color literal)
    - .planning/phases/04-propertymanager-panel/04-UI-SPEC.md § "Spacing" (32px header band exception), § "Color" (accent reserved for ✓ accept / ✗ cancel; green=commit, red=cancel; blue reserved for prompt icons — never the header), § "Copywriting Contract" (OK / Cancel; tooltips "Accept (Enter)" / "Cancel (Esc)"; NO "SolidWorks" string)
    - src/Gui/FreeWorks/CMakeLists.txt lines 17-44 (`FreeWorks_CPP_SRCS` / `FreeWorks_HPP_SRCS` — add `FwPropertyManagerHeader.cpp`/`.h` additively next to `FwRollbackBar`)
    - tests/src/Gui/FwPropertyManagerWidget.cpp (this plan's 04-01 scaffold — extend it; FwFeatureTreeWidget.cpp `Probe*` pattern for exposing protected seams if the header needs it)
    - src/Gui/TaskView/TaskView.h lines 67,82 (`TaskGroup`/`TaskBox` — the rollout types the QTEST asserts are still present + expandable after the re-host)
    - .planning/phases/04-propertymanager-panel/04-PATTERNS.md § "FwPropertyManagerHeader.{h,cpp}" (the analog + ✓/✗→Control mapping + copy)
    - .planning/phases/04-propertymanager-panel/04-VALIDATION.md § "Per-Task Verification Map" (the PROP-01 header / rollout / saveState rows)
  </read_first>
  <behavior>
    - `FwPropertyManagerHeader` is a thin `QWidget` (namespace `FreeWorksGui`, `FreeWorksGuiExport`) — a fixed 32px-height band with two controls: a green-✓ "OK" accept control (tooltip "Accept (Enter)") wired `connect(greenCheck, &QAbstractButton::clicked, [] { Gui::Control().accept(); })`, and a red-✗ "Cancel" control (tooltip "Cancel (Esc)") wired to `Gui::Control().reject()`. A panel-title label (DemiBold, inherited font — Phase 7 owns fonts) may sit between them.
    - Color is QPalette-role-derived ONLY (green/red functional tones derived from palette roles, never a hex or `setStyleSheet` color literal — mirror `FwFeatureTreeDelegate`). No "SolidWorks" identifier anywhere.
    - The header introduces NO commit/transaction logic: it only calls the existing Control slots. Hold any host pointer via `QPointer` (FwRibbonContext discipline).
    - `mountPropertyManager()` (Task 1) places the header above/around the left-hosted Tasks dock content (e.g. as the hosted dock's title-bar widget or a band added to the dock's container) WITHOUT reparenting the inner `TaskView`. (If a clean placement seam is unavailable, the header attaches as the dock's `setTitleBarWidget`-equivalent band — still container-level, no `TaskView` edit.)
    - Extend the offscreen QTEST (`FwPropertyManagerWidget.cpp`): after `mountPropertyManager()`, assert `Gui::Control().taskPanel() != nullptr` AND its parent QDockWidget is `Qt::LeftDockWidgetArea`; assert a stub `TaskDialog` shown via `Control().showDialog` is reachable and the header's ✓/✗ invoke `Control().accept()`/`reject()` (use a probe/spy or assert the dialog closes); assert a `TaskGroup`/`TaskBox` rollout is present + `isExpandable`/collapsible after the re-host; assert `saveState()`/`restoreState()` round-trips with the "Tasks" container objectName + the "Tasks" inner-widget objectName preserved.
  </behavior>
  <action>
Create `FwPropertyManagerHeader.{h,cpp}` as a thin `FreeWorksGui` QWidget mirroring `FwRollbackBar`'s structure: a fixed 32px band (UI-SPEC § Spacing) with a green-✓ "OK" control (tooltip "Accept (Enter)") connected to `Gui::Control().accept()` and a red-✗ "Cancel" control (tooltip "Cancel (Esc)") connected to `Gui::Control().reject()` (Control.h:98-100) — no new commit logic, palette-role-derived green/red tints only (mirror FwFeatureTreeDelegate.cpp:154-162; NO hex, NO `setStyleSheet` color literal), and NO "SolidWorks" string. Have `FwLayout::mountPropertyManager()` (Task 1) attach the header to the left-hosted Tasks dock at the container level (dock title-band / wrapper, never reparenting the inner `TaskView`). Add `FwPropertyManagerHeader.cpp`/`.h` to `FreeWorks_CPP_SRCS`/`FreeWorks_HPP_SRCS` in `src/Gui/FreeWorks/CMakeLists.txt` additively. Extend `tests/src/Gui/FwPropertyManagerWidget.cpp` with the PROP-01 production asserts from 04-VALIDATION: after `mountPropertyManager()` the host's parent QDockWidget is in `Qt::LeftDockWidgetArea`, `Control().taskPanel()` non-null, ✓/✗ fire `Control().accept()`/`reject()` against a stub `TaskDialog`, `TaskBox`/`TaskGroup` rollout present + expandable, and a `saveState`/`restoreState` round-trip preserving the "Tasks" objectNames. Map the green/red controls onto the SAME accept/reject the hosted `TaskEditControl` `QDialogButtonBox` drives (TaskView.cpp:644-686) — never a parallel button backend.
  </action>
  <verify>
    <automated>if [ -d build ]; then cmake --build build --target FwPropertyManagerWidget_Tests_run && ctest --test-dir build -R "FwPropertyManagerWidget" --output-on-failure; else echo "NO-BUILD-TREE: compile-intended; CMake/grep verification only (Phases 1-3 precedent)"; fi && test -f src/Gui/FreeWorks/FwPropertyManagerHeader.cpp && grep -Eq "Gui::Control\(\)\.accept" src/Gui/FreeWorks/FwPropertyManagerHeader.cpp && grep -Eq "Gui::Control\(\)\.reject" src/Gui/FreeWorks/FwPropertyManagerHeader.cpp && ! grep -En 'setStyleSheet\([^)]*#[0-9a-fA-F]' src/Gui/FreeWorks/FwPropertyManagerHeader.cpp && grep -q "FwPropertyManagerHeader" src/Gui/FreeWorks/CMakeLists.txt && echo "HEADER-OK" && bash tools/fw-string-leak-grep.sh</automated>
    <human-check>No build tree: header authored compile-intended; the live green-✓/red-✗ visual + accept/reject FEEL is deferred to SPIKE_LIVE_CHECKLIST.md Phase 4. Pixel/animation FEEL is manual-only per 04-VALIDATION.</human-check>
  </verify>
  <done>`FwPropertyManagerHeader.{h,cpp}` exists, drives `Gui::Control().accept()`/`reject()` with no new commit logic, is palette-role-only (no hex/`setStyleSheet` color literal) and carries no "SolidWorks" string; it is added to the left-hosted dock at container level; CMake wires it; the extended QTEST asserts left placement + `taskPanel()` reachable + accept/reject fire + rollouts intact + saveState round-trip; the `<automated>` block builds FwPropertyManagerWidget_Tests_run (when a build tree exists) and prints `HEADER-OK`; leak-grep clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted `QMainWindow` dock layout (`saveState`/`restoreState`) | Hosting the Tasks `TaskView` in a left `"Tasks"`-named QDockWidget changes the saved layout; the `"Tasks"` container objectName + the `"Tasks"` inner-widget objectName must be preserved or the saved state fails to restore the host. |
| `.FCStd` document content → hosted panel construction | The hosted `TaskDialog` builds from document objects; FreeWorks adds no parsing — the header only calls `accept()`/`reject()`. |
| (no boundary) network / auth / PII | None — desktop GUI chrome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `Control::taskPanel()` lookup broken by the host (panels stop appearing) | mitigate | Host the registered Tasks widget into a container named `"Tasks"` via `addDockWindow` (so the container objectName == `"Tasks"`, DockWindowManager.cpp:290), preserving the inner-widget `"Tasks"` objectName (Pitfall 4); resolve robustly via `findRegisteredDockWindow` + `Control().taskPanel()` walk-up, never a hardcoded `getDockContainer("Std_TaskView")` (F3). QTEST asserts `Control().taskPanel() != nullptr` and a saveState round-trip after the host. Committed by the SPIKE A1 verdict before this plan runs. |
| T-04-05 | Tampering | Header drives a parallel commit path that bypasses transactions | mitigate | The header calls ONLY `Gui::Control().accept()`/`reject()`; the hosted `TaskDialog` owns its `openTransaction`/`commitTransaction` (D-10). No FreeWorks-side document write. |
| T-04-06 | Denial of Service (resource) | Reveal consumer / host leaks across activate/deactivate cycles, OR host torn down mid-edit during the assureWorkbench round-trip | mitigate | Single reveal consumer owned by a `unique_ptr` static, `reset()` in `unmountPropertyManager()` (mirror `s_ribbonContext`); `QPointer` for held widgets; `unmountPropertyManager()` GUARDED against teardown while `Control().activeDialog() != nullptr` so the A2/F4 WB round-trip does not strand the edit; the consumer re-asserts the host idempotently on `signalInEdit`; leak-grep per task. |
| T-04-07 | (compliance) | Trademark/asset leak in header/layout source | mitigate | No "SolidWorks" identifier; palette-only color (no hex/`setStyleSheet` literal); leak-grep clean. |

All `low`/`medium`; none `high`. T-04-04 (panels-stop-appearing) and T-04-06 (mid-edit teardown) are the highest-attention items and are gated by the SPIKE A1 verdict + the QTEST `taskPanel()` assertion + the deactivated() active-dialog guard. Nothing blocks.
</threat_model>

<verification>
- `ctest -R "FwPropertyManagerWidget"` passes (or, no build tree: compile-intended + grep `MOUNT-WIRED-OK` / `HEADER-OK`).
- After `mountPropertyManager()`: `Control().taskPanel() != nullptr` and its parent QDockWidget is in `Qt::LeftDockWidgetArea`, rollouts intact, saveState round-trips, ✓/✗ fire accept/reject.
- The host survives a simulated `assureWorkbench` round-trip and is NOT torn down while `Control().activeDialog()` is set.
- No `MainWindow.cpp`/`Control.cpp`/`TaskView.cpp` body edit; palette-only; leak-grep clean.
</verification>

<success_criteria>
- PROP-01 core slice end-to-end: panel appears left-docked on command/edit start (event-driven via `signalInEdit`), with a green-✓/red-✗ header driving the existing accept/reject, the hosted panel unmodified inside, non-modal.
- The host is idempotent + reversible (stock workbenches keep their default Tasks panel) + survives the PartDesign WB round-trip.
- `Control().taskPanel()` keeps resolving after the host; saveState round-trips.
</success_criteria>

<output>
Create `.planning/phases/04-propertymanager-panel/04-02-SUMMARY.md` when done, recording the mount API surface (host-the-registered-Tasks-widget mechanism, the robust resolution path, the placeholder consumption, the active-dialog teardown guard), the reveal-consumer ownership + re-assert, the header→Control mapping, and the QTEST assertions for Plans 04-03/04-04 to consume.
</output>
