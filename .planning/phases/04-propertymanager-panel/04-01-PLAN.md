---
phase: 04-propertymanager-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/src/Gui/FwPropertyManager.cpp
  - tests/src/Gui/FwPropertyManagerWidget.cpp
  - tests/src/Gui/FwReferenceBoxStyler.cpp
  - tests/src/Gui/CMakeLists.txt
  - .planning/phases/04-propertymanager-panel/SPIKE.md
  - src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md
autonomous: false
requirements:
  - PROP-01
  - PROP-02
  - TREE-03
  - FLOW-01
user_setup: []

must_haves:
  truths:
    - "The Wave 0 test scaffold reuses the EXISTING tests/src/Gui/FwTestGuiBootstrap.h verbatim (no edit): tests::ensureGuiTestBootstrap() already creates the Gui::Application(false) singleton then imports PartDesignGui/SketcherGui, so Gui::Control(), Gui::TaskView::TaskView, Gui::Selection, PartDesign feature dialogs, and the SketcherGui::ViewProviderSketch type all resolve headlessly (04-RESEARCH § Validation Architecture bootstrap; 04-PATTERNS § test pattern assignments) — mirrors the Phase-2/3 FwRibbon.cpp / FwFeatureTree.cpp SetUpTestSuite discipline"
    - "The D-03 SPIKE resolves Open Question 1 / A1 BEFORE any production mount is committed: it records at runtime (or by source-read on the documentation-approval basis, no build tree in env) whether Gui::Control::taskPanel() resolves the Tasks host via the dock CONTAINER objectName \"Std_TaskView\" or the inner TaskView WIDGET objectName \"Tasks\" (DockWindowManager.cpp:323-327 matches (*it)->objectName(); Control.cpp:55-61 calls getDockWindow(\"Tasks\")). VERDICT uses the literal token 'reuse-and-rehost committed' (PASS: moving the existing container left preserves Control().taskPanel()) or 'thin-adopt committed' (FAIL: fall back to a thin FwPropertyManager that adopts the running TaskView/TaskDialog) — a documentation-only 'reachable' note without the container-vs-widget objectName resolution recorded is NOT a PASS (04-RESEARCH Open Question 1, Pitfall 4)"
    - "The SPIKE records A2 (Pitfall 3): what happens to the FreeWorks left re-host + ribbon when PartDesign ViewProvider::setEdit fires assureWorkbench(\"PartDesignWorkbench\") (ViewProvider.cpp:165) during a tree double-click edit — verdict states whether the idempotent find-or-reuse mount must re-assert on signalInEdit, mirroring the find-or-reuse mountRibbon/mountFeatureManager discipline; live observation deferred to SPIKE_LIVE_CHECKLIST.md"
    - "The SPIKE records A3 (Pitfall 6): the hook for 'which reference box is currently active' — whether the hosted panel exposes an active-box signal/accessor, or FreeWorks must infer it from focus + Gui::Selection selection-gate state; a // SW-FORK HOOK accessor is recorded as the explicit last resort (never the first choice). The box machinery (fill/highlight/auto-expand) stays OWNED by the hosted panel's SelectionFilterGate/SelectionObserver (PartDesignGui::ReferenceSelection, ReferenceSelection.h:42) — FreeWorks restyles the active state only (D-05)"
    - "The SPIKE confirms by source-read the reuse primitives are present unmodified: Gui::TaskView::TaskGroup/TaskBox collapsible rollouts (TaskView.h:67/82), Gui::Control().accept()/reject() slots (Control.h:98-100) wired to the TaskEditControl QDialogButtonBox (TaskView.cpp:644-686), TaskView::keyPressEvent Enter→accept/Esc→reject (TaskView.cpp:393-457), and the non-modal nature of the TaskView dock (it is a QDockWidget, never QDialog::exec) — so NO new task-panel backend is built (D-01/D-04/D-07)"
    - "A GTest (Gui_tests_run, FwPropertyManager.cpp) proves the FLOW-01 decision-core logic with the REAL type system: a pure helper given a view-provider type-name STRING returns 'select-and-handoff' ONLY for the literal \"SketcherGui::ViewProviderSketch\" and 'no-op' for any other type — proving sketch identification needs NO Sketcher include/link (D-11, Pitfall 2; reuse the kSketchViewProviderTypeName constant already defined in FwRibbonContext.cpp:54, never a new #include <Mod/Sketcher/...>) and that NO feature command is auto-launched (D-11)"
    - "A GTest proves the header→Control mapping decision: a pure mapping returns Gui::Control().accept() for the green-✓ activation and Gui::Control().reject() for the red-✗ activation — no new commit/transaction pipeline is introduced (each hosted TaskDialog already wraps its own openTransaction/commitTransaction — D-04/D-10)"
    - "A GTest proves the pink-active-box STATE MACHINE in isolation: exactly one box is 'active' at a time (activating a second deactivates the first), an active box resolves its tone through a QPalette ROLE (no hex literal, no setStyleSheet color literal), a filled box uses QPalette::Highlight, an inactive caption uses QPalette::Disabled/Text, and 'blue' is NEVER the active tone (PROP-02 reserved-color rule, D-06; mirror FwFeatureTreeDelegate.cpp:154-162 palette-derivation)"
    - "A QTEST_MAIN (offscreen) test constructs the test surfaces under a real QApplication (QApplication is required for any QWidget/QDockWidget — cannot run in the bare Gui_tests_run target, mirroring the Phase-2 FwRibbonWidget / Phase-3 FwFeatureTreeWidget split) and asserts the bootstrap brings up a reachable Gui::Control().taskPanel() host that the production left-re-host test (Plan 04-02) will assert against"
    - "All three new test files are wired into tests/src/Gui/CMakeLists.txt additively: FwPropertyManager.cpp and FwReferenceBoxStyler.cpp appended to the Gui_tests_run add_executable list (after FwFeatureTree.cpp line 19), and setup_qt_test(FwPropertyManagerWidget) added next to setup_qt_test(FwFeatureTreeWidget) (line 25) — no existing test target is edited or removed"
    - "tools/fw-string-leak-grep.sh is clean over src/Gui/FreeWorks/ and the new tests/src/Gui/Fw*.cpp — no bare 'SolidWorks' token in any new source or test (only the pre-allow-listed Gui::SolidWorksNavigationStyle identifier is permitted; SPIKE.md prose may name the reference CAD product since the grep covers src/, not .planning/)"
  artifacts:
    - path: "tests/src/Gui/FwPropertyManager.cpp"
      provides: "GTest (Gui_tests_run): FLOW-01 sketch-type-name decision-core (select+no-auto-launch), header→accept/reject mapping, non-modal assertion (no exec()). Covers PROP-01, FLOW-01, D-10 logic"
      contains: "TEST_F"
      min_lines: 50
    - path: "tests/src/Gui/FwReferenceBoxStyler.cpp"
      provides: "GTest (Gui_tests_run): pink active-box state machine (exactly one active, revert on deactivate) + palette-role assertion (no hex, no setStyleSheet color literal). Covers PROP-02"
      contains: "TEST_F"
      min_lines: 40
    - path: "tests/src/Gui/FwPropertyManagerWidget.cpp"
      provides: "QTEST_MAIN (offscreen) construction harness: real QApplication bring-up + reachable Gui::Control().taskPanel() host the Plan 04-02 left-re-host asserts will extend"
      contains: "QTEST_MAIN"
      min_lines: 40
    - path: ".planning/phases/04-propertymanager-panel/SPIKE.md"
      provides: "D-03 verdict: literal token 'reuse-and-rehost committed' (PASS) or 'thin-adopt committed' (FAIL); per-item PASS/FAIL for the A1 dock-name resolution, A2 WB-switch teardown, A3 active-box hook, and the reuse-primitive source confirmation"
      contains: "Verdict"
    - path: "src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md"
      provides: "Appended Phase-4 live-only FEEL obligations (slide-in feel, pink-box pick feel, SW key feel, sketch→feature loop feel, A2 live WB-switch observation) — recorded, not code-completion gating"
      contains: "Phase 4"
  key_links:
    - from: "tests/src/Gui/FwPropertyManager.cpp"
      to: "tests/src/Gui/FwTestGuiBootstrap.h"
      via: "SetUpTestSuite calls tests::ensureGuiTestBootstrap() so Gui::Control/TaskView/Selection + SketcherGui::ViewProviderSketch resolve before the asserts"
      pattern: "ensureGuiTestBootstrap"
    - from: "tests/src/Gui/CMakeLists.txt"
      to: "tests/src/Gui/FwPropertyManagerWidget.cpp"
      via: "setup_qt_test(FwPropertyManagerWidget) so a QApplication exists for QWidget/QDockWidget construction"
      pattern: "FwPropertyManagerWidget"
    - from: ".planning/phases/04-propertymanager-panel/SPIKE.md"
      to: "src/Gui/Control.cpp"
      via: "the A1 verdict records whether getDockWindow(\"Tasks\") resolves the container or widget objectName — the resolution Plan 04-02's mountPropertyManager move strategy depends on"
      pattern: "getDockWindow"
---

<objective>
Stand up the Wave 0 test scaffold AND run the time-boxed D-03 container spike that resolves the keystone foundation decision for this phase: can FreeCAD's existing `Gui::Control` / `Gui::TaskView::TaskView` task-panel host be **re-hosted in the LEFT dock area** (instead of its default right "Tasks" dock) — with collapsible `TaskBox`/`TaskGroup` rollouts and `accept()`/`reject()` intact — **without editing `Control.cpp` / `TaskView.cpp` bodies** (CONTEXT D-01/D-02/D-03; 04-RESEARCH § Open Questions)? The spike must resolve the three spike-gated unknowns the full build depends on: **A1** — does `Control::taskPanel()`'s `getDockWindow("Tasks")` lookup resolve by the dock *container* objectName (`"Std_TaskView"`) or the inner *widget* objectName (`"Tasks"`), and therefore does moving the existing container left preserve the lookup (the single most load-bearing assumption — 04-RESEARCH Open Question 1 / Pitfall 4); **A2** — does PartDesign `setEdit`'s `assureWorkbench("PartDesignWorkbench")` tear down the FreeWorks left re-host / ribbon mid-edit, and must the idempotent mount re-assert (Pitfall 3); **A3** — what hook tells FreeWorks which reference box is currently active so exactly one renders pink (Pitfall 6). The spike's exit criteria make the reuse-vs-fallback choice explicit: PASS → `SPIKE.md` records `reuse-and-rehost committed` and moving the existing `"Std_TaskView"` container left is the committed engine for Plans 04-02/04-03/04-04; FAIL → record `thin-adopt committed` and commit to the CONCRETE fallback (a thin `FwPropertyManager` container that **adopts the running `TaskView`/`TaskDialog`** — still additive, still reusing the `TaskDialog` machinery, NEVER a from-scratch task system).

Purpose: De-risk the one genuinely uncertain part of an otherwise ~80%-reuse phase (04-RESEARCH § Summary: the moment the dock is left-hosted, ~80% of the success criteria are satisfied with no new mechanism) before any production mount is written, and establish the headless GTest + offscreen QTEST harness Plans 04-02/04-03/04-04 sample against (mirrors the Phase-2 native-ribbon and Phase-3 tree-reuse spike-gate structure exactly).
Output: three new test files (`FwPropertyManager.cpp` GTest logic, `FwReferenceBoxStyler.cpp` GTest state-machine, `FwPropertyManagerWidget.cpp` QTEST_MAIN construction) wired additively into `tests/src/Gui/CMakeLists.txt`, `SPIKE.md` recording the D-03 verdict (`reuse-and-rehost committed`/`thin-adopt committed`) with explicit A1/A2/A3 resolution, and the Phase-4 live FEEL obligations appended to `src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-propertymanager-panel/04-CONTEXT.md
@.planning/phases/04-propertymanager-panel/04-RESEARCH.md
@.planning/phases/04-propertymanager-panel/04-PATTERNS.md
@.planning/phases/04-propertymanager-panel/04-UI-SPEC.md
@.planning/phases/04-propertymanager-panel/04-VALIDATION.md
@.planning/phases/02-commandmanager-ribbon/SPIKE.md
</context>

<artifacts_this_phase_produces>
Symbols/files created across Phase 4 (this plan creates the test scaffold + spike verdict; no production FreeWorks symbol is committed until the spike PASSes):

- **This plan (04-01):** `tests/src/Gui/FwPropertyManager.cpp` (GTest, added to `Gui_tests_run`): FLOW-01 sketch-type-name decision-core, header→`Control().accept()`/`reject()` mapping, non-modal (no `exec()`) assertion. `tests/src/Gui/FwReferenceBoxStyler.cpp` (GTest): pink active-box state machine + palette-role-not-hex assertion. `tests/src/Gui/FwPropertyManagerWidget.cpp` (QTEST_MAIN, offscreen, registered via `setup_qt_test`): construction harness + reachable `Control().taskPanel()`. `SPIKE.md` (D-03 verdict `reuse-and-rehost committed`/`thin-adopt committed` + A1/A2/A3 resolution). Appended Phase-4 obligations in `src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md`. CMake edits to `tests/src/Gui/CMakeLists.txt` (`Gui_tests_run` list + `setup_qt_test(FwPropertyManagerWidget)`).
- **Plan 04-02 (later, gated on this SPIKE):** `FwLayout::mountPropertyManager()`/`unmountPropertyManager()` (move the `"Std_TaskView"` dock container to `Qt::LeftDockWidgetArea`, idempotent find-or-reuse; reverse to `Qt::RightDockWidgetArea` on deactivate); `FwPropertyManagerHeader.{h,cpp}` (green-✓/red-✗ header band → `Control().accept()`/`reject()`, palette-only); the `signalInEdit` reveal consumer; `FwWorkbench::activated()`/`deactivated()` wiring.
- **Plan 04-03 (later):** `FwReferenceBoxStyler.{h,cpp}` (pink active-box restyle, reads `Gui::Selection`/focus); `FwPropertyKeyFilter.{h,cpp}` (D-10 Tab traversal + Enter→accept/Esc→reject container-level guarantee).
- **Plan 04-04 (later):** the FLOW-01 sketch-exit handler (extend `FwRibbonContext` or a sibling `signalResetEdit` consumer: select finished sketch + Features-tab handoff, no auto-launch); the TREE-03 double-click→left-panel integration confirmation.

> **Downstream gate:** Plans 04-02/04-03/04-04 require this plan's `SPIKE.md` to record the D-03 verdict. If it reads `reuse-and-rehost committed`, moving the existing `"Std_TaskView"` container left is the committed engine; if it reads `thin-adopt committed`, those plans build on the CONCRETE thin-adopt fallback (a thin `FwPropertyManager` that adopts the running `TaskView`/`TaskDialog` widgets — NOT a from-scratch task system). They must NOT proceed on the container-move strategy merely because 04-01 ran.

> **Concrete fallback (committed if the spike FAILs A1):** if `getDockWindow("Tasks")` resolves by an objectName that the container move would break, the FAIL fallback is the thin-adopt path: a `FwPropertyManager` QWidget registered under `Fw_PropertyManager` that, on `Control().showDialog`, adopts the running `TaskView` into the left dock for the edit's lifetime and releases it on close — reusing the `TaskDialog`/`accept`/`reject` machinery, never rebuilding it. A one-line `// SW-FORK HOOK` in `Control.cpp`/`TaskView.cpp` is the final escalation and must be flagged for developer approval.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — GTest logic (FLOW-01 decision + header→accept/reject mapping + non-modal) and GTest pink-box state machine, plus QTEST_MAIN construction harness, split across targets and wired into CMake</name>
  <files>tests/src/Gui/FwPropertyManager.cpp, tests/src/Gui/FwReferenceBoxStyler.cpp, tests/src/Gui/FwPropertyManagerWidget.cpp, tests/src/Gui/CMakeLists.txt</files>
  <read_first>
    - tests/src/Gui/FwRibbon.cpp (GTest analog: `SetUpTestSuite()` calls `tests::ensureGuiTestBootstrap()`; `constexpr const char*` ID/type constants; the `kSketchVpTypeName`/`kSketchViewProviderTypeName` type-name-literal precedent — mirror it; the intentionally-empty allow-list guard pattern)
    - tests/src/Gui/FwFeatureTree.cpp (GTest analog: REAL-engine logic asserts after bootstrap; how a PartDesign document is built headlessly to exercise real types)
    - tests/src/Gui/FwRibbonWidget.cpp (EXACT QTEST analog: `class testFwRibbonWidget : public QObject` with ctor calling `ensureGuiTestBootstrap()`, `private Q_SLOTS`, the `ensureRealMainWindow()` leaked-singleton helper, `QTEST_MAIN(...)` + `#include "FwRibbonWidget.moc"` at EOF)
    - tests/src/Gui/FwFeatureTreeWidget.cpp (QTEST analog: `initTestCase`/`init`/`cleanup`/`test_*` private slots; a `Probe*` subclass to expose protected seams; real-QApplication QWidget construction)
    - tests/src/Gui/FwTestGuiBootstrap.h (REUSE VERBATIM, no edit: `tests::ensureGuiTestBootstrap()` creates `Gui::Application(false)` then imports PartDesignGui/SketcherGui — confirm it loads the modules so `Gui::Control`, `Gui::TaskView::TaskView`, `Gui::Selection`, and `SketcherGui::ViewProviderSketch` resolve)
    - tests/src/Gui/CMakeLists.txt (lines 7-25: the `Gui_tests_run` add_executable list — add `FwPropertyManager.cpp` and `FwReferenceBoxStyler.cpp` after `FwFeatureTree.cpp` line 19; the `setup_qt_test(FwFeatureTreeWidget)` call line 25 — add `setup_qt_test(FwPropertyManagerWidget)` next to it)
    - tests/CMakeLists.txt (the `function(setup_qt_test)` harness: QTEST_MAIN + links FreeCADApp/FreeCADGui/QtTest + `QT_QPA_PLATFORM=offscreen`)
    - src/Gui/Control.h lines 54,68,76,98-100 (`ControlSingleton`/`Control()`; `taskPanel()` → `TaskView`; `accept()`/`reject()`/`closeDialog()` slots — the mapping the header→Control test asserts)
    - src/Gui/TaskView/TaskView.h lines 67,82,163,241 (`TaskGroup`/`TaskBox` collapsible rollouts; `TaskView` host; `accept()`/`reject()` — confirm the reuse primitives the spike cites)
    - src/Gui/FreeWorks/FwRibbonContext.cpp line 54 (the existing `kSketchViewProviderTypeName` constant `"SketcherGui::ViewProviderSketch"` — REUSE this exact literal in the FLOW-01 decision-core test; do NOT add a Sketcher include — Pitfall 2)
    - src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp lines 154-162 (the palette-DERIVED state-color precedent: `option.palette.color(QPalette::Disabled, QPalette::Text)` etc. — the pink-active-box state-machine test asserts the styler derives its tone the same way, never a hex/`setStyleSheet` literal)
    - .planning/phases/04-propertymanager-panel/04-PATTERNS.md § "Test Pattern Assignments" (the exact GTest/QTEST class skeletons + bootstrap discipline)
    - .planning/phases/04-propertymanager-panel/04-VALIDATION.md § "Per-Task Verification Map" + § "Wave 0 Requirements" (wire each assertion to its Req row)
  </read_first>
  <behavior>
    - GTest (Gui_tests_run, FwPropertyManager.cpp), after `ensureGuiTestBootstrap()` in `SetUpTestSuite()`:
      - FLOW-01 decision-core (D-11, Pitfall 2): a pure helper that takes a view-provider type-name STRING returns a `select-and-handoff` decision ONLY for the literal `"SketcherGui::ViewProviderSketch"` and a `no-op` decision for any other type-name; assert it never returns a `launch-feature-command` decision for any input (D-11 forbids auto-launch). The test uses the `kSketchViewProviderTypeName` constant, never a `#include <Mod/Sketcher/...>`.
      - Header→Control mapping (PROP-01, D-04): a pure mapping returns the `Gui::Control().accept()` action for the green-✓ activation and `Gui::Control().reject()` for the red-✗ activation — assert the two map to distinct Control slots and that NO separate transaction/commit pipeline is referenced (the hosted TaskDialog owns its own transaction — D-10).
      - Non-modal assertion (PROP-01, D-07): assert (by source-contract / type check available at this scaffold stage) that the panel host is a `QDockWidget`-hosted `TaskView`, never a `QDialog::exec()` path — encode the expectation so Plan 04-02's production mount is held to non-modality.
    - GTest (Gui_tests_run, FwReferenceBoxStyler.cpp), after `ensureGuiTestBootstrap()`:
      - Pink-active state machine (PROP-02, D-05/D-06): model the active-box state as a pure state machine (no widget needed for the logic): activating box B deactivates the previously-active box A (exactly one active at a time); deactivating reverts to no-active; assert the active state resolves a tone through a `QPalette` ROLE (the test constructs a `QPalette` and checks the styler picks a derived role-color), a filled box uses `QPalette::Highlight`, an inactive caption uses `QPalette::Disabled, QPalette::Text`.
      - Reserved-color rule (PROP-02): assert the active tone is NOT the palette's blue/link-style role and that the styler exposes no hardcoded hex string / `setStyleSheet` color literal (string-search the styler's declared API surface at this stage; the full assertion lands in Plan 04-03 against the real `FwReferenceBoxStyler`).
    - QTEST_MAIN (offscreen, FwPropertyManagerWidget.cpp), `class testFwPropertyManagerWidget : public QObject` with `private Q_SLOTS`:
      - `initTestCase()` calls `ensureGuiTestBootstrap()` and ensures a real `MainWindow` singleton (mirror `FwRibbonWidget.cpp` `ensureRealMainWindow()`).
      - A `test_bootstrap_taskPanel_reachable()` asserts `Gui::Control().taskPanel()` is reachable (non-null) under the offscreen QApplication — the baseline the Plan 04-02 left-re-host test extends to assert the LEFT area. (Construction cannot run in the bare `Gui_tests_run` target — no QApplication — mirroring the Phase-2/3 widget-test split.)
      - EOF: `QTEST_MAIN(testFwPropertyManagerWidget)` + `#include "FwPropertyManagerWidget.moc"`.
    - CMake (tests/src/Gui/CMakeLists.txt): append `FwPropertyManager.cpp` and `FwReferenceBoxStyler.cpp` to the `Gui_tests_run` add_executable list; add `setup_qt_test(FwPropertyManagerWidget)` next to `setup_qt_test(FwFeatureTreeWidget)`. Additive only — no existing line removed.
  </behavior>
  <action>
Create the three Wave 0 test files following the Phase-2/3 test discipline exactly. In `FwPropertyManager.cpp` write a GTest `TEST_F` fixture whose `SetUpTestSuite()` calls `tests::ensureGuiTestBootstrap()` (reuse `FwTestGuiBootstrap.h` verbatim), then encode three logic groups: the FLOW-01 sketch-type-name decision-core keyed on the reused `kSketchViewProviderTypeName` literal `"SketcherGui::ViewProviderSketch"` (select-and-handoff for that type, no-op otherwise, never auto-launch — D-11), the header→`Gui::Control().accept()`/`reject()` mapping (✓→accept, ✗→reject, distinct slots, no new commit pipeline — D-04), and the non-modal contract (host is a `QDockWidget`/`TaskView`, never `QDialog::exec` — D-07). In `FwReferenceBoxStyler.cpp` write a GTest modeling the pink-active-box state machine (exactly one active; revert on deactivate; active tone derived through a `QPalette` role mirroring `FwFeatureTreeDelegate.cpp:154-162`; filled→`QPalette::Highlight`; inactive→`QPalette::Disabled/Text`; never blue; no hex/`setStyleSheet` literal — PROP-02/D-06). In `FwPropertyManagerWidget.cpp` write a `QTEST_MAIN` `QObject` test class with `private Q_SLOTS` (`initTestCase` calling `ensureGuiTestBootstrap()` + a real-`MainWindow` helper like `FwRibbonWidget.cpp`'s `ensureRealMainWindow()`), a `test_bootstrap_taskPanel_reachable()` asserting `Gui::Control().taskPanel()` is non-null offscreen, and the `QTEST_MAIN(...)` + `.moc` include at EOF. Wire all three into `tests/src/Gui/CMakeLists.txt` additively (two filenames into `Gui_tests_run`, one `setup_qt_test(FwPropertyManagerWidget)`). These tests are RED scaffolds: the production symbols they will fully assert against (`FwPropertyManagerHeader`, `FwReferenceBoxStyler`, `mountPropertyManager`) are created in Plans 04-02/04-03, so at this stage the tests assert the reuse primitives + decision-core logic that need no FreeWorks production code, and leave the production-symbol asserts as the Wave 0 gap the later plans close.
  </action>
  <verify>
    <automated>cmake --build build --target Gui_tests_run 2>/dev/null && ctest --test-dir build -R "FwPropertyManager|FwReferenceBoxStyler" --output-on-failure; echo "If no build tree: confirm the three .cpp exist, are syntactically C++20, and appear in tests/src/Gui/CMakeLists.txt"; grep -q "FwPropertyManager.cpp" tests/src/Gui/CMakeLists.txt && grep -q "FwReferenceBoxStyler.cpp" tests/src/Gui/CMakeLists.txt && grep -q "setup_qt_test(FwPropertyManagerWidget)" tests/src/Gui/CMakeLists.txt && echo "CMAKE-WIRED-OK"; { sed -i.bak -e 's/^wave_0_complete: false/wave_0_complete: true/' -e 's/^nyquist_compliant: false/nyquist_compliant: true/' .planning/phases/04-propertymanager-panel/04-VALIDATION.md && rm -f .planning/phases/04-propertymanager-panel/04-VALIDATION.md.bak; }; grep -q '^wave_0_complete: true' .planning/phases/04-propertymanager-panel/04-VALIDATION.md && echo "VALIDATION-FLAGS-SET"</automated>
    <human-check>If no build tree is available in env (consistent with Phases 1-3), the tests are authored compile-intended and the CMake wiring is grep-verified; live ctest deferred to the build-tree checklist.</human-check>
  </verify>
  <done>Three test files exist (`FwPropertyManager.cpp`, `FwReferenceBoxStyler.cpp` in `Gui_tests_run`; `FwPropertyManagerWidget.cpp` via `setup_qt_test`), each calls `ensureGuiTestBootstrap()`, the FLOW-01/header/state-machine logic asserts are present using the reused sketch type-name literal and palette-role derivation, CMake grep-verification prints `CMAKE-WIRED-OK`, `04-VALIDATION.md` frontmatter is flipped to `wave_0_complete: true` and `nyquist_compliant: true` (`VALIDATION-FLAGS-SET`), and `tools/fw-string-leak-grep.sh` is clean over the new test files.</done>
</task>

<task type="auto">
  <name>Task 2: D-03 container spike — resolve A1 (dock-name resolution), A2 (WB-switch teardown), A3 (active-box hook) by source-read; write SPIKE.md verdict and append Phase-4 obligations to SPIKE_LIVE_CHECKLIST.md</name>
  <files>.planning/phases/04-propertymanager-panel/SPIKE.md, src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md</files>
  <read_first>
    - .planning/phases/02-commandmanager-ribbon/SPIKE.md (the EXACT structure to mirror: "Approval basis (READ FIRST — documentation approval)", evidence classes [CODE]/[RESEARCH]/[LIVE-DEFERRED], per-item PASS/FAIL, the literal-token Verdict, the carried open obligation, the Sign-off block)
    - .planning/phases/04-propertymanager-panel/04-RESEARCH.md § "Open Questions" (the 3 questions = A1/A2/A3 the spike resolves) + § "Assumptions Log" (A1-A6) + § "Common Pitfalls" (Pitfall 3/4/6 detail)
    - src/Gui/Control.cpp lines 55-61 (`taskPanel()` calls `getDockWindow("Tasks")` — the A1 resolution site) and 149-195 (`showDialog` — confirm the host the spike must preserve)
    - src/Gui/DockWindowManager.cpp lines 251-347 (`getDockWindow`/`getDockContainer`: which objectName is matched — the A1 answer; `addDockWidget` find-or-reuse the move uses)
    - src/Gui/MainWindow.cpp lines 609-633 (`setupTaskView()`: the `TaskView` widget objectName `"Tasks"`, the dock container registered `"Std_TaskView"`, docked right by default — the names the move must preserve)
    - src/Mod/PartDesign/Gui/ViewProvider.cpp lines 114-176 (`setEdit` → `Control().showDialog` chain) and line 165 (`assureWorkbench("PartDesignWorkbench")` — the A2 WB-switch site) and 190-211 (`unsetEdit` restore)
    - src/Mod/PartDesign/Gui/ReferenceSelection.h lines 42-129 (`PartDesignGui::ReferenceSelection : Gui::SelectionFilterGate` — the in-panel reference-box machinery; the A3 hook question is whether this or the panel exposes "which box is active")
    - src/Gui/TaskView/TaskView.cpp lines 393-457 (`keyPressEvent` Enter→accept/Esc→reject, incl. the macOS workaround) and 644-686 (the `TaskEditControl` `QDialogButtonBox` accept/reject wiring) — the reuse-primitive confirmation the spike records
    - src/Gui/FreeWorks/FwLayout.cpp lines 117-146 (`mountFeatureManager` find-or-reuse) — the mount discipline the A1 PASS path commits to mirror for the container move
    - src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md (the existing Phase 1-3 live obligations — APPEND a Phase 4 section, do not rewrite existing content)
  </read_first>
  <behavior>
    - SPIKE.md opens with the same "documentation approval" basis block as 02's SPIKE.md (no build tree / GUI in env; the live container-move round-trip and WB-switch observation are deferred to SPIKE_LIVE_CHECKLIST.md; PASS marks granted on [CODE]+[RESEARCH] basis).
    - A1 item (the load-bearing one): record by source-read whether `DockWindowManager::getDockWindow("Tasks")` matches the dock-container objectName or the inner-widget objectName, and therefore whether moving the existing `"Std_TaskView"` container to the left area preserves `Control().taskPanel() != nullptr`. State the resolution explicitly with the file:line evidence (DockWindowManager.cpp:323-327 vs MainWindow.cpp:620-628). If the source-read shows the move preserves the lookup → A1 PASS; if it shows the move would break it → A1 FAIL → thin-adopt fallback.
    - A2 item: record what `setEdit`'s `assureWorkbench("PartDesignWorkbench")` does to `FwWorkbench::activated()`/`deactivated()` during a double-click edit (does it tear down + re-mount the ribbon/left dock?), and the disposition: the find-or-reuse mount must re-assert on `signalInEdit` if the WB round-trip tears it down (idempotent mounts already do — `mountRibbon`/`mountFeatureManager`). Live observation → SPIKE_LIVE_CHECKLIST.md.
    - A3 item: record whether the hosted panel exposes a clean "active reference box" hook; if not, the disposition is to infer from focus + `Gui::Selection`/selection-gate state, with a `// SW-FORK HOOK` accessor as the explicit last resort. The box machinery stays owned by the hosted panel (`SelectionFilterGate`/`SelectionObserver`) — FreeWorks restyles only.
    - Reuse-primitive confirmation item: record [RESEARCH]+[CODE] that `TaskGroup`/`TaskBox` rollouts, `Control().accept()`/`reject()`, `TaskView::keyPressEvent`, and the non-modal `QDockWidget` host are present unmodified.
    - VERDICT: the literal token `reuse-and-rehost committed` (PASS) or `thin-adopt committed` (FAIL), with the same "no second decision round" framing as 02.
    - SPIKE_LIVE_CHECKLIST.md gets an appended `## Phase 4 — PropertyManager live FEEL obligations` section listing: slide-in reveal feel, pink active-box pick feel, SW Enter/Esc/Tab feel, sketch→feature loop feel, and the A2 live WB-switch observation — each marked [LIVE-DEFERRED], not code-completion gating.
  </behavior>
  <action>
Write `.planning/phases/04-propertymanager-panel/SPIKE.md` mirroring `02-commandmanager-ribbon/SPIKE.md` structure exactly: the "Approval basis (READ FIRST — documentation approval)" preamble (no build tree in env, PASS marks on [CODE]+[RESEARCH], live items deferred), then a per-item PASS/FAIL section resolving A1 (does `getDockWindow("Tasks")` resolve by container or widget objectName — cite DockWindowManager.cpp:323-327 and MainWindow.cpp:620-628; state whether moving the `"Std_TaskView"` container left preserves `Control().taskPanel()`), A2 (the `assureWorkbench("PartDesignWorkbench")` mid-edit teardown question — cite ViewProvider.cpp:165 — with the re-assert-on-`signalInEdit` disposition), A3 (the active-box hook — cite ReferenceSelection.h:42 — with the focus+selection-gate inference disposition and `// SW-FORK HOOK` last resort), and the reuse-primitive confirmation (TaskView.h:67/82, Control.h:98-100, TaskView.cpp:393-457). Close with the VERDICT using the literal token `reuse-and-rehost committed` or `thin-adopt committed`, a carried open-obligation note pointing at `SPIKE_LIVE_CHECKLIST.md`, and a Sign-off block with the resume signal. Then APPEND a `## Phase 4 — PropertyManager live FEEL obligations` section to `src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md` (do not rewrite existing Phase 1-3 content) listing the five [LIVE-DEFERRED] items. Do NOT write any production FreeWorks source in this task — the spike is a verdict document; production mounts land in Plan 04-02 gated on this verdict.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "reuse-and-rehost committed|thin-adopt committed" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -q "getDockWindow" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -q "assureWorkbench" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -q "Phase 4" src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md && echo "SPIKE-VERDICT-RECORDED"; bash tools/fw-string-leak-grep.sh 2>/dev/null || echo "leak-grep: run when tool present"</automated>
  </verify>
  <done>`SPIKE.md` exists with the A1/A2/A3 resolution (citing `getDockWindow`/`assureWorkbench`/`ReferenceSelection`), a VERDICT carrying the literal token `reuse-and-rehost committed` or `thin-adopt committed`, and a Sign-off block; `SPIKE_LIVE_CHECKLIST.md` has an appended `Phase 4` section; the grep verification prints `SPIKE-VERDICT-RECORDED`; no production FreeWorks source was written in this task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The D-03 container-spike verdict (`SPIKE.md`) resolving the three spike-gated unknowns — A1 (does moving the existing `"Std_TaskView"` dock container to the left area preserve `Control::taskPanel()`'s `getDockWindow("Tasks")` resolution), A2 (does `assureWorkbench("PartDesignWorkbench")` tear down FreeWorks chrome mid-edit), A3 (the active-reference-box hook) — plus the Wave 0 test scaffold (three new headless/offscreen test files wired into CMake) and the appended Phase-4 live-FEEL obligations. This gates whether Plans 04-02/04-03/04-04 build on the container-move engine (`reuse-and-rehost committed`) or the thin-adopt fallback (`thin-adopt committed`). This mirrors the Phase-2 native-ribbon and Phase-3 tree-reuse spike gates, which were cleared by documentation approval (no build tree / GUI / CI in this environment).</what-built>
  <how-to-verify>
    1. Read `.planning/phases/04-propertymanager-panel/SPIKE.md`. Confirm the A1 item resolves whether `getDockWindow("Tasks")` matches the container objectName (`"Std_TaskView"`) or the widget objectName (`"Tasks"`) with file:line evidence, and states whether the container move preserves `Control().taskPanel()`.
    2. Confirm the VERDICT carries exactly one literal token: `reuse-and-rehost committed` (PASS) or `thin-adopt committed` (FAIL).
    3. Confirm A2 and A3 each have a disposition (re-assert-on-signalInEdit; focus+selection-gate inference with `// SW-FORK HOOK` last resort).
    4. Confirm `src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md` has an appended `Phase 4` section (existing Phase 1-3 content untouched).
    5. Confirm the three test files exist and `tests/src/Gui/CMakeLists.txt` wires them (`CMAKE-WIRED-OK` from Task 1).
  </how-to-verify>
  <resume-signal>Type "approved: reuse-and-rehost" (or "approved: thin-adopt") to commit the engine for Plans 04-02/04-03/04-04, or describe required changes. The approval is on the documentation basis (Phase 1-3 precedent); the live container-move round-trip + WB-switch observation remain open obligations in `SPIKE_LIVE_CHECKLIST.md`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.FCStd` document content → panel/preview construction | A hosted `TaskDialog` builds its UI from the active document's objects; a malformed/untrusted document drives that construction. FreeWorks adds NO new parsing — the hosted panel (App-layer property validation) owns it. |
| 3D-view pick → reference-box fill | Picked geometry crosses into the hosted panel's `SelectionFilterGate`; FreeWorks only reads `Gui::Selection`, never parses raw input. |
| (no boundary) network / auth / PII | None — desktop CAD GUI panel; no network, auth, sessions, secrets, or crypto in Phase 4 scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Document mutation during recompute (panel-driven edit) | mitigate | This plan writes NO document mutation — it is test scaffold + spike verdict only. The disposition is recorded for downstream plans: all edits route through the hosted `TaskDialog`'s `openTransaction`/`commitTransaction` (D-10), never a FreeWorks-side document write during recompute (ARCHITECTURE.md anti-pattern). |
| T-04-02 | Denial of Service (resource) | Throwaway spike harness / test bring-up leak | mitigate | No production widget is constructed in this plan; the offscreen QTEST follows the leak-grep + scoped-construction discipline. `tools/fw-string-leak-grep.sh` runs per task. |
| T-04-03 | (compliance) | Trademark/asset leak in new source/tests | mitigate | No "SolidWorks" identifier in any new `.cpp`/`.h`/test (only the pre-allow-listed `Gui::SolidWorksNavigationStyle`); `SPIKE.md` prose may name the reference CAD product (grep covers `src/`, not `.planning/`). Leak-grep clean. |

All threats are `low` for this plan (test scaffold + documentation verdict; no document mutation, no production widget, no network). None is `high`; nothing blocks.
</threat_model>

<verification>
- `ctest -R "FwPropertyManager|FwReferenceBoxStyler"` builds and runs (or, no build tree: tests compile-intended + CMake grep-verified `CMAKE-WIRED-OK`).
- `SPIKE.md` records the D-03 verdict with the literal token and the A1/A2/A3 resolutions.
- `SPIKE_LIVE_CHECKLIST.md` has the appended Phase 4 obligations.
- `tools/fw-string-leak-grep.sh` clean over new source/tests.
</verification>

<success_criteria>
- Three Wave 0 test files exist, call `ensureGuiTestBootstrap()`, and are wired additively into `tests/src/Gui/CMakeLists.txt`.
- The FLOW-01 decision-core test uses the reused `"SketcherGui::ViewProviderSketch"` literal (no Sketcher include) and never returns an auto-launch decision.
- The pink-box state-machine test derives its active tone through a `QPalette` role (no hex / `setStyleSheet` literal) and never uses blue.
- `SPIKE.md` resolves A1/A2/A3 and carries the verdict token `reuse-and-rehost committed` or `thin-adopt committed`.
- Blocking spike gate cleared (documentation approval, Phase 1-3 precedent); downstream plans know which engine is committed.
</success_criteria>

<output>
Create `.planning/phases/04-propertymanager-panel/04-01-SUMMARY.md` when done, recording the D-03 verdict token, the A1/A2/A3 resolutions, and the Wave 0 test inventory for Plans 04-02/04-03/04-04 to consume.
</output>
