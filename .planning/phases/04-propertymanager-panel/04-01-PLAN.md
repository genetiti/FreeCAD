---
phase: 04-propertymanager-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/src/Gui/FwPropertyManager.cpp
  - tests/src/Gui/FwPropertyManagerWidget.cpp
  - tests/src/Gui/FwReferenceBoxStyler.cpp
  - tests/src/Gui/CMakeLists.txt
  - .planning/phases/04-propertymanager-panel/SPIKE.md
  - src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md
autonomous: false
requirements:
  - PROP-01
  - PROP-02
  - TREE-03
  - FLOW-01
user_setup: []

must_haves:
  truths:
    - "The Wave 0 test scaffold reuses the EXISTING tests/src/Gui/FwTestGuiBootstrap.h verbatim (no edit): tests::ensureGuiTestBootstrap() already creates the Gui::Application(false) singleton then imports PartDesignGui/SketcherGui, so Gui::Control(), Gui::TaskView::TaskView, Gui::Selection, PartDesign feature dialogs, and the SketcherGui::ViewProviderSketch type all resolve headlessly (04-RESEARCH § Validation Architecture bootstrap; 04-PATTERNS § test pattern assignments) — mirrors the Phase-2/3 FwRibbon.cpp / FwFeatureTree.cpp SetUpTestSuite discipline"
    - "The D-03 SPIKE resolves Open Question 1 / A1 BEFORE any production mount is committed, with a DECISIVE verdict token. The A1 item MUST record the LEFT-PLACEMENT MECHANISM correctly (R2-F1 VERIFIED): addDockWindow(\"Tasks\", taskView, Left) will NOT move an ALREADY-docked Tasks panel — DockWindowManager::addDockWindow() returns the existing dock immediately when widget->parentWidget() is already a QDockWidget (DockWindowManager.cpp:256-258), BEFORE the mw->addDockWidget(pos, dw) placement (:278) and the objectName-set (:290), both of which run ONLY on the NEW-dock branch. Stock/PartDesign workbenches dock Tasks RIGHT via Std_TaskView (Workbench.cpp:925, Qt::RightDockWidgetArea), so coming from such a workbench Control().taskPanel() is non-null inside an existing right QDockWidget addDockWindow would never move. So A1 resolves that the LEFT placement is achieved by resolving the host from Gui::Control().taskPanel(), walking UP to its parent QDockWidget, and branching on getMainWindow()->dockWidgetArea(dock): re-dock an existing non-left container left via Gui::getMainWindow()->addDockWidget(Qt::LeftDockWidgetArea, dock) (the came-from-right-dock case), OR create the dock left via addDockWindow(\"Tasks\", taskView, Left) ONLY when taskPanel() has no parent QDockWidget (the never-docked case — true in FreeWorks mode where FwWorkbench::setupDockWindows() (FwWorkbench.cpp:95-124) never returns Std_TaskView, F1). The 4-name disambiguation {registry key 'Std_TaskView' (MainWindow.cpp:628), toggle-action data, dock-CONTAINER objectName 'Tasks', child-WIDGET objectName 'Tasks' (MainWindow.cpp:623)} MUST be recorded, AND that getDockWindow('Tasks') (Control.cpp:55-61; matches the container objectName at DockWindowManager.cpp:325) is PARENT-INDEPENDENT so re-docking preserves Control().taskPanel() != nullptr, and that the managed PropertyManager dock identity is 'Tasks' (R2-F3) with the Fw_PropertyManager placeholder CONSUMED. A1 PASS only with the correct two-branch placement mechanism + the parent-independent-lookup + the 'Tasks' managed-identity recorded; a 'host the registered widget via addDockWindow and the container becomes Tasks' note that ignores the can't-move-an-existing-dock fact (R2-F1) is NOT a PASS. VERDICT uses the literal token 'reuse-and-rehost committed' (PASS) or 'thin-adopt committed' (FAIL: fall back to a thin FwPropertyManager that adopts the running TaskView/TaskDialog)"
    - "The SPIKE gives A2 a DECISIVE disposition with a verdict token, resolving the deactivated-before-activated-before-showDialog ORDERING (R2-F2 VERIFIED) for the DOCK PLACEMENT — and A2 is now SUBSUMED into the broader A4 survivability gate below (the dock placement is one of the things that must survive; A4 generalizes the same ordering fact to the chrome + handlers mounted ON the dock — R3-ROOT, and the same too-late ordering defeats a pre-checked flag — R4-BLOCKER): Application::activateWorkbench() calls oldWb->deactivated() (Application.cpp:1984) BEFORE newWb->activated() (Application.cpp:2006); PartDesign ViewProvider::setEdit() calls assureWorkbench('PartDesignWorkbench') (ViewProvider.cpp:165) BEFORE Control().showDialog() (ViewProvider.cpp:175); signalInEdit fires only after startEditing() (Document.cpp:680-692). So on a tree double-click FreeWorks deactivated() runs while Control().activeDialog() is STILL null AND before signalInEdit fires — a teardown guard on activeDialog()!=nullptr is TOO LATE and would NOT protect the dock, and a pre-checked edit-in-progress flag is ALSO too late (still false at that instant); the edit would land in stock PartDesign's right Tasks dock. The A2 item MUST resolve the DOCK-PLACEMENT teardown policy accounting for this ordering: the 'Tasks' dock placement is a MainWindow-level QDockWidget, so unmountPropertyManager() must NOT reverse the left placement on deactivated() AT ALL — leaving the dock LEFT is harmless under stock workbenches and survives the assureWorkbench round-trip so the edit lands left regardless of active workbench. deactivated() may SCHEDULE a deferred teardown but MUST NOT move the dock back right synchronously. The signalInEdit reveal consumer (fired after startEditing, within the same synchronous turn) RE-ASSERTS the idempotent left placement AND cancels the deferred teardown when the edit lands. A2 PASS = 'dock stays left on deactivated() (no synchronous right-move); deferred teardown cancelled + left re-asserted on signalInEdit; the activeDialog() guard AND a pre-checked flag are both recorded as insufficient per the ordering'. A2 FAIL = the dock cannot survive → escalate. Live observation deferred to SPIKE_LIVE_CHECKLIST.md, but the source-read disposition is a PASS/FAIL gate here"
    - "The SPIKE gives A4 — the LIFECYCLE/SURVIVABILITY gate (R3-ROOT, the keystone of round 3, now sharpened by R4-BLOCKER) — a DECISIVE verdict token, resolving the ONE question that both R3 blockers share AND the R4 ordering defect that defeats a pre-checked flag: do FreeWorks chrome (the ribbon's s_ribbonContext, the PropertyManager left-host + header, the key filter, the pink styler) AND the FLOW-01 signalResetEdit handler SURVIVE an EDIT-TIME workbench switch (PartDesign assureWorkbench / Sketcher edit), or are they torn down before the edit/exit fires? VERIFIED ground truth: FwWorkbench::deactivated() calls FwLayout::unmountRibbon() (FwWorkbench.cpp:66/71) which does s_ribbonContext.reset() (FwLayout.cpp:286), AND (per the 04-02/04-03 plans as written) deactivated() would also call unmountPropertyManager() releasing the header/key-filter/styler; editing a feature TRANSIENTLY activates ANOTHER workbench DURING setEdit() BEFORE showDialog() — PartDesign assureWorkbench('PartDesignWorkbench') (ViewProvider.cpp:165/175), Sketcher edit → SketcherWorkbench (ViewProviderSketch.cpp:635, activated :3963/:4001); Qt's activateWorkbench() runs oldWb->deactivated() (Application.cpp:1984) BEFORE newWb->activated() (:2006); signalResetEdit/signalInEdit fire LATER (Document.cpp:680-692, 747) — but WITHIN the same synchronous activateWorkbench call stack (one event-loop turn). R4-BLOCKER VERIFIED: because signalInEdit fires AFTER deactivated() within that one turn, a pre-checked edit-in-progress flag (set on signalInEdit) is STILL FALSE when deactivated()→unmountPropertyManager() runs, so a flag-guarded teardown would STILL strip the chrome before the edit. CONSEQUENCE (R3-BLOCKER1): the FLOW-01 sketch-exit handler, if hung off the s_ribbonContext signalResetEdit consumer (FwRibbonContext.cpp:111-121), is DESTROYED before sketch exit fires, so the real handler never runs (the isolation test passes while the product fails). CONSEQUENCE (R3-BLOCKER2): if the header/key-filter/styler are released in unmountPropertyManager() wired into deactivated(), a tree double-click lands in a left RAW Tasks dock WITHOUT the FreeWorks chrome, failing PROP-01/PROP-02/D-10 in the exact round-trip the plans claim to fix — preserving dock PLACEMENT (the r2 fix) is NOT enough; the BEHAVIOR mounted on it is gone too. A4 MUST DECIDE the correct SURVIVABLE install/teardown policy by source-read + a live checklist item, and the CHOSEN mechanism is the DEFERRED, CANCELLABLE teardown: deactivated()→unmountPropertyManager() does NOT tear down inline and does NOT rely on a pre-checked flag (too late, R4-BLOCKER) — it SCHEDULES the true-exit teardown via a cancellable queued call (QTimer::singleShot(0, …) or an equivalent owned/restartable QTimer) and remembers the pending-teardown handle; because the WHOLE transient sequence (assureWorkbench → deactivated() → activated() → setEdit() → tryStartEditing() → emit signalInEdit) runs SYNCHRONOUSLY within ONE event-loop turn, the queued teardown does NOT fire until control returns to the event loop, by which time signalInEdit (and the FreeWorks re-activated() that re-mounts) have already CANCELLED it — so the chrome + the FLOW-01 handler SURVIVE; for a GENUINE no-edit workbench switch no signalInEdit fires, so the uncancelled queued teardown runs on the next turn and releases the chrome normally. The s_propertyReveal / FLOW-01 handler (04-04), the styler/key-filter (04-03), AND the dock placement (A2) all ride this single deferred-cancellable teardown — none torn down synchronously in deactivated(). A4 PASS = 'overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)' with the chosen mechanism recorded for 04-02/04-03/04-04 to implement; A4 FAIL = 'overlays-lost' → escalate. The A4 verdict ALSO records, as a CROSS-PHASE OBSERVATION (do NOT silently edit shipped phases), that the Phase 2-3 ribbon (s_ribbonContext) uses the SAME synchronous-teardown-on-deactivated pattern (FwLayout.cpp:286) so it LIKELY vanishes during edits too — a pre-existing latent issue for the spike to confirm and flag for a follow-up, never patched here. The live checklist confirms the synchronous-within-one-turn assumption (the basis the singleShot cancellation relies on) at runtime"
    - "The SPIKE gives A3 a DECISIVE disposition with a verdict token, resolving BOTH the active-box hook AND the pink SOURCE against a REAL HOSTED PartDesign 2-FIELD panel (R2-F4 + R2-F6 VERIFIED). R2-F4: Gui::Selection exposes only a selection-gate pointer, not which field armed it; the real two-field active state is PRIVATE (TaskPatternParameters::activeDirectionWidget — TaskPatternParameters.h:91 — set at TaskPatternParameters.cpp:294-307, with two real direction widgets parametersWidget/parametersWidget2 at .h:88-89), so a focus+selection-only styler can pass a SYNTHETIC two-field QTEST and still color the WRONG real box. The A3 item MUST resolve that NO public active-field hook exists, so the recorded mechanism is EITHER a minimal '// SW-FORK HOOK' active-field accessor (the explicit last resort, now justified by R2-F4) OR focus inference VALIDATED against the real TaskPatternParameters linear-pattern panel (two direction widgets). R2-F6: a bare activeReferenceBoxTone()→QColor accessor while FwTheme::apply() stays no-op does NOT satisfy D-06's 'via a QPalette role' (D-06 is locked, 'functional state now'). So A3 MUST also resolve that FwTheme::apply() becomes FUNCTIONAL for this — install the single FreeWorks-owned pink VALUE into a chosen QPalette ROLE on the reference-box widget's LOCAL palette (QPalette has no custom roles, so carry it on the box widget's local palette via a chosen role, reversible — FwTheme.cpp:31-39 is currently a no-op); the styler then READS widget->palette().color(<role>). The pink VALUE still lives once in FwTheme expressed WITHOUT a hex string literal (a QColor(r,g,b) int-RGB constructor or a palette-derived/HSV-shifted value — never QColor("#…"), so D-06's 'never a hex literal' holds at the source site and the 04-03 automated hex-grep covers FwTheme.{h,cpp}); the A3 spike NAMES the exact QPalette role. A3 PASS = 'active-box hook resolved (// SW-FORK HOOK accessor OR focus inference validated against the real TaskPatternParameters panel) + FwTheme::apply() populates the named QPalette role on the box widget's local palette + the styler reads widget->palette().color(<role>)'; A3 FAIL = neither resolvable → escalate"
    - "The SPIKE confirms by source-read the reuse primitives are present unmodified: Gui::TaskView::TaskGroup/TaskBox collapsible rollouts (TaskView.h:67/82), Gui::Control().accept()/reject() slots (Control.h:98-100) wired to the TaskEditControl QDialogButtonBox (TaskView.cpp:644-686), TaskView::keyPressEvent Enter→accept/Esc→reject (TaskView.cpp:393-457), and the non-modal nature of the TaskView dock (it is a QDockWidget, never QDialog::exec) — so NO new task-panel backend is built (D-01/D-04/D-07)"
    - "A GTest (Gui_tests_run, FwPropertyManager.cpp) proves the FLOW-01 decision-core logic with the REAL type system: a pure helper given a view-provider type-name STRING returns 'select-and-handoff' ONLY for the literal \"SketcherGui::ViewProviderSketch\" and 'no-op' for any other type — proving sketch identification needs NO Sketcher include/link (D-11, Pitfall 2; reuse the kSketchViewProviderTypeName constant already defined in FwRibbonContext.cpp:54, never a new #include <Mod/Sketcher/...>) and that NO feature command is auto-launched (D-11)"
    - "A GTest proves the header→Control mapping decision: a pure mapping returns Gui::Control().accept() for the green-✓ activation and Gui::Control().reject() for the red-✗ activation — no new commit/transaction pipeline is introduced (each hosted TaskDialog already wraps its own openTransaction/commitTransaction — D-04/D-10)"
    - "A GTest (Gui_tests_run, FwReferenceBoxStyler.cpp — NOT a separate FwReferenceBoxStyler_Tests_run target, which does not exist: R2-F5, the file is appended to the Gui_tests_run add_executable list, and setup_qt_test is the ONLY thing that creates *_Tests_run targets — tests/CMakeLists.txt:13-17) proves the pink-active-box STATE MACHINE in isolation: exactly one box is 'active' at a time (activating a second deactivates the first), an active box resolves its tone by READING a QPalette ROLE off the box widget's LOCAL palette that FwTheme::apply() populated (R2-F6 — not accessor-equality; no hex literal, no setStyleSheet color literal), a filled box uses QPalette::Highlight, an inactive caption uses QPalette::Disabled/Text, and 'blue' is NEVER the active tone (PROP-02 reserved-color rule, D-06; mirror FwFeatureTreeDelegate.cpp:154-162 palette-derivation; the pink role named by the A3 spike). ctest -R FwReferenceBoxStyler matches this gtest case inside Gui_tests_run"
    - "A QTEST_MAIN (offscreen, target FwPropertyManagerWidget_Tests_run) test constructs the test surfaces under a real QApplication (QApplication is required for any QWidget/QDockWidget — cannot run in the bare Gui_tests_run target, mirroring the Phase-2 FwRibbonWidget / Phase-3 FwFeatureTreeWidget split) and asserts the bootstrap can bring up a TaskView host that the production left-placement test (Plan 04-02) will assert lands left for BOTH the never-docked and came-from-right-dock cases (note: in FreeWorks mode Control().taskPanel() is null until 04-02's mount runs — F1 — so this scaffold asserts the registered Tasks widget is resolvable, the baseline 04-02 extends)"
    - "All three new test files are wired into tests/src/Gui/CMakeLists.txt additively: FwPropertyManager.cpp and FwReferenceBoxStyler.cpp appended to the Gui_tests_run add_executable list (after FwFeatureTree.cpp line 19 — so the styler GTest runs INSIDE Gui_tests_run; there is NO FwReferenceBoxStyler_Tests_run target, R2-F5), and setup_qt_test(FwPropertyManagerWidget) added next to setup_qt_test(FwFeatureTreeWidget) (line 25) — which creates the build target FwPropertyManagerWidget_Tests_run (tests/CMakeLists.txt:13-17); no existing test target is edited or removed"
    - "tools/fw-string-leak-grep.sh is clean over src/Gui/FreeWorks/ and the new tests/src/Gui/Fw*.cpp — no bare 'SolidWorks' token in any new source or test (only the pre-allow-listed Gui::SolidWorksNavigationStyle identifier is permitted; SPIKE.md prose may name the reference CAD product since the grep covers src/, not .planning/)"
  artifacts:
    - path: "tests/src/Gui/FwPropertyManager.cpp"
      provides: "GTest (Gui_tests_run): FLOW-01 sketch-type-name decision-core (select+no-auto-launch), header→accept/reject mapping, non-modal assertion (no exec()). Covers PROP-01, FLOW-01, D-10 logic"
      contains: "TEST_F"
      min_lines: 50
    - path: "tests/src/Gui/FwReferenceBoxStyler.cpp"
      provides: "GTest (Gui_tests_run — NOT a separate FwReferenceBoxStyler_Tests_run target, R2-F5): pink active-box state machine (exactly one active, revert on deactivate) + palette-role assertion (READ the role off the box widget's local palette FwTheme::apply() populated — R2-F6; no hex, no setStyleSheet color literal). Covers PROP-02"
      contains: "TEST_F"
      min_lines: 40
    - path: "tests/src/Gui/FwPropertyManagerWidget.cpp"
      provides: "QTEST_MAIN (offscreen, target FwPropertyManagerWidget_Tests_run): real QApplication bring-up + resolvable registered Tasks TaskView the Plan 04-02 left-placement test asserts will extend (both never-docked and came-from-right-dock)"
      contains: "QTEST_MAIN"
      min_lines: 40
    - path: ".planning/phases/04-propertymanager-panel/SPIKE.md"
      provides: "D-03 verdict: literal token 'reuse-and-rehost committed' (PASS) or 'thin-adopt committed' (FAIL); per-item PASS/FAIL with a DECISIVE verdict token for A1 (two-branch left-placement mechanism — re-dock-existing-left vs create-left, R2-F1 + the 4-name disambiguation + parent-independent getDockWindow('Tasks') + 'Tasks' managed identity), A2 (dock placement stays left on deactivated(), no synchronous right-move; deferred teardown cancelled + re-assert on signalInEdit; the activeDialog() guard AND a pre-checked flag recorded as insufficient per the deactivated-before-activated-before-signalInEdit ordering, R2-F2/R4-BLOCKER), A3 (active-box hook via // SW-FORK HOOK or focus inference validated against the real TaskPatternParameters 2-field panel + FwTheme::apply() populates the named QPalette role on the box widget's local palette, R2-F4/R2-F6), A4 (the LIFECYCLE/SURVIVABILITY gate — R3-ROOT/R4-BLOCKER: do the chrome + the FLOW-01 reset handler SURVIVE an edit-time WB switch; verdict token 'overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)' PASS / 'overlays-lost' FAIL, naming the deferred-cancellable install/teardown mechanism the downstream plans implement + the Phase 2-3 ribbon cross-phase observation), and the reuse-primitive source confirmation"
      contains: "Verdict"
    - path: "src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md"
      provides: "Appended Phase-4 live-only FEEL obligations (slide-in feel, pink-box pick feel, SW key feel, sketch→feature loop feel, A2 live WB-switch dock observation, A4 live overlays-survive-the-edit-time-WB-switch observation incl. the FLOW-01 reset handler still firing, AND the synchronous-within-one-turn confirmation the deferred-cancellable teardown relies on) — recorded, not code-completion gating"
      contains: "Phase 4"
  key_links:
    - from: "tests/src/Gui/FwPropertyManager.cpp"
      to: "tests/src/Gui/FwTestGuiBootstrap.h"
      via: "SetUpTestSuite calls tests::ensureGuiTestBootstrap() so Gui::Control/TaskView/Selection + SketcherGui::ViewProviderSketch resolve before the asserts"
      pattern: "ensureGuiTestBootstrap"
    - from: "tests/src/Gui/CMakeLists.txt"
      to: "tests/src/Gui/FwPropertyManagerWidget.cpp"
      via: "setup_qt_test(FwPropertyManagerWidget) creates the FwPropertyManagerWidget_Tests_run target so a QApplication exists for QWidget/QDockWidget construction (FwReferenceBoxStyler.cpp instead goes into Gui_tests_run — no *_Tests_run target, R2-F5)"
      pattern: "FwPropertyManagerWidget"
    - from: ".planning/phases/04-propertymanager-panel/SPIKE.md"
      to: "src/Gui/DockWindowManager.cpp"
      via: "the A1 verdict records that addDockWindow CANNOT move an already-docked panel (DockWindowManager.cpp:256-258, R2-F1) so the left placement re-docks an existing container via getMainWindow()->addDockWidget(Left, dock) and only creates-via-addDockWindow when never-docked — the resolution Plan 04-02's mountPropertyManager placement strategy depends on"
      pattern: "addDockWindow|addDockWidget|dockWidgetArea"
    - from: ".planning/phases/04-propertymanager-panel/SPIKE.md"
      to: "src/Gui/FreeWorks/FwWorkbench.cpp"
      via: "the A4 verdict records the survivable install/teardown policy (the named 'overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)') that resolves R3-ROOT/R4-BLOCKER: deactivated() (FwWorkbench.cpp:66/71) fires DURING the assureWorkbench round-trip (Application.cpp:1984 before :2006) before showDialog/signalInEdit, so a synchronous teardown OR a pre-checked-flag-guarded one (still false then) destroys the chrome + the FLOW-01 handler — the deferred-cancellable singleShot policy 04-02/04-03/04-04 implement"
      pattern: "deactivated|unmountRibbon|s_ribbonContext|singleShot|signalInEdit"
---

<objective>
Stand up the Wave 0 test scaffold AND run the time-boxed D-03 container spike that resolves the keystone foundation decision for this phase: in FreeWorks mode (where `FwWorkbench::setupDockWindows()` never returns `Std_TaskView`, so the framework builds NO live "Tasks" dock — F1 VERIFIED), can FreeCAD's existing `Gui::Control` / `Gui::TaskView::TaskView` task-panel host be placed in the **LEFT** PropertyManager slot — with collapsible `TaskBox`/`TaskGroup` rollouts and `accept()`/`reject()` intact — **without editing `Control.cpp` / `TaskView.cpp` bodies** (CONTEXT D-01/D-02/D-03; 04-RESEARCH § Open Questions)? The spike must resolve the spike-gated unknowns the full build depends on, each with a DECISIVE verdict token:

**A1** — the LEFT-PLACEMENT MECHANISM (R2-F1 VERIFIED): `addDockWindow("Tasks", taskView, Left)` will **NOT move an already-docked Tasks panel** — `DockWindowManager::addDockWindow()` returns the existing dock immediately when `widget->parentWidget()` is already a `QDockWidget` (DockWindowManager.cpp:256-258), *before* the `mw->addDockWidget(pos, dw)` placement (:278) and the objectName-set (:290), both of which run only on the NEW-dock branch. Stock/PartDesign workbenches dock Tasks **right** via `Std_TaskView` (Workbench.cpp:925), so coming from such a workbench `Control().taskPanel()` is non-null inside an existing right `QDockWidget` `addDockWindow` would never move. So A1 resolves that the LEFT placement is achieved by resolving the host from `Gui::Control().taskPanel()`, walking **up** to its parent `QDockWidget`, and branching on `getMainWindow()->dockWidgetArea(dock)`: re-dock an existing non-left container left via `Gui::getMainWindow()->addDockWidget(Qt::LeftDockWidgetArea, dock)` (the came-from-right-dock case), OR create the dock left via `addDockWindow("Tasks", taskView, Left)` only when `taskPanel()` has no parent `QDockWidget` (the never-docked case, F1). Record the 4-name disambiguation {registry key `"Std_TaskView"`, toggle-action data, dock-CONTAINER objectName `"Tasks"`, child-WIDGET objectName `"Tasks"`}, that `getDockWindow("Tasks")` (Control.cpp:55-61, matching the container objectName at DockWindowManager.cpp:325) is **parent-independent** so re-docking preserves `Control().taskPanel() != nullptr`, and that the managed PropertyManager dock identity is `"Tasks"` (R2-F3) with the `Fw_PropertyManager` placeholder consumed.

**A2** — the DOCK-PLACEMENT teardown policy under the deactivated-before-activated-before-showDialog ordering (R2-F2 VERIFIED), now a SUB-CASE of A4 below: `Application::activateWorkbench()` calls `oldWb->deactivated()` (Application.cpp:1984) **before** `newWb->activated()` (:2006); PartDesign `setEdit()` calls `assureWorkbench("PartDesignWorkbench")` (ViewProvider.cpp:165) **before** `Control().showDialog()` (:175); `signalInEdit` fires only after `startEditing()` (Document.cpp:680-692). So on a tree double-click FreeWorks `deactivated()` runs while `Control().activeDialog()` is **still null** AND before `signalInEdit` fires — a teardown guard on `activeDialog() != nullptr` is **too late**, and a **pre-checked edit-in-progress flag is ALSO too late** (still false at that instant — R4-BLOCKER). A2 resolves that `unmountPropertyManager()` must **not reverse the left placement on `deactivated()` at all** (the `"Tasks"` dock is a MainWindow-level `QDockWidget`; leaving it left is harmless under stock workbenches and survives the round-trip), that `deactivated()` may SCHEDULE a deferred teardown but never moves the dock back right synchronously, and that the `signalInEdit` reveal consumer (fired after `startEditing`, within the same synchronous turn) re-asserts the left placement AND cancels the deferred teardown when the edit lands.

**A4** — the LIFECYCLE/SURVIVABILITY gate (R3-ROOT, the keystone of round 3, sharpened by R4-BLOCKER, the legitimate resolution vehicle for the remaining live-behavior unknown): A2 fixed the dock *placement*; A4 fixes the *behavior mounted on it*. The SAME deactivated-before-activated-before-signalInEdit ordering means that on an edit-time workbench switch (PartDesign `assureWorkbench` / Sketcher edit), FreeWorks `deactivated()` (FwWorkbench.cpp:66/71) runs and — as the plans were written — would `s_ribbonContext.reset()` (FwLayout.cpp:286) AND `unmountPropertyManager()` (releasing the header / key filter / pink styler) BEFORE `showDialog`/`signalInEdit`/`signalResetEdit` fire (Document.cpp:680-692, 747). **R4-BLOCKER VERIFIED:** because `signalInEdit` fires AFTER `deactivated()` within that one synchronous `activateWorkbench` turn, a **pre-checked edit-in-progress flag (set on `signalInEdit`) is STILL FALSE** when `deactivated()`→`unmountPropertyManager()` runs — so a flag-guarded teardown would STILL strip the chrome before the edit. So: the FLOW-01 sketch-exit handler hung off the `s_ribbonContext` `signalResetEdit` consumer (FwRibbonContext.cpp:111-121) is DESTROYED before sketch exit fires (R3-BLOCKER1 — isolation test passes while the product fails), and a tree double-click lands in a left RAW Tasks dock without the FreeWorks chrome (R3-BLOCKER2 — fails PROP-01/PROP-02/D-10 in the exact round-trip the plans claim to fix). A4 DECIDES the correct **survivable install/teardown policy**, and the resolution is the **DEFERRED, CANCELLABLE teardown**: `deactivated()`→`unmountPropertyManager()` does **not** tear down inline and does **not** rely on a pre-checked flag (too late) — it **SCHEDULES** the true-exit teardown via a cancellable `QTimer::singleShot(0, …)` (or an equivalent owned/restartable `QTimer`) and remembers the pending handle. Because the whole transient sequence (`assureWorkbench → deactivated() → activated() → setEdit() → tryStartEditing() → emit signalInEdit`) runs **synchronously within one event-loop turn**, the queued teardown does not fire until control returns to the event loop — by which time `signalInEdit` (and the FreeWorks re-`activated()` that re-mounts) have **CANCELLED** it, so the chrome + the FLOW-01 handler survive. For a **genuine** no-edit workbench switch, no `signalInEdit` fires, so the uncancelled queued teardown runs on the next turn and releases the chrome normally. The s_propertyReveal / FLOW-01 handler (04-04), the styler/key-filter (04-03), AND the dock placement (A2) all ride this single deferred-cancellable teardown. A4 PASS = `overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)` naming the chosen mechanism for 04-02/04-03/04-04 to implement; A4 FAIL = `overlays-lost` → escalate. A4 ALSO records the CROSS-PHASE OBSERVATION (do NOT edit shipped phases) that the Phase 2-3 ribbon uses the same synchronous-teardown-on-deactivated pattern and likely vanishes during edits too — flag for a follow-up, do not patch here. The live checklist confirms the synchronous-within-one-turn assumption the singleShot cancellation relies on.

**A3** — the active-reference-box hook AND a FreeWorks-OWNED pink palette ROLE against a **real hosted PartDesign 2-field panel** (R2-F4 + R2-F6 VERIFIED): `Gui::Selection` exposes only a selection-gate pointer; the real two-field active state is **private** (`TaskPatternParameters::activeDirectionWidget` — TaskPatternParameters.h:91 — set at .cpp:294-307, two real direction widgets at .h:88-89), so a focus+selection-only styler can pass a synthetic test and still color the wrong real box. And a bare `activeReferenceBoxTone()→QColor` accessor while `FwTheme::apply()` stays no-op does **not** satisfy D-06's "via a QPalette role" (D-06 is locked, "functional state now"). A3 resolves (a) the active-box hook — a minimal `// SW-FORK HOOK` active-field accessor (the explicit last resort, now justified) OR focus inference validated against the real `TaskPatternParameters` linear-pattern panel; and (b) that `FwTheme::apply()` becomes **functional** for this — install the single FreeWorks-owned pink VALUE into a chosen `QPalette` ROLE on the reference-box widget's **local** palette (QPalette has no custom roles, so carry it on the box widget's local palette via a chosen role, reversible), and the styler reads `widget->palette().color(<role>)`. The pink VALUE still lives once in `FwTheme`; the spike names the exact role.

The spike's exit criteria make the reuse-vs-fallback choice explicit: PASS → `SPIKE.md` records `reuse-and-rehost committed` and placing the Tasks `TaskView` in the left slot (re-dock existing / create-left) is the committed engine for Plans 04-02/04-03/04-04; FAIL → record `thin-adopt committed` and commit to the CONCRETE fallback (a thin `FwPropertyManager` container that **adopts the running `TaskView`/`TaskDialog`** — still additive, still reusing the `TaskDialog` machinery, NEVER a from-scratch task system).

Purpose: De-risk the one genuinely uncertain part of an otherwise ~80%-reuse phase (04-RESEARCH § Summary: the moment the dock is left-placed, ~80% of the success criteria are satisfied with no new mechanism) before any production mount is written, resolve the R3-ROOT/R4-BLOCKER survivability question DECISIVELY in the spike (so 04-02/04-03/04-04 implement a chrome that actually survives the edit-time WB switch via the deferred-cancellable teardown, not one torn down before the edit fires — and not one gated on a pre-checked flag that is too late), and establish the headless GTest + offscreen QTEST harness Plans 04-02/04-03/04-04 sample against (mirrors the Phase-2 native-ribbon and Phase-3 tree-reuse spike-gate structure exactly).
Output: three new test files (`FwPropertyManager.cpp` GTest logic, `FwReferenceBoxStyler.cpp` GTest state-machine, `FwPropertyManagerWidget.cpp` QTEST_MAIN construction) wired additively into `tests/src/Gui/CMakeLists.txt`, `SPIKE.md` recording the D-03 verdict (`reuse-and-rehost committed`/`thin-adopt committed`) with DECISIVE A1/A2/A3/A4 verdict tokens (A4 naming the deferred-cancellable teardown), and the Phase-4 live FEEL obligations appended to `src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-propertymanager-panel/04-CONTEXT.md
@.planning/phases/04-propertymanager-panel/04-RESEARCH.md
@.planning/phases/04-propertymanager-panel/04-PATTERNS.md
@.planning/phases/04-propertymanager-panel/04-UI-SPEC.md
@.planning/phases/04-propertymanager-panel/04-VALIDATION.md
@.planning/phases/02-commandmanager-ribbon/SPIKE.md
</context>

<artifacts_this_phase_produces>
Symbols/files created across Phase 4 (this plan creates the test scaffold + spike verdict; no production FreeWorks symbol is committed until the spike PASSes):

- **This plan (04-01):** `tests/src/Gui/FwPropertyManager.cpp` (GTest, added to `Gui_tests_run`): FLOW-01 sketch-type-name decision-core, header→`Control().accept()`/`reject()` mapping, non-modal (no `exec()`) assertion. `tests/src/Gui/FwReferenceBoxStyler.cpp` (GTest, added to `Gui_tests_run` — NOT a separate `*_Tests_run` target, R2-F5): pink active-box state machine + palette-role-read-off-the-box-widget-palette-not-hex-not-blue assertion (R2-F6). `tests/src/Gui/FwPropertyManagerWidget.cpp` (QTEST_MAIN, offscreen, registered via `setup_qt_test` → target `FwPropertyManagerWidget_Tests_run`): construction harness + resolvable registered Tasks `TaskView`. `SPIKE.md` (D-03 verdict `reuse-and-rehost committed`/`thin-adopt committed` + A1/A2/A3/A4 verdict tokens). Appended Phase-4 obligations in `src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md`. CMake edits to `tests/src/Gui/CMakeLists.txt` (`Gui_tests_run` list + `setup_qt_test(FwPropertyManagerWidget)`).
- **Plan 04-02 (later, gated on this SPIKE):** `FwLayout::mountPropertyManager()`/`unmountPropertyManager()` (place the Tasks `TaskView` in the left slot — re-dock an existing container left via `getMainWindow()->addDockWidget(Left, dock)` or create-left via `addDockWindow` when never-docked, R2-F1; idempotent; consume the placeholder; resolve robustly via `Control().taskPanel()` walk-up; the survivable install/teardown policy per the A4 verdict — `unmountPropertyManager()` SCHEDULES a cancellable `QTimer::singleShot(0)` teardown cancelled by signalInEdit / re-activation, so `deactivated()` does NOT move the dock back right AND does NOT release the chrome during an edit-time WB switch, R2-F2/R3-ROOT/R4-BLOCKER); `FwPropertyManagerHeader.{h,cpp}` (green-✓/red-✗ header band → `Control().accept()`/`reject()`, palette-only); the `signalInEdit` reveal/re-assert + teardown-cancel consumer (`s_propertyReveal`, owning the pending-teardown QTimer handle); `FwWorkbench::activated()`/`deactivated()` wiring.
- **Plan 04-03 (later):** `FwReferenceBoxStyler.{h,cpp}` (pink active-box restyle reading a QPalette role FwTheme::apply() populates on the box widget's local palette, R2-F6; reads `Gui::Selection`/focus or the SW-FORK active-field hook, R2-F4); `FwPropertyKeyFilter.{h,cpp}` (D-10 Tab traversal + Enter→accept/Esc→reject container-level guarantee) — both installed by mountPropertyManager() and released only by the A4 deferred-cancellable true-exit teardown so they persist through the edit-time WB switch.
- **Plan 04-04 (later):** the FLOW-01 sketch-exit handler installed on the SURVIVABLE `s_propertyReveal` consumer's signalResetEdit connection per the A4 verdict (NOT the synchronously-reset `s_ribbonContext` — R3-BLOCKER1: persistently select finished sketch + Features-tab handoff, no auto-launch); the TREE-03 double-click→left-panel integration confirmation against a real PartDesign feature VP.

> **Downstream gate:** Plans 04-02/04-03/04-04 require this plan's `SPIKE.md` to record the D-03 verdict AND the A4 `overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)` token. If D-03 reads `reuse-and-rehost committed`, placing the Tasks `TaskView` in the left slot is the committed engine; if it reads `thin-adopt committed`, those plans build on the CONCRETE thin-adopt fallback. The A4 policy token is the named survivable install/teardown mechanism those plans implement so the chrome + the FLOW-01 handler survive the edit-time WB switch. They must NOT proceed on the placement/lifecycle strategy merely because 04-01 ran.

> **Concrete fallback (committed if the spike FAILs A1):** if neither re-docking an existing Tasks container left nor creating one left preserves `getDockWindow("Tasks")` resolution, the FAIL fallback is the thin-adopt path: a `FwPropertyManager` QWidget registered under `Fw_PropertyManager` that, on `Control().showDialog`, adopts the running `TaskView` into the left dock for the edit's lifetime and releases it on close — reusing the `TaskDialog`/`accept`/`reject` machinery, never rebuilding it. A one-line `// SW-FORK HOOK` in `Control.cpp`/`TaskView.cpp` is the final escalation and must be flagged for developer approval. (Note: the A3 active-field `// SW-FORK HOOK` accessor — R2-F4 — is a separate, narrower hook on the hosted PartDesign panel, also developer-flagged.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — GTest logic (FLOW-01 decision + header→accept/reject mapping + non-modal) and GTest pink-box state machine (palette-role read off the box widget's local palette, R2-F6), plus QTEST_MAIN construction harness, split across targets and wired into CMake (FwReferenceBoxStyler.cpp into Gui_tests_run — no separate *_Tests_run target, R2-F5)</name>
  <files>tests/src/Gui/FwPropertyManager.cpp, tests/src/Gui/FwReferenceBoxStyler.cpp, tests/src/Gui/FwPropertyManagerWidget.cpp, tests/src/Gui/CMakeLists.txt</files>
  <read_first>
    - tests/src/Gui/FwRibbon.cpp (GTest analog: `SetUpTestSuite()` calls `tests::ensureGuiTestBootstrap()`; `constexpr const char*` ID/type constants; the `kSketchVpTypeName`/`kSketchViewProviderTypeName` type-name-literal precedent — mirror it; the intentionally-empty allow-list guard pattern)
    - tests/src/Gui/FwFeatureTree.cpp (GTest analog: REAL-engine logic asserts after bootstrap; how a PartDesign document is built headlessly to exercise real types)
    - tests/src/Gui/FwRibbonWidget.cpp (EXACT QTEST analog: `class testFwRibbonWidget : public QObject` with ctor calling `ensureGuiTestBootstrap()`, `private Q_SLOTS`, the `ensureRealMainWindow()` leaked-singleton helper, `QTEST_MAIN(...)` + `#include "FwRibbonWidget.moc"` at EOF)
    - tests/src/Gui/FwFeatureTreeWidget.cpp (QTEST analog: `initTestCase`/`init`/`cleanup`/`test_*` private slots; a `Probe*` subclass to expose protected seams; real-QApplication QWidget construction)
    - tests/src/Gui/FwTestGuiBootstrap.h (REUSE VERBATIM, no edit: `tests::ensureGuiTestBootstrap()` creates `Gui::Application(false)` then imports PartDesignGui/SketcherGui — confirm it loads the modules so `Gui::Control`, `Gui::TaskView::TaskView`, `Gui::Selection`, and `SketcherGui::ViewProviderSketch` resolve)
    - tests/src/Gui/CMakeLists.txt (lines 7-25: the `Gui_tests_run` add_executable list — add `FwPropertyManager.cpp` and `FwReferenceBoxStyler.cpp` after `FwFeatureTree.cpp` line 19 so the styler GTest runs INSIDE Gui_tests_run; the `setup_qt_test(FwFeatureTreeWidget)` call line 25 — add `setup_qt_test(FwPropertyManagerWidget)` next to it. Do NOT invent a `FwReferenceBoxStyler_Tests_run` target — R2-F5)
    - tests/CMakeLists.txt lines 13-17 (`function(setup_qt_test)`: `add_executable(${_testname}_Tests_run ...)` — confirm setup_qt_test is the ONLY thing that creates `*_Tests_run` targets, so `FwPropertyManagerWidget_Tests_run` exists but `FwReferenceBoxStyler_Tests_run` does NOT — R2-F5; QTEST_MAIN + links FreeCADApp/FreeCADGui/QtTest + `QT_QPA_PLATFORM=offscreen`)
    - src/Gui/Control.h lines 54,68,76,98-100 (`ControlSingleton`/`Control()`; `taskPanel()` → `TaskView`; `accept()`/`reject()`/`closeDialog()` slots — the mapping the header→Control test asserts)
    - src/Gui/TaskView/TaskView.h lines 67,82,163,241 (`TaskGroup`/`TaskBox` collapsible rollouts; `TaskView` host; `accept()`/`reject()` — confirm the reuse primitives the spike cites)
    - src/Gui/FreeWorks/FwRibbonContext.cpp line 54 (the existing `kSketchViewProviderTypeName` constant `"SketcherGui::ViewProviderSketch"` — REUSE this exact literal in the FLOW-01 decision-core test; do NOT add a Sketcher include — Pitfall 2)
    - src/Gui/FreeWorks/FwTheme.cpp lines 31-39 (apply() is CURRENTLY a no-op — Plan 04-03 makes it FUNCTIONAL: populate the named QPalette role on the reference-box widget's local palette, R2-F6; this scaffold's styler test READS the role off a box widget's local palette, NOT an accessor-equality check)
    - src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp lines 154-162 (the palette-DERIVED state-color precedent: `option.palette.color(QPalette::Disabled, QPalette::Text)` etc. — the pink-active-box state-machine test asserts the styler reads its active tone through a palette role off the box widget's local palette, never a hex/`setStyleSheet` literal; note Highlight is the SYSTEM highlight, NOT pink — the pink VALUE source is the FwTheme-populated role per the A3 spike)
    - src/Mod/PartDesign/Gui/TaskPatternParameters.h lines 82-95 (the REAL 2-field panel the A3 hosted-panel proof targets: parametersWidget/parametersWidget2 at :88-89, activeDirectionWidget PRIVATE at :91 — R2-F4: the synthetic-only QTEST is insufficient; the real active-field state is private)
    - .planning/phases/04-propertymanager-panel/04-PATTERNS.md § "Test Pattern Assignments" (the exact GTest/QTEST class skeletons + bootstrap discipline)
    - .planning/phases/04-propertymanager-panel/04-VALIDATION.md § "Per-Task Verification Map" + § "Wave 0 Requirements" (wire each assertion to its Req row)
  </read_first>
  <behavior>
    - GTest (Gui_tests_run, FwPropertyManager.cpp), after `ensureGuiTestBootstrap()` in `SetUpTestSuite()`:
      - FLOW-01 decision-core (D-11, Pitfall 2): a pure helper that takes a view-provider type-name STRING returns a `select-and-handoff` decision ONLY for the literal `"SketcherGui::ViewProviderSketch"` and a `no-op` decision for any other type-name; assert it never returns a `launch-feature-command` decision for any input (D-11 forbids auto-launch). The test uses the `kSketchViewProviderTypeName` constant, never a `#include <Mod/Sketcher/...>`.
      - Header→Control mapping (PROP-01, D-04): a pure mapping returns the `Gui::Control().accept()` action for the green-✓ activation and `Gui::Control().reject()` for the red-✗ activation — assert the two map to distinct Control slots and that NO separate transaction/commit pipeline is referenced (the hosted TaskDialog owns its own transaction — D-10).
      - Non-modal assertion (PROP-01, D-07): assert (by source-contract / type check available at this scaffold stage) that the panel host is a `QDockWidget`-hosted `TaskView`, never a `QDialog::exec()` path — encode the expectation so Plan 04-02's production mount is held to non-modality.
    - GTest (Gui_tests_run, FwReferenceBoxStyler.cpp — NOT a separate target, R2-F5), after `ensureGuiTestBootstrap()`:
      - Pink-active state machine (PROP-02, D-05/D-06): model the active-box state as a state machine: activating box B deactivates the previously-active box A (exactly one active at a time); deactivating reverts to no-active. The active state resolves a tone by READING a `QPalette` ROLE off a box widget's LOCAL palette that has been populated with the FreeWorks-owned pink VALUE (R2-F6: construct a box-like QWidget, set the named role on its local palette to the FwTheme pink, and assert the styler reads `widget->palette().color(<role>)` — NOT an accessor-equality check); a filled box uses `QPalette::Highlight`, an inactive caption uses `QPalette::Disabled, QPalette::Text`. (At this scaffold stage `FwTheme::apply()` is still the no-op; the test sets the role on the local palette directly to encode the expectation the Plan 04-03 `FwTheme::apply()` will then populate — the test asserts the READ-the-role contract, which is what makes D-06 'via a QPalette role' satisfiable.)
      - Reserved-color rule (PROP-02): assert the active tone is NOT the palette's blue/link-style role and that the styler exposes no hardcoded hex string / `setStyleSheet` color literal (string-search the styler's declared API surface at this stage; the full assertion lands in Plan 04-03 against the real `FwReferenceBoxStyler`).
    - QTEST_MAIN (offscreen, FwPropertyManagerWidget.cpp), `class testFwPropertyManagerWidget : public QObject` with `private Q_SLOTS`:
      - `initTestCase()` calls `ensureGuiTestBootstrap()` and ensures a real `MainWindow` singleton (mirror `FwRibbonWidget.cpp` `ensureRealMainWindow()`).
      - A `test_bootstrap_taskView_resolvable()` asserts the registered Tasks `TaskView` is resolvable (e.g. `Gui::DockWindowManager::instance()->findRegisteredDockWindow("Std_TaskView") != nullptr`, or that a real MainWindow setupTaskView ran) under the offscreen QApplication — the baseline the Plan 04-02 left-placement test extends to assert `Control().taskPanel()` lands in the LEFT area for BOTH the never-docked and came-from-right-dock cases. (Note: in FreeWorks mode `Control().taskPanel()` is null until 04-02's mount runs — F1 — so this scaffold asserts resolvability of the registered widget, not a live left dock.)
      - EOF: `QTEST_MAIN(testFwPropertyManagerWidget)` + `#include "FwPropertyManagerWidget.moc"`.
    - CMake (tests/src/Gui/CMakeLists.txt): append `FwPropertyManager.cpp` and `FwReferenceBoxStyler.cpp` to the `Gui_tests_run` add_executable list (FwReferenceBoxStyler is a GTest INSIDE Gui_tests_run — there is NO `FwReferenceBoxStyler_Tests_run` target, R2-F5); add `setup_qt_test(FwPropertyManagerWidget)` next to `setup_qt_test(FwFeatureTreeWidget)` (creating the target `FwPropertyManagerWidget_Tests_run`). Additive only — no existing line removed.
  </behavior>
  <action>
Create the three Wave 0 test files following the Phase-2/3 test discipline exactly. In `FwPropertyManager.cpp` write a GTest `TEST_F` fixture whose `SetUpTestSuite()` calls `tests::ensureGuiTestBootstrap()` (reuse `FwTestGuiBootstrap.h` verbatim), then encode three logic groups: the FLOW-01 sketch-type-name decision-core keyed on the reused `kSketchViewProviderTypeName` literal `"SketcherGui::ViewProviderSketch"` (select-and-handoff for that type, no-op otherwise, never auto-launch — D-11), the header→`Gui::Control().accept()`/`reject()` mapping (✓→accept, ✗→reject, distinct slots, no new commit pipeline — D-04), and the non-modal contract (host is a `QDockWidget`/`TaskView`, never `QDialog::exec` — D-07). In `FwReferenceBoxStyler.cpp` write a GTest modeling the pink-active-box state machine (exactly one active; revert on deactivate; the active tone READ through a `QPalette` role off a box widget's LOCAL palette populated with the FreeWorks-owned pink VALUE — R2-F6, NOT accessor-equality; mirroring `FwFeatureTreeDelegate.cpp:154-162`; filled→`QPalette::Highlight`; inactive→`QPalette::Disabled/Text`; never blue; no hex/`setStyleSheet` literal — PROP-02/D-06). In `FwPropertyManagerWidget.cpp` write a `QTEST_MAIN` `QObject` test class with `private Q_SLOTS` (`initTestCase` calling `ensureGuiTestBootstrap()` + a real-`MainWindow` helper like `FwRibbonWidget.cpp`'s `ensureRealMainWindow()`), a `test_bootstrap_taskView_resolvable()` asserting the registered Tasks `TaskView` is resolvable offscreen (F1: `Control().taskPanel()` is null in FreeWorks mode until 04-02 mounts), and the `QTEST_MAIN(...)` + `.moc` include at EOF. Wire all three into `tests/src/Gui/CMakeLists.txt` additively (two filenames — FwPropertyManager.cpp and FwReferenceBoxStyler.cpp — into the `Gui_tests_run` add_executable list, one `setup_qt_test(FwPropertyManagerWidget)` which creates `FwPropertyManagerWidget_Tests_run`; do NOT add a `FwReferenceBoxStyler_Tests_run` target — R2-F5). These tests are RED scaffolds: the production symbols they will fully assert against (`FwPropertyManagerHeader`, `FwReferenceBoxStyler`, `mountPropertyManager`) are created in Plans 04-02/04-03, so at this stage the tests assert the reuse primitives + decision-core logic + the read-the-palette-role contract that need no FreeWorks production code, and leave the production-symbol asserts as the Wave 0 gap the later plans close.
  </action>
  <verify>
    <automated>if [ -d build ]; then cmake --build build --target Gui_tests_run FwPropertyManagerWidget_Tests_run && ctest --test-dir build -R "FwPropertyManager|FwReferenceBoxStyler|FwPropertyManagerWidget" --output-on-failure; else echo "NO-BUILD-TREE: compile-intended; CMake/grep verification only (Phases 1-3 precedent)"; fi && grep -q "FwPropertyManager.cpp" tests/src/Gui/CMakeLists.txt && grep -q "FwReferenceBoxStyler.cpp" tests/src/Gui/CMakeLists.txt && grep -q "setup_qt_test(FwPropertyManagerWidget)" tests/src/Gui/CMakeLists.txt && ! grep -q "FwReferenceBoxStyler_Tests_run" tests/src/Gui/CMakeLists.txt && echo "CMAKE-WIRED-OK" && bash tools/fw-string-leak-grep.sh</automated>
    <human-check>If no build tree is available in env (consistent with Phases 1-3), the tests are authored compile-intended and the CMake wiring is grep-verified; live ctest deferred to the build-tree checklist. Note (R2-F5): the styler GTest runs inside `Gui_tests_run` (`ctest -R FwReferenceBoxStyler` matches the gtest case name there); there is no `FwReferenceBoxStyler_Tests_run` executable.</human-check>
  </verify>
  <done>Three test files exist (`FwPropertyManager.cpp`, `FwReferenceBoxStyler.cpp` in `Gui_tests_run` — no separate `FwReferenceBoxStyler_Tests_run` target, R2-F5; `FwPropertyManagerWidget.cpp` via `setup_qt_test`, creating `FwPropertyManagerWidget_Tests_run`), each calls `ensureGuiTestBootstrap()`, the FLOW-01/header/state-machine logic asserts are present using the reused sketch type-name literal and the read-the-palette-role-off-the-box-widget contract (R2-F6), the `<automated>` block builds the REAL targets `Gui_tests_run` + `FwPropertyManagerWidget_Tests_run` (when a build tree exists), confirms no invented `FwReferenceBoxStyler_Tests_run` in CMake, and prints `CMAKE-WIRED-OK`, and `tools/fw-string-leak-grep.sh` is clean over the new test files. (Note: the `04-VALIDATION.md` `wave_0_complete`/`nyquist_compliant` flag flip is a SEPARATE step performed in this task's wrap-up — see below — NOT woven into the pass/fail verify command.)</done>
  <post-verify-step>After the `<automated>` verification passes, flip the `04-VALIDATION.md` frontmatter flags as a distinct, non-gating step: set `wave_0_complete: true` and `nyquist_compliant: true` in `.planning/phases/04-propertymanager-panel/04-VALIDATION.md`. This is a documentation status update recorded AFTER the verify command succeeds — it is intentionally NOT part of the pass/fail `<automated>` command so a failing build/test can never leave the flags flipped.</post-verify-step>
</task>

<task type="auto">
  <name>Task 2: D-03 container spike — resolve A1 (two-branch left-placement mechanism, R2-F1 + 4-name disambiguation + parent-independent getDockWindow('Tasks') + 'Tasks' managed identity), A2 (dock placement stays left on deactivated(), no synchronous right-move, deferred teardown cancelled + re-assert on signalInEdit — R2-F2/R4-BLOCKER), A4 (the LIFECYCLE/SURVIVABILITY gate — R3-ROOT/R4-BLOCKER: do the chrome + the FLOW-01 reset handler SURVIVE an edit-time WB switch; name the deferred-cancellable install/teardown policy), A3 (active-box hook via SW-FORK or focus inference validated against the real TaskPatternParameters 2-field panel + FwTheme::apply() populates the named QPalette role — R2-F4/R2-F6) by source-read; write SPIKE.md with DECISIVE per-item verdict tokens; append Phase-4 obligations to SPIKE_LIVE_CHECKLIST.md</name>
  <files>.planning/phases/04-propertymanager-panel/SPIKE.md, src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md</files>
  <read_first>
    - .planning/phases/02-commandmanager-ribbon/SPIKE.md (the EXACT structure to mirror: "Approval basis (READ FIRST — documentation approval)", evidence classes [CODE]/[RESEARCH]/[LIVE-DEFERRED], per-item PASS/FAIL, the literal-token Verdict, the carried open obligation, the Sign-off block)
    - .planning/phases/04-propertymanager-panel/04-RESEARCH.md § "Open Questions" (the questions = A1/A2/A3/A4 the spike resolves) + § "Assumptions Log" (A1-A6) + § "Common Pitfalls" (Pitfall 3/4/6 detail)
    - src/Gui/Control.cpp lines 55-61 (`taskPanel()` calls `getDockWindow("Tasks")` — the A1 resolution site) and 149-195 (`showDialog` — confirm the host the spike must preserve)
    - src/Gui/DockWindowManager.cpp lines 251-259 (R2-F1: `addDockWindow` returns the existing dock at :256-258 when the widget already has a QDockWidget parent — so it CANNOT move an already-docked panel), 260-314 (the NEW-dock branch: `mw->addDockWidget(pos, dw)` :278, sets the new container objectName from the `name` arg at :290 — only reached when the widget had no parent dock), 321-331 (`getDockWindow` matches the CONTAINER objectName — parent-independent), 484-512 (`registerDockWindow`/`findRegisteredDockWindow`/`unregisterDockWindow`), 515-551 (`setup()` :530-535 sets the live container objectName FROM the widget objectName — the A1 disambiguation proof on the create path)
    - src/Gui/MainWindow.h (`addDockWidget`/`dockWidgetArea` — `getMainWindow()->addDockWidget(Qt::LeftDockWidgetArea, dock)` re-docks an EXISTING container; `getMainWindow()->dockWidgetArea(dock)` reports the area — the R2-F1 re-dock + idempotency mechanism A1 records)
    - src/Gui/MainWindow.cpp lines 609-633 (`setupTaskView()`: WIDGET objectName "Tasks" (:623), registry KEY "Std_TaskView" (:628), NOT added to _dockedWindows — the 4-name source)
    - src/Gui/Workbench.cpp lines 918-925 (`StdWorkbench::setupDockWindows()` docks Std_TaskView RIGHT, :925 Qt::RightDockWidgetArea — the came-from-right-dock source A1's re-dock branch handles)
    - src/Gui/FreeWorks/FwWorkbench.cpp lines 44-74 (`activated()` mounts ribbon then hideStockChrome :61-63; `deactivated()` restoreStockChrome + `unmountRibbon()` :66/70-71 — the A4 teardown site; unmountRibbon resets s_ribbonContext SYNCHRONOUSLY at FwLayout.cpp:286) and lines 95-124 (`setupDockWindows()` returns ONLY Fw_FeatureManager/Fw_PropertyManager/Fw_TaskPane — NOT Std_TaskView: confirm no live Tasks dock in FreeWorks mode — F1)
    - src/Gui/FreeWorks/FwLayout.cpp lines 117-146 (`mountFeatureManager` placeholder-replacement) + 162-175 (`install()` registers the Fw_PropertyManager placeholder via ensureDock — the placeholder the host consumes — F2) + lines 266-300 (`bindRibbonContext`/`unmountRibbon`: `s_ribbonContext` is owned via a unique_ptr static and RESET SYNCHRONOUSLY on unmountRibbon :286 — the A4 ROOT teardown-on-deactivated pattern; the FLOW-01 handler must NOT hang off this, and the property-host teardown must be DEFERRED-CANCELLABLE not synchronous)
    - src/Mod/PartDesign/Gui/ViewProvider.cpp lines 158-181 (`setEdit`: clearSelection :162, then `assureWorkbench("PartDesignWorkbench")` :165 BEFORE `Control().showDialog()` :175 — the A2/A4 WB-switch site, R2-F2 ordering) and the unsetEdit restore
    - src/Mod/Sketcher/Gui/ViewProviderSketch.cpp lines 633-636 (sketch edit defaults to SketcherWorkbench :635) and 3960-4002 (SketcherWorkbench activated :3963/:4001 during sketch setEdit — the A4 Sketcher-edit WB-switch arm) and 4262-4264 (unsetEdit self-selects the edited sketch BEFORE signalResetEdit — the R2-F8 hazard A4 also bears on)
    - src/Gui/Application.cpp lines 1983-2007 (`activateWorkbench`: `oldWb->deactivated()` :1984 BEFORE `newWb->activated()` :2006 — R2-F2/A4/R4-BLOCKER: deactivated() runs while activeDialog() is still null AND before signalInEdit fires, so a pre-checked flag is false then; the whole sequence is ONE synchronous call stack, so a QTimer::singleShot(0) scheduled in deactivated() does not fire until the loop turn AFTER signalInEdit — the basis for the deferred-cancellable teardown)
    - src/Gui/Command.cpp lines 851-864 (`assureWorkbench` activates another workbench — the A2/A4 round-trip) ; src/Gui/Document.cpp lines 680-692 (signalInEdit fires only after startEditing) and 747-750 (signalResetEdit fires on reset — so the reveal/reset consumers fire AFTER the WB round-trip but WITHIN the same synchronous turn, and a synchronous deactivated() teardown destroys them first — the A4 ROOT; the deferred singleShot is cancelled by signalInEdit before it can run)
    - src/Gui/FreeWorks/FwTheme.cpp lines 31-39 (`apply()` is CURRENTLY a no-op — R2-F6: A3 resolves that apply() becomes FUNCTIONAL, populating the named QPalette role on the box widget's local palette so D-06's 'via a QPalette role' is satisfiable) ; src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp lines 154-159 (the only palette precedent reads Disabled/Text + Highlight; Highlight is the system highlight, typically blue, NOT pink — the A3 'pink needs a FreeWorks-owned role-value' proof)
    - src/Mod/PartDesign/Gui/TaskPatternParameters.h lines 82-95 (R2-F4: the REAL 2-field panel — parametersWidget/parametersWidget2 :88-89, activeDirectionWidget PRIVATE :91; A3 must validate the active-box mechanism against THIS, not a synthetic state machine) and src/Mod/PartDesign/Gui/ReferenceSelection.h lines 42-57 (`PartDesignGui::ReferenceSelection : Gui::SelectionFilterGate` — ONLY a selection gate, NO public "active field" hook — the A3 'no public hook' proof)
    - src/Gui/TaskView/TaskView.cpp lines 393-457 (`keyPressEvent` Enter→accept/Esc→reject, incl. the macOS workaround) and 644-686 (the `TaskEditControl` `QDialogButtonBox` accept/reject wiring) — the reuse-primitive confirmation the spike records
    - src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md (the existing Phase 1-3 live obligations — APPEND a Phase 4 section, do not rewrite existing content)
  </read_first>
  <behavior>
    - SPIKE.md opens with the same "documentation approval" basis block as 02's SPIKE.md (no build tree / GUI in env; the live placement round-trip, WB-switch observation, AND the overlays-survive-the-edit-time-WB-switch observation are deferred to SPIKE_LIVE_CHECKLIST.md; PASS marks granted on [CODE]+[RESEARCH] basis).
    - A1 item (the load-bearing one) — DECISIVE verdict token required: record that `addDockWindow` CANNOT move an already-docked panel (it returns the existing dock at DockWindowManager.cpp:256-258 before placement — R2-F1), so the LEFT placement resolves the host from `Gui::Control().taskPanel()`, walks up to its parent QDockWidget, and branches on `getMainWindow()->dockWidgetArea(dock)`: re-dock an existing non-left container left via `getMainWindow()->addDockWidget(Qt::LeftDockWidgetArea, dock)` (the came-from-right-dock case — Std_TaskView docks right per Workbench.cpp:925), or create-left via `addDockWindow("Tasks", taskView, Left)` only when never-docked (F1, FwWorkbench.cpp:95-124). Record the 4-name disambiguation {registry key "Std_TaskView" (MainWindow.cpp:628), toggle-action data, dock-CONTAINER objectName "Tasks", child-WIDGET objectName "Tasks" (MainWindow.cpp:623)}, that `getDockWindow("Tasks")` matches the CONTAINER objectName (DockWindowManager.cpp:321-331) and is parent-independent so re-docking preserves `Control().taskPanel() != nullptr`, and that the managed PropertyManager dock identity is "Tasks" (R2-F3) with the Fw_PropertyManager placeholder consumed. A1 PASS only with the correct two-branch mechanism + parent-independent lookup + "Tasks" identity. A "host the registered widget and the container becomes Tasks" note that ignores the can't-move-an-existing-dock fact is NOT a PASS.
    - A2 item — DECISIVE verdict token required (the DOCK-PLACEMENT sub-case of A4): record the deactivated-before-activated-before-showDialog ordering — `oldWb->deactivated()` (Application.cpp:1984) BEFORE `newWb->activated()` (:2006); PartDesign `setEdit` calls `assureWorkbench` (ViewProvider.cpp:165) BEFORE `Control().showDialog()` (:175); `signalInEdit` after `startEditing` (Document.cpp:680-692). Resolve that a teardown guard on `activeDialog() != nullptr` is TOO LATE (deactivated runs while it is still null) AND a pre-checked edit-in-progress flag is ALSO too late (still false because signalInEdit has not fired — R4-BLOCKER), so `unmountPropertyManager()` must NOT reverse the left placement on `deactivated()` at all — the "Tasks" dock is a MainWindow-level QDockWidget; leaving it left is harmless under stock workbenches and survives the round-trip; `deactivated()` may SCHEDULE a deferred teardown but never moves the dock back right synchronously; the `signalInEdit` reveal consumer re-asserts the left placement AND cancels the deferred teardown when the edit lands. A2 PASS = "dock stays left on deactivated() (no synchronous right-move); deferred teardown cancelled + left re-asserted on signalInEdit; activeDialog() guard AND a pre-checked flag both recorded as insufficient per the ordering". Live observation → SPIKE_LIVE_CHECKLIST.md, source-read PASS/FAIL gated HERE.
    - A4 item (the R3-ROOT/R4-BLOCKER keystone) — DECISIVE verdict token required: record the SAME ordering generalized to the chrome + handlers mounted on the dock — on an edit-time WB switch FreeWorks `deactivated()` (FwWorkbench.cpp:66/71) runs and, as written, would `s_ribbonContext.reset()` (FwLayout.cpp:286) AND `unmountPropertyManager()` (releasing header/key-filter/styler) BEFORE `showDialog`/`signalInEdit`/`signalResetEdit` fire (ViewProvider.cpp:165 before :175; Document.cpp:680-692, 747-750; Application.cpp:1984 before :2006), and the Sketcher edit arm does the same (ViewProviderSketch.cpp:635 activates SketcherWorkbench at :3963/:4001). RECORD R4-BLOCKER: because signalInEdit fires AFTER deactivated() within that ONE synchronous activateWorkbench turn, a pre-checked edit-in-progress flag (set on signalInEdit) is STILL FALSE when deactivated()→unmountPropertyManager() runs, so a flag-guarded teardown would STILL strip the chrome — a pre-check is too late. CONSEQUENCE: the FLOW-01 sketch-exit handler on the `s_ribbonContext` `signalResetEdit` consumer (FwRibbonContext.cpp:111-121) is destroyed before sketch exit fires (R3-BLOCKER1), and the tree double-click lands in a left RAW Tasks dock without the chrome (R3-BLOCKER2). DECIDE and RECORD the survivable policy as the DEFERRED, CANCELLABLE teardown: `deactivated()`→`unmountPropertyManager()` does NOT tear down inline and does NOT use a pre-checked flag — it SCHEDULES the true-exit teardown via a cancellable `QTimer::singleShot(0, …)` (or owned/restartable QTimer) and remembers the pending handle; because the whole transient sequence runs synchronously within ONE event-loop turn, the queued teardown does not fire until control returns to the loop, by which time `signalInEdit` (and the FreeWorks re-`activated()`) have CANCELLED it — so the chrome + the FLOW-01 handler survive; a genuine no-edit switch fires no signalInEdit so the uncancelled queued teardown runs on the next turn and releases the chrome. A4 PASS = the literal token `overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)` with the mechanism spelled out so 04-02/04-03/04-04 implement it; A4 FAIL = `overlays-lost` → escalate. ALSO record the CROSS-PHASE OBSERVATION that the Phase 2-3 ribbon (`s_ribbonContext`, FwLayout.cpp:286) uses the SAME synchronous-teardown-on-deactivated pattern so it likely vanishes during edits too — a pre-existing latent issue flagged for a follow-up, NOT patched here (do not silently edit shipped phases). The live checklist confirms the synchronous-within-one-turn assumption the singleShot cancellation relies on. Live confirmation → SPIKE_LIVE_CHECKLIST.md; source-read PASS/FAIL gated HERE.
    - A3 item — DECISIVE verdict token required, resolved against the REAL TaskPatternParameters 2-field panel (R2-F4, not a synthetic state machine): (a) the active-box hook — record that NO public active-field hook exists (ReferenceSelection.h:42-57 is only a SelectionFilterGate; TaskPatternParameters::activeDirectionWidget is private at .h:91), so the disposition is EITHER a minimal `// SW-FORK HOOK` active-field accessor on the hosted panel (the explicit last resort, now justified by R2-F4 — developer-flagged) OR focus inference VALIDATED against the real TaskPatternParameters linear-pattern panel (two direction widgets); AND (b) the pink SOURCE — record that QPalette::Highlight is the system highlight (typically blue, NOT pink) and FwTheme::apply() is currently a no-op (FwTheme.cpp:31-39), so a bare accessor does NOT satisfy D-06's "via a QPalette role" (R2-F6); the disposition is that FwTheme::apply() becomes FUNCTIONAL — install the single FreeWorks-owned pink VALUE into a chosen QPalette ROLE on the reference-box widget's LOCAL palette (QPalette has no custom roles, carry it on the local palette via a chosen role, reversible), and the styler reads `widget->palette().color(<role>)`. A3 NAMES the exact role. A3 PASS = "active-box hook resolved (// SW-FORK HOOK accessor OR focus inference validated against the real TaskPatternParameters 2-field panel) + FwTheme::apply() populates the named QPalette role on the box widget's local palette + styler reads widget->palette().color(<role>)". The box machinery (fill/highlight/auto-expand) stays OWNED by the hosted panel's SelectionFilterGate/SelectionObserver — FreeWorks restyles the active state only (D-05).
    - Reuse-primitive confirmation item: record [RESEARCH]+[CODE] that `TaskGroup`/`TaskBox` rollouts, `Control().accept()`/`reject()`, `TaskView::keyPressEvent`, and the non-modal `QDockWidget` host are present unmodified.
    - VERDICT: the literal token `reuse-and-rehost committed` (PASS) or `thin-adopt committed` (FAIL), with the same "no second decision round" framing as 02.
    - SPIKE_LIVE_CHECKLIST.md gets an appended `## Phase 4 — PropertyManager live FEEL obligations` section listing: slide-in reveal feel, pink active-box pick feel (against the real TaskPatternParameters panel), SW Enter/Esc/Tab feel, sketch→feature loop feel, the A2 live WB-switch dock observation (verify the dock stays left through the assureWorkbench round-trip), the A4 live overlays-survive observation (verify the header / key filter / styler AND the FLOW-01 reset handler still function through the edit-time WB switch — i.e. a tree double-click of a PartDesign feature opens LEFT WITH the chrome, and a sketch exit still auto-selects the sketch), AND the synchronous-within-one-turn confirmation (verify the assureWorkbench → deactivated() → activated() → setEdit() → signalInEdit sequence is one synchronous event-loop turn so the QTimer::singleShot(0) teardown is cancelled before it fires) — each marked [LIVE-DEFERRED], not code-completion gating.
  </behavior>
  <action>
Write `.planning/phases/04-propertymanager-panel/SPIKE.md` mirroring `02-commandmanager-ribbon/SPIKE.md` structure exactly: the "Approval basis (READ FIRST — documentation approval)" preamble (no build tree in env, PASS marks on [CODE]+[RESEARCH], live items deferred), then a per-item PASS/FAIL section with a DECISIVE verdict token for each. A1: record that addDockWindow CANNOT move an already-docked panel (DockWindowManager.cpp:256-258, R2-F1), so the left placement resolves the host from Gui::Control().taskPanel() walk-up to its parent QDockWidget and branches on getMainWindow()->dockWidgetArea(dock) — re-dock an existing non-left container left via getMainWindow()->addDockWidget(Qt::LeftDockWidgetArea, dock) (came-from-right-dock; Std_TaskView docks right Workbench.cpp:925), or create-left via addDockWindow("Tasks", taskView, Left) only when never-docked (F1); record the 4-name disambiguation {registry key "Std_TaskView", toggle-action data, dock-CONTAINER objectName "Tasks", child-WIDGET objectName "Tasks"} citing MainWindow.cpp:623/628, that getDockWindow("Tasks") matches the container objectName (DockWindowManager.cpp:321-331) and is parent-independent, and that the managed dock identity is "Tasks" (R2-F3) with the Fw_PropertyManager placeholder consumed. A2: cite Application.cpp:1984/2006 + ViewProvider.cpp:165/175 + Document.cpp:680-692, resolve that the activeDialog() guard AND a pre-checked flag are both too late (deactivated runs while activeDialog() is null and before signalInEdit fires — R4-BLOCKER) so unmountPropertyManager() does NOT reverse the left placement synchronously on deactivated() — the dock stays left, deactivated() only SCHEDULES a deferred teardown, the signalInEdit consumer re-asserts AND cancels it on edit start (R2-F2). A4 (the R3-ROOT/R4-BLOCKER keystone): cite FwWorkbench.cpp:66/71 (deactivated → restoreStockChrome + unmountRibbon) + FwLayout.cpp:286 (s_ribbonContext.reset() synchronous) + ViewProvider.cpp:165/175 + ViewProviderSketch.cpp:635/3963/4001 + Application.cpp:1984/2006 + Document.cpp:680-692/747-750, record R4-BLOCKER (signalInEdit fires after deactivated() within one synchronous turn so a pre-checked flag is still false then), resolve that a synchronous OR flag-guarded deactivated() teardown destroys the chrome + the FLOW-01 reset handler before the edit/exit fires (R3-BLOCKER1/R3-BLOCKER2), and DECIDE the survivable policy with the literal token `overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)` — spell out that deactivated() SCHEDULES a cancellable singleShot teardown that signalInEdit / re-activation cancels within the same event-loop turn, surviving the chrome + the FLOW-01 handler, for 04-02/04-03/04-04 to implement. Record the Phase 2-3 ribbon cross-phase observation (same synchronous pattern at FwLayout.cpp:286, likely vanishes during edits, flag for follow-up, do NOT patch shipped phases). A3: cite FwTheme.cpp:31-39 (currently no-op → made functional), FwFeatureTreeDelegate.cpp:154-159 (Highlight = system highlight, not pink), ReferenceSelection.h:42-57 (no public active-field hook) + TaskPatternParameters.h:88-91 (private activeDirectionWidget, two real direction widgets — R2-F4), resolve the active-box hook (// SW-FORK HOOK accessor OR focus inference validated against the real TaskPatternParameters panel) AND that FwTheme::apply() populates the named QPalette role on the box widget's local palette so the styler reads widget->palette().color(<role>) (R2-F6); name the exact role. Add the reuse-primitive confirmation (TaskView.h:67/82, Control.h:98-100, TaskView.cpp:393-457). Close with the VERDICT using the literal token `reuse-and-rehost committed` or `thin-adopt committed`, a carried open-obligation note pointing at `SPIKE_LIVE_CHECKLIST.md` (incl. the A4 overlays-survive live item AND the synchronous-within-one-turn confirmation), and a Sign-off block with the resume signal. Then APPEND a `## Phase 4 — PropertyManager live FEEL obligations` section to `src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md` (do not rewrite existing Phase 1-3 content) listing the [LIVE-DEFERRED] items (incl. the A2 dock-stays-left, the A4 overlays-survive, and the synchronous-within-one-turn observations). Do NOT write any production FreeWorks source in this task — the spike is a verdict document; production mounts land in Plan 04-02 gated on this verdict.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "reuse-and-rehost committed|thin-adopt committed" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -q "getDockWindow" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "addDockWidget|addDockWindow" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -q "assureWorkbench" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "deactivated" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "overlays-survive|overlays-lost" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "singleShot|deferred-cancellable" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "s_ribbonContext|signalInEdit|signalResetEdit" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "QPalette|palette role|local palette" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -Eq "SW-FORK HOOK|FreeWorks-owned|TaskPatternParameters" .planning/phases/04-propertymanager-panel/SPIKE.md && grep -q "Phase 4" src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md && echo "SPIKE-VERDICT-RECORDED" && bash tools/fw-string-leak-grep.sh</automated>
  </verify>
  <done>`SPIKE.md` exists with DECISIVE per-item verdict tokens for A1 (two-branch left-placement mechanism — re-dock-existing-left vs create-left, R2-F1; 4-name disambiguation; parent-independent getDockWindow("Tasks"); "Tasks" managed identity), A2 (dock placement stays left on deactivated(), no synchronous right-move, deferred teardown cancelled + re-assert on signalInEdit; activeDialog() guard AND a pre-checked flag recorded as insufficient per the deactivated-before-activated-before-signalInEdit ordering, R2-F2/R4-BLOCKER), A4 (the LIFECYCLE/SURVIVABILITY gate — R3-ROOT/R4-BLOCKER: the literal token `overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)` PASS / `overlays-lost` FAIL, naming the deferred-cancellable install/teardown policy for the chrome + the FLOW-01 reset handler, plus the Phase 2-3 ribbon cross-phase observation), and A3 (active-box hook via // SW-FORK HOOK or focus inference validated against the real TaskPatternParameters 2-field panel + FwTheme::apply() populates the named QPalette role on the box widget's local palette, R2-F4/R2-F6), a top-level VERDICT carrying the literal token `reuse-and-rehost committed` or `thin-adopt committed`, and a Sign-off block; `SPIKE_LIVE_CHECKLIST.md` has an appended `Phase 4` section (incl. the A4 overlays-survive live item AND the synchronous-within-one-turn confirmation); the `<automated>` block prints `SPIKE-VERDICT-RECORDED` and leak-grep is clean; no production FreeWorks source was written in this task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The D-03 container-spike verdict (`SPIKE.md`) resolving the spike-gated unknowns with DECISIVE verdict tokens — A1 (the two-branch left-placement mechanism: addDockWindow CANNOT move an already-docked panel — DockWindowManager.cpp:256-258, R2-F1 — so re-dock an existing right container left via `getMainWindow()->addDockWidget(Left, dock)` or create-left via `addDockWindow` when never-docked; the 4-name disambiguation; parent-independent `getDockWindow("Tasks")`; the "Tasks" managed identity, R2-F3), A2 (the "Tasks" dock placement stays LEFT on `deactivated()` — the `activeDialog()` guard AND a pre-checked flag are both too late because `deactivated()` runs before `activated()`, before `showDialog()`, and before `signalInEdit`, R2-F2/R4-BLOCKER — deferred teardown cancelled + re-asserted on `signalInEdit`), A4 (the R3-ROOT/R4-BLOCKER LIFECYCLE/SURVIVABILITY gate: a synchronous OR flag-guarded `deactivated()` teardown destroys the FreeWorks chrome + the FLOW-01 reset handler DURING the edit-time WB switch — `s_ribbonContext.reset()` at FwLayout.cpp:286 + `unmountPropertyManager()` fire before `showDialog`/`signalInEdit` — so the verdict names the DEFERRED-CANCELLABLE policy `overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)` for 04-02/04-03/04-04, plus the Phase 2-3 ribbon cross-phase observation), A3 (the active-reference-box hook via `// SW-FORK HOOK` accessor or focus inference VALIDATED against the real `TaskPatternParameters` 2-field panel — R2-F4 — and `FwTheme::apply()` made functional to populate the named QPalette role on the box widget's local palette so D-06's "via a QPalette role" holds, R2-F6) — plus the Wave 0 test scaffold (three new headless/offscreen test files wired into CMake; `FwReferenceBoxStyler.cpp` is a GTest INSIDE `Gui_tests_run`, NOT a separate `*_Tests_run` target — R2-F5; `setup_qt_test` creates `FwPropertyManagerWidget_Tests_run`) and the appended Phase-4 live-FEEL obligations. This gates whether Plans 04-02/04-03/04-04 build on the place-the-Tasks-TaskView-left engine (`reuse-and-rehost committed`) or the thin-adopt fallback (`thin-adopt committed`), AND that they implement the deferred-cancellable survivable-lifecycle policy. This mirrors the Phase-2 native-ribbon and Phase-3 tree-reuse spike gates, which were cleared by documentation approval (no build tree / GUI / CI in this environment).</what-built>
  <how-to-verify>
    1. Read `.planning/phases/04-propertymanager-panel/SPIKE.md`. Confirm the A1 item records that `addDockWindow` cannot move an already-docked panel (DockWindowManager.cpp:256-258, R2-F1) and that the left placement re-docks an existing container via `getMainWindow()->addDockWidget(Left, dock)` (came-from-right-dock) or creates-left via `addDockWindow` only when never-docked, with the 4-name disambiguation + parent-independent `getDockWindow("Tasks")` + "Tasks" managed identity (R2-F3).
    2. Confirm the VERDICT carries exactly one literal token: `reuse-and-rehost committed` (PASS) or `thin-adopt committed` (FAIL).
    3. Confirm A2 records the deactivated-before-activated-before-showDialog ordering (Application.cpp:1984/2006, ViewProvider.cpp:165/175, Document.cpp:680-692) and resolves that the dock STAYS LEFT on `deactivated()` (no synchronous right-move; the `activeDialog()` guard AND a pre-checked flag are both insufficient — R4-BLOCKER), with the deferred teardown cancelled + left re-asserted on `signalInEdit` (R2-F2).
    4. Confirm A4 (the R3-ROOT/R4-BLOCKER keystone) records that a synchronous OR flag-guarded `deactivated()` teardown destroys the chrome + the FLOW-01 reset handler during the edit-time WB switch (FwLayout.cpp:286 `s_ribbonContext.reset()`; FwWorkbench.cpp:66/71; ViewProvider.cpp:165/175; ViewProviderSketch.cpp:635/3963/4001; Document.cpp:680-692/747-750; the R4-BLOCKER ordering — signalInEdit fires after deactivated() in one synchronous turn), and carries the DECISIVE `overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)` PASS token (or `overlays-lost` FAIL) naming the deferred-cancellable install/teardown policy 04-02/04-03/04-04 implement, plus the Phase 2-3 ribbon cross-phase observation.
    5. Confirm A3 records the active-box hook validated against the real `TaskPatternParameters` 2-field panel (R2-F4) + `FwTheme::apply()` populating the named QPalette role on the box widget's local palette (R2-F6).
    6. Confirm `src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md` has an appended `Phase 4` section (existing Phase 1-3 content untouched) including the A4 overlays-survive live item AND the synchronous-within-one-turn confirmation the singleShot cancellation relies on.
    7. Confirm the three test files exist and `tests/src/Gui/CMakeLists.txt` wires them (`CMAKE-WIRED-OK` from Task 1; `FwReferenceBoxStyler.cpp` in `Gui_tests_run`, NO `FwReferenceBoxStyler_Tests_run` target — R2-F5; target `FwPropertyManagerWidget_Tests_run` created).
  </how-to-verify>
  <resume-signal>Type "approved: reuse-and-rehost" (or "approved: thin-adopt") to commit the engine for Plans 04-02/04-03/04-04, or describe required changes. The approval is on the documentation basis (Phase 1-3 precedent); the live placement round-trip, WB-switch observation, the A4 overlays-survive observation, and the synchronous-within-one-turn confirmation remain open obligations in `SPIKE_LIVE_CHECKLIST.md`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.FCStd` document content → panel/preview construction | A hosted `TaskDialog` builds its UI from the active document's objects; a malformed/untrusted document drives that construction. FreeWorks adds NO new parsing — the hosted panel (App-layer property validation) owns it. |
| 3D-view pick → reference-box fill | Picked geometry crosses into the hosted panel's `SelectionFilterGate`; FreeWorks only reads `Gui::Selection`, never parses raw input. |
| Workbench activate/deactivate lifecycle → chrome + handler teardown | An edit-time WB switch (assureWorkbench / Sketcher edit) fires `deactivated()` mid-edit; a synchronous teardown (or one gated on a pre-checked flag still false at that instant — R4-BLOCKER) destroys the FreeWorks chrome + the FLOW-01 reset handler before the edit/exit fires (R3-ROOT). The A4 verdict records the deferred-cancellable survivable policy. |
| (no boundary) network / auth / PII | None — desktop CAD GUI panel; no network, auth, sessions, secrets, or crypto in Phase 4 scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Document mutation during recompute (panel-driven edit) | mitigate | This plan writes NO document mutation — it is test scaffold + spike verdict only. The disposition is recorded for downstream plans: all edits route through the hosted `TaskDialog`'s `openTransaction`/`commitTransaction` (D-10), never a FreeWorks-side document write during recompute (ARCHITECTURE.md anti-pattern). |
| T-04-02 | Denial of Service (resource) | Throwaway spike harness / test bring-up leak | mitigate | No production widget is constructed in this plan; the offscreen QTEST follows the leak-grep + scoped-construction discipline. `tools/fw-string-leak-grep.sh` runs per task. |
| T-04-0L | Tampering | FreeWorks chrome + the FLOW-01 reset handler torn down DURING the edit-time WB switch (R3-ROOT/R4-BLOCKER) so the edit lands chrome-less / the sketch-exit handler never runs | mitigate | The A4 SPIKE gate resolves the survivable install/teardown policy (`overlays-survive: deferred-cancellable teardown (QTimer::singleShot(0) cancelled by signalInEdit / re-activation)` — deactivated() SCHEDULES a cancellable teardown rather than tearing down inline or gating on a pre-checked flag that is too late, R4-BLOCKER) by source-read (FwLayout.cpp:286; FwWorkbench.cpp:66/71; Application.cpp:1984/2006; ViewProvider.cpp:165/175; Document.cpp:680-692/747-750) with a live confirmation deferred to the checklist; downstream plans 04-02/04-03/04-04 implement it. The Phase 2-3 ribbon same-pattern risk is recorded as a cross-phase observation, not patched here. |
| T-04-03 | (compliance) | Trademark/asset leak in new source/tests | mitigate | No "SolidWorks" identifier in any new `.cpp`/`.h`/test (only the pre-allow-listed `Gui::SolidWorksNavigationStyle`); `SPIKE.md` prose may name the reference CAD product (grep covers `src/`, not `.planning/`). Leak-grep clean. |

All threats are `low`/`medium` for this plan (test scaffold + documentation verdict; no document mutation, no production widget, no network). T-04-0L (chrome/handler torn down mid-edit, R3-ROOT/R4-BLOCKER) is the highest-attention item and is resolved by the A4 SPIKE gate naming the deferred-cancellable teardown. None is `high`; nothing blocks.
</threat_model>

<verification>
- The `<automated>` blocks build the REAL targets (`Gui_tests_run`, `FwPropertyManagerWidget_Tests_run`) and run `ctest` chained with `&&` (no `2>/dev/null` masking) when a build tree exists; otherwise an EXPLICIT no-build-tree branch prints the compile-intended notice and the CMake wiring is grep-verified `CMAKE-WIRED-OK`. `FwReferenceBoxStyler.cpp` is a GTest inside `Gui_tests_run` (no `FwReferenceBoxStyler_Tests_run` target — R2-F5).
- `SPIKE.md` records the D-03 verdict with the literal token and DECISIVE A1/A2/A3/A4 verdict tokens (A1 two-branch placement R2-F1, A2 dock-placement-stays-left R2-F2/R4-BLOCKER, A4 overlays-survive via the deferred-cancellable teardown R3-ROOT/R4-BLOCKER, A3 real-panel hook + functional FwTheme palette role R2-F4/R2-F6).
- `SPIKE_LIVE_CHECKLIST.md` has the appended Phase 4 obligations (incl. the A4 overlays-survive live item AND the synchronous-within-one-turn confirmation).
- `tools/fw-string-leak-grep.sh` clean over new source/tests.
- The `04-VALIDATION.md` flag flip is a DISTINCT post-verify step, never woven into the `<automated>` pass/fail command.
</verification>

<success_criteria>
- Three Wave 0 test files exist, call `ensureGuiTestBootstrap()`, and are wired additively into `tests/src/Gui/CMakeLists.txt` (`FwReferenceBoxStyler.cpp` in `Gui_tests_run`, no `*_Tests_run` target — R2-F5; target `FwPropertyManagerWidget_Tests_run` created).
- The FLOW-01 decision-core test uses the reused `"SketcherGui::ViewProviderSketch"` literal (no Sketcher include) and never returns an auto-launch decision.
- The pink-box state-machine test reads its active tone through a `QPalette` role off the box widget's local palette (R2-F6, no hex / `setStyleSheet` literal) and never uses blue.
- `SPIKE.md` resolves A1 (R2-F1 two-branch placement)/A2 (R2-F2/R4-BLOCKER dock-placement-stays-left)/A4 (R3-ROOT/R4-BLOCKER overlays-survive via the deferred-cancellable teardown, naming the singleShot policy)/A3 (R2-F4 real-panel hook + R2-F6 functional palette role) with DECISIVE verdict tokens and carries the verdict token `reuse-and-rehost committed` or `thin-adopt committed`.
- Blocking spike gate cleared (documentation approval, Phase 1-3 precedent); downstream plans know which engine AND the deferred-cancellable survivable-lifecycle policy are committed.
</success_criteria>

<output>
Create `.planning/phases/04-propertymanager-panel/04-01-SUMMARY.md` when done, recording the D-03 verdict token, the A1/A2/A3/A4 resolutions (A1 two-branch placement R2-F1, A2 dock-placement-stays-left R2-F2/R4-BLOCKER, A4 overlays-survive via the deferred-cancellable teardown — QTimer::singleShot(0) cancelled by signalInEdit / re-activation — R3-ROOT/R4-BLOCKER with the named policy, A3 real-panel hook + functional FwTheme palette role R2-F4/R2-F6), and the Wave 0 test inventory for Plans 04-02/04-03/04-04 to consume.
</output>
</content>
