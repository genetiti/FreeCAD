---
phase: 02-commandmanager-ribbon
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/Gui/FreeWorks/FwRibbonMap.h
  - src/Gui/FreeWorks/FwRibbonMap.cpp
  - src/Gui/FreeWorks/FwRibbon.h
  - src/Gui/FreeWorks/FwRibbon.cpp
  - src/Gui/FreeWorks/CMakeLists.txt
  - tests/src/Gui/FwRibbon.cpp
autonomous: true
requirements:
  - RIBBON-01
user_setup: []

must_haves:
  truths:
    - "A SolidWorks user sees Features / Sketch / Evaluate tabs of large labeled icon-over-label buttons built from FreeCAD's command registry"
    - "Clicking a primary ribbon button fires the real FreeCAD command (thin trigger via CommandManager, no duplicated backend)"
    - "Flyout split-buttons work: a *_Comp* group id (e.g. dress-up Fillet/Chamfer, additive primitives) shows a dropdown of related commands — D-13 (large icon-over-label buttons + working flyouts driven by the registry's command groups, using existing icons; final SW theming is Phase 7's job, structure + behavior only)"
    - "An uncurated workbench auto-derives its ribbon tabs from a LIVE source — Workbench::getToolbarItems() (public, returns std::list<pair<string,list<string>>>), NOT the transient setupToolBars() ToolBarItem* tree which Workbench::activate() consumes/deletes (REVIEW concern 5)"
    - "The per-row resolution test passes for EVERY curated row INCLUDING the Evaluate tab, because Plan 02-01's ensureGuiTestBootstrap() imports the owning GUI modules: MeasureGui (Std_Measure/Std_MassProperties), the PartGui pulled in by PartDesignGui (Part_CheckGeometry), and MatGui (Materials_Inspect*). If any of those module imports is missing from the bootstrap, Std_Measure resolves to nullptr and this test FAILS — it is the load-bearing guard that the bootstrap covers all owning modules (REVIEW cycle-2 NEW HIGH 2)"
    - "Curated command IDs that do not resolve are silently omitted at RUNTIME (no extras appended) — D-08 — BUT the headless per-row test treats an unresolved curated row as a FAILURE (a missing owning-module import must fail loudly in test, not degrade the ribbon silently); D-08 silent-omit is a runtime robustness behavior, not a license for the test to pass on unresolved curated rows"
    - "Flyout verification inspects the ACTUAL widget output: after cmd->addTo(toolbar), the produced QToolButton has popupMode()==MenuButtonPopup and its menu has >1 action — NOT merely a getGroupCommands() metadata count (REVIEW concern 8)"
  artifacts:
    - path: "src/Gui/FreeWorks/FwRibbonMap.h"
      provides: "Declarative curated tab→panel→commandId table type + accessor"
      contains: "FwRibbonMap"
    - path: "src/Gui/FreeWorks/FwRibbonMap.cpp"
      provides: "The curated core-loop rows (Features/Sketch/Evaluate) of real command IDs"
      contains: "PartDesign_Pad"
    - path: "src/Gui/FreeWorks/FwRibbon.cpp"
      provides: "Full tab/panel build from the curated map + auto-derive fallback from getToolbarItems()"
      contains: "getToolbarItems"
  key_links:
    - from: "src/Gui/FreeWorks/FwRibbon.cpp"
      to: "src/Gui/FreeWorks/FwRibbonMap.cpp"
      via: "buildFromCuratedMap consumes FwRibbonMap rows"
      pattern: "FwRibbonMap"
    - from: "src/Gui/FreeWorks/FwRibbon.cpp"
      to: "Gui::Workbench::getToolbarItems()"
      via: "auto-derive consumes the public getToolbarItems() list (group name -> command ids) when no curated map"
      pattern: "getToolbarItems"
    - from: "src/Gui/FreeWorks/FwRibbon.cpp"
      to: "Gui::Command::addTo"
      via: "command id → existing QAction button (incl. *_Comp* split-buttons)"
      pattern: "addTo"
---

<objective>
Deliver the full RIBBON-01 vertical slice: the curated SolidWorks-faithful declarative map (D-05/D-06/D-08) and the complete `FwRibbon` build that turns it into Features / Sketch / Evaluate tabs of large labeled buttons and working flyout split-buttons, plus the auto-derive fallback (D-07) so any workbench gets a non-empty ribbon. After this plan, a SolidWorks user looking at FreeWorks mode sees a recognizable CommandManager whose buttons fire the real PartDesign/Sketcher commands — the "Features tab whose Pad button actually fires the command" slice. Mounting into the window, chrome hiding, and persistence are Plan 03; context tab switching is Plan 04.

Purpose: RIBBON-01 — the tabbed ribbon driven by the command registry with large labeled icons and flyout split-buttons (Success Criteria 1, 2, 3).
Output: `FwRibbonMap.{h,cpp}` (curated table + auto-derive support), extended `FwRibbon.{h,cpp}` (`buildFromCuratedMap`, `buildAutoDerived`, panel titles, unique panel objectNames), extended GTest coverage.

> Dependency on Plan 02-01: this plan may only proceed if `SPIKE.md` records the native verdict — i.e. it contains `native committed`. If `SPIKE.md` records `SARibbon fallback triggered` instead, STOP and re-scope this plan's FwRibbon build actions onto the SARibbon API before executing (the curated map + command-ID rows are engine-agnostic and carry over unchanged). Do not proceed merely because Plan 02-01 ran.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-commandmanager-ribbon/02-CONTEXT.md
@.planning/phases/02-commandmanager-ribbon/02-RESEARCH.md
@.planning/phases/02-commandmanager-ribbon/02-PATTERNS.md
@.planning/phases/02-commandmanager-ribbon/02-UI-SPEC.md
@.planning/phases/02-commandmanager-ribbon/02-01-SUMMARY.md
@src/Mod/PartDesign/Gui/Workbench.cpp
@src/Mod/Sketcher/Gui/Workbench.cpp
</context>

<artifacts_this_phase_produces>
- **This plan adds:** `FwRibbonMap.{h,cpp}` (namespace `FreeWorksGui`) — `struct FwRibbonRow { const char* tab; const char* panel; const char* commandId; }` and a `constexpr`/`std::array` table of curated rows plus an accessor returning the rows for the core loop; `FwRibbon` gains `buildFromCuratedMap()`, `buildAutoDerived(const std::list<std::pair<std::string, std::list<std::string>>>& toolbarGroups)` (consumes the value-type list from `Workbench::getToolbarItems()` — a stable, copied source, NOT the transient `setupToolBars()` tree), `setCurrentTab(const QString&)` (exposed now for Plan 04), per-panel `QToolBar` objectNames `Fw_RibbonPanel_<Tab>_<Panel>`. Curated tab names: `Features`, `Sketch`, `Evaluate`.

> **Evaluate-tab owning modules (REVIEW cycle-2 NEW HIGH 2):** The Evaluate row IDs are NOT all owned by PartDesignGui/SketcherGui — `Std_Measure`/`Std_MassProperties` are registered by `MeasureGui` (`src/Mod/Measure/Gui/Command.cpp:48,94`), `Materials_Inspect*` by `MatGui` (`src/Mod/Material/Gui/Command.cpp`), and `Part_CheckGeometry` by `PartGui` (`src/Mod/Part/Gui/Command.cpp`). The per-row resolution test for these rows depends on Plan 02-01's `ensureGuiTestBootstrap()` importing `MeasureGui` and `MatGui` (PartGui arrives via PartDesignGui). This plan does NOT change the bootstrap (that is Plan 01's job, now fixed) — it RELIES on it and the per-row test is what catches a regression if a module import is dropped.

> **Auto-derive source (REVIEW concern 5):** `Workbench::setupToolBars()` returns a heap `ToolBarItem*` that `Workbench::activate()` builds the real toolbars from and then deletes — it is not a live source at ribbon-build time. `Gui::Workbench::getToolbarItems()` (public, `Workbench.h:103`) returns a copied `std::list<std::pair<std::string /*group*/, std::list<std::string> /*command ids*/>>` and IS a valid live source. The separator sentinel inside FreeCAD's toolbar tree is the literal string `"Separator"` (verified: `Workbench.cpp:290`), not an empty command — auto-derive must treat `"Separator"` as the separator, not `command()==""`.
- **Pre-existing (Plan 01):** `FwRibbon` shell + `tabCount()` + `addTabFromCommandIds`.
- **Later (Plan 03):** mount/chrome/persistence/overflow. **Later (Plan 04):** `FwRibbonContext`.

> If SPIKE.md verdict was "SARibbon fallback", re-scope this plan's FwRibbon build actions onto the SARibbon API (SARibbonCategory/SARibbonPanel) before executing — the curated map and command-ID rows are engine-agnostic and carry over unchanged.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curated declarative map (FwRibbonMap) — core-loop rows of real command IDs</name>
  <files>src/Gui/FreeWorks/FwRibbonMap.h, src/Gui/FreeWorks/FwRibbonMap.cpp, src/Gui/FreeWorks/CMakeLists.txt, tests/src/Gui/FwRibbon.cpp</files>
  <read_first>
    - src/Mod/PartDesign/Gui/Workbench.cpp lines 487-526 (verified additive/subtractive/transformation command-ID groupings) and lines 177,208 (dress-up IDs PartDesign_Fillet/Chamfer)
    - src/Mod/Sketcher/Gui/Workbench.cpp lines 264-301 (lifecycle), 325-447 (geometry comp/create IDs), 477-563 (constraint IDs) AND src/Mod/Sketcher/Gui/CommandConstraints.cpp (the real `Sketcher_Constrain*` IDs — already verified/PINNED in RESEARCH § "Verified core-loop command IDs", Open Questions Q2 RESOLVED; NO execute-time grep needed, just place the pinned IDs from this plan's action below)
    - .planning/phases/02-commandmanager-ribbon/02-RESEARCH.md § "Verified core-loop command IDs" (pinned constraint + measure IDs) + § "Open Questions (RESOLVED)" Q1/Q2 + § "Curated-Map File Format" (C++ table recommendation) + Assumptions A1/A2
    - src/Mod/Measure/Gui/Command.cpp lines 48,94 (`Std_Measure`/`Std_MassProperties` owned by MeasureGui), src/Mod/Material/Gui/Command.cpp (`Materials_Inspect*` owned by MatGui), src/Mod/Part/Gui/Command.cpp (`Part_CheckGeometry` owned by PartGui) — confirms the Evaluate-tab rows need MeasureGui+MatGui (+PartGui via PartDesignGui) imported by Plan 01's bootstrap (REVIEW cycle-2 NEW HIGH 2)
    - tests/src/Gui/FwTestGuiBootstrap.h (Plan 01 — confirm it imports MeasureGui and MatGui before relying on the Evaluate rows resolving)
    - .planning/phases/02-commandmanager-ribbon/02-PATTERNS.md § "FwRibbonMap.{h,cpp}"
    - .planning/phases/02-commandmanager-ribbon/02-UI-SPEC.md § Copywriting Contract (curated tab names `Features`/`Sketch`/`Evaluate`; panel-group titles authored per panel; D-08 omit-missing)
    - src/Gui/FreeWorks/FwLayout.h (header license/copyright block to copy verbatim)
  </read_first>
  <behavior>
    - The curated table exposes >=3 distinct tab names: "Features", "Sketch", "Evaluate".
    - Every commandId in the curated table either resolves via getCommandByName OR is recorded as a known gap note (D-08) — the headless test asserts each resolves or is in an allow-listed gap set.
    - The Features tab includes a *_Comp* group id (e.g. "PartDesign_CompPrimitiveSubtractive" and the dress-up group) so a flyout is produced downstream.
    - Tab order in the table is Features → Sketch → Evaluate (RESEARCH Open Questions Q1 RESOLVED-WITH-FALLBACK; note the source in a comment).
  </behavior>
  <action>
    Create `FwRibbonMap.h`/`FwRibbonMap.cpp` under `src/Gui/FreeWorks/`, namespace `FreeWorksGui`, copying FwLayout's license/copyright header. Define `struct FwRibbonRow { const char* tab; const char* panel; const char* commandId; };` and a `constexpr std::array<FwRibbonRow, N>` (or a function returning a `std::span`/`const std::vector<FwRibbonRow>&`) of curated rows. Populate from the PINNED IDs in RESEARCH § "Verified core-loop command IDs" (Open Questions Q1/Q2 RESOLVED — these are real registered IDs read from this checkout; place them as-is, no grep):

    **Features** tab:
      - panel "Sketch": `PartDesign_NewSketch`
      - panel "Additive": flyout `PartDesign_CompPrimitiveAdditive`, plus `PartDesign_Pad`, `PartDesign_Revolution`, `PartDesign_AdditiveLoft`, `PartDesign_AdditivePipe`, `PartDesign_AdditiveHelix`
      - panel "Subtractive": flyout `PartDesign_CompPrimitiveSubtractive`, plus `PartDesign_Pocket`, `PartDesign_Hole`, `PartDesign_Groove`, `PartDesign_SubtractiveLoft`, `PartDesign_SubtractivePipe`, `PartDesign_SubtractiveHelix`
      - panel "Dress-Up": `PartDesign_Fillet`, `PartDesign_Chamfer`, `PartDesign_Draft`, `PartDesign_Thickness`
      - panel "Pattern": `PartDesign_Mirrored`, `PartDesign_LinearPattern`, `PartDesign_PolarPattern`, `PartDesign_MultiTransform`
      - panel "Reference": `PartDesign_Body`, `PartDesign_ShapeBinder`, `PartDesign_SubShapeBinder`, `PartDesign_Clone`, `PartDesign_Boolean`

    **Sketch** tab:
      - panel "Sketch": lifecycle `Sketcher_NewSketch`, `Sketcher_EditSketch`, `Sketcher_LeaveSketch`, `Sketcher_MapSketch`, `Sketcher_ValidateSketch`
      - panel "Geometry": flyouts `Sketcher_CompLine`, `Sketcher_CompCreateArc`, `Sketcher_CompCreateConic`, `Sketcher_CompCreateRectangles`, `Sketcher_CompCreateRegularPolygon`, plus `Sketcher_CreateLine`, `Sketcher_CreatePolyline`, `Sketcher_CreateArc`, `Sketcher_CreateCircle`, `Sketcher_CreateRectangle`
      - panel "Dimensions": `Sketcher_Dimension`, `Sketcher_ConstrainDistance`, `Sketcher_ConstrainDistanceX`, `Sketcher_ConstrainDistanceY`, `Sketcher_ConstrainRadius`, `Sketcher_ConstrainDiameter`, `Sketcher_ConstrainRadiam`, `Sketcher_ConstrainAngle` (dimension flyouts `Sketcher_CompDimensionTools`, `Sketcher_CompConstrainRadDia` may be used in place of the individual radius/diameter IDs)
      - panel "Relations": `Sketcher_ConstrainCoincidentUnified` (or `Sketcher_ConstrainCoincident` + `Sketcher_ConstrainPointOnObject`), `Sketcher_ConstrainHorizontal`, `Sketcher_ConstrainVertical` (flyout `Sketcher_CompHorVer`), `Sketcher_ConstrainParallel`, `Sketcher_ConstrainPerpendicular`, `Sketcher_ConstrainTangent`, `Sketcher_ConstrainEqual`, `Sketcher_ConstrainSymmetric`, `Sketcher_ConstrainBlock`, `Sketcher_ConstrainLock`

    **Evaluate** tab:
      - panel "Evaluate": `Part_CheckGeometry`, `Std_Measure`, `Std_MassProperties`, `Materials_InspectMaterial`, `Materials_InspectAppearance`

    Panel-group titles are SW-faithful plain nouns authored per panel (UI-SPEC). Apply D-08 strictly: these IDs are already verified, so place them directly; do NOT auto-append any FreeCAD-only extras; track notable SW-with-no-FreeCAD-equivalent gaps as `// gap:` comments. Append `FwRibbonMap.cpp`/`.h` to `FreeWorks_CPP_SRCS`/`FreeWorks_HPP_SRCS`. In `tests/src/Gui/FwRibbon.cpp` add a test iterating every curated row asserting `getCommandByName(row.commandId) != nullptr` (the resolution test is the typo guard, since the IDs are pinned not grepped). Keep `kKnownGaps` EMPTY initially (REVIEW concern, MEDIUM): an allow-list that swallows unresolved IDs would undermine the typo guard by silently accepting a misspelling. If a curated row genuinely has no FreeCAD command, remove the row and record the absence as a `// gap:` comment rather than adding a fake/unresolvable command ID to `kKnownGaps`. The resolution test must therefore assert EVERY curated `commandId` resolves non-null (run after `tests::ensureGuiTestBootstrap()` from Plan 01 so module commands are loaded). No "SolidWorks" string anywhere (leak-grep).
  </action>
  <verify>
    <automated>ctest -R Gui_tests_run --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/Gui/FreeWorks/FwRibbonMap.h` contains `FwRibbonRow` and `FwRibbonMap`.
    - `src/Gui/FreeWorks/FwRibbonMap.cpp` contains `PartDesign_Pad`, `Sketcher_NewSketch`, at least one `*_Comp*` id (`PartDesign_Comp` substring present), at least one pinned constraint id (`Sketcher_ConstrainDistance`), and the Evaluate IDs (`Part_CheckGeometry`, `Std_Measure`).
    - The map exposes the three tab names `Features`, `Sketch`, `Evaluate` (grep each present in FwRibbonMap.cpp).
    - `grep -v '^#' src/Gui/FreeWorks/CMakeLists.txt | grep -c 'FwRibbonMap.cpp'` returns >=1.
    - `ctest -R Gui_tests_run --output-on-failure` passes, including the per-row resolution test (the typo guard for the pinned IDs), which runs after `tests::ensureGuiTestBootstrap()` so ALL owning module commands are loaded; the test explicitly covers the Evaluate rows — `getCommandByName("Std_Measure")`, `"Std_MassProperties"`, `"Part_CheckGeometry"`, and `"Materials_InspectMaterial"` all resolve non-null (proves the bootstrap imported MeasureGui/MatGui/PartGui — REVIEW cycle-2 NEW HIGH 2). If any returns nullptr the test FAILS rather than passing on an unresolved curated row.
    - `kKnownGaps` (if present at all) is empty — `grep -A3 'kKnownGaps' tests/src/Gui/FwRibbon.cpp` shows no command-ID string literals inside it.
    - No "SolidWorks" identifier/string in the new files.
  </acceptance_criteria>
  <done>The curated C++ table compiles into FreeCADGui from the pinned verified IDs, every curated ID resolves non-null (no swallowing allow-list), the three core-loop tabs are present, and the per-row resolution test is green after the module-GUI bootstrap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FwRibbon full build — curated tabs/panels, flyouts, and auto-derive fallback</name>
  <files>src/Gui/FreeWorks/FwRibbon.h, src/Gui/FreeWorks/FwRibbon.cpp, tests/src/Gui/FwRibbonWidget.cpp</files>
  <read_first>
    - src/Gui/FreeWorks/FwRibbon.h, src/Gui/FreeWorks/FwRibbon.cpp (the Plan 01 minimal shell to extend)
    - src/Gui/Action.cpp lines 472-485 (ActionGroup::addTo → native MenuButtonPopup split-button: after addAction it sets `tb->setPopupMode(QToolButton::MenuButtonPopup)` and `tb->setMenu(menu)` with the group's actions — the flyout comes free from a *_Comp* id; THIS is what the flyout test must inspect)
    - src/Gui/Workbench.h lines 103 (getToolbarItems() — public; returns `std::list<std::pair<std::string, std::list<std::string>>>`), 119 (setupToolBars() is the transient tree — DO NOT use as the live source) — auto-derive source per REVIEW concern 5
    - src/Gui/Workbench.cpp lines 288-300 (the `"Separator"` literal sentinel used in the toolbar tree)
    - src/Gui/Command.h lines 421 (addTo), 1005 (getGroupCommands), 1011 (getCommandByName)
    - .planning/phases/02-commandmanager-ribbon/02-RESEARCH.md § "Pattern 1/2" + "Auto-derive a tab for an uncurated workbench (D-07)" code example
    - .planning/phases/02-commandmanager-ribbon/02-PATTERNS.md § "FwRibbon.{h,cpp}" (large button, flyout reuse, auto-derive, objectName discipline) + § "Unique objectName for persistence"
    - .planning/phases/02-commandmanager-ribbon/02-UI-SPEC.md § Spacing (32px icon, 56×72 button, 88-96 ribbon body, 1px separator) + § Typography (2-line wrapped labels, never ellipsis) + § Color (QPalette/QStyle only) + § Copywriting (empty-state strings)
  </read_first>
  <behavior>
    - buildFromCuratedMap() produces tabCount()==3 (Features/Sketch/Evaluate), each tab hosting >=1 QToolBar panel of large icon-over-label buttons; unresolved curated IDs are silently skipped (D-08).
    - A flyout group id in the curated map produces a real `QToolButton` whose `popupMode()==QToolButton::MenuButtonPopup` and whose `menu()` has >1 action — asserted by inspecting the actual widget produced by `addTo` on a QToolBar, NOT by counting `getGroupCommands()` metadata (REVIEW concern 8).
    - buildAutoDerived(getToolbarItems()-shaped list) on a workbench with N toolbar groups produces N panels; a `"Separator"` entry inserts a separator (not a button); a list with zero derivable groups yields the empty-state widget (UI-SPEC copy "No commands available for this workbench").
    - Each persisted panel QToolBar has a unique objectName `Fw_RibbonPanel_<Tab>_<Panel>` (Pitfall 4 / D-14 prep).
  </behavior>
  <action>
    Extend `FwRibbon` with `void buildFromCuratedMap();` (iterate `FwRibbonMap` rows, grouping by tab then panel; create one tab page per tab, one `QToolBar` panel per panel title, one button per resolved command id via `cmd->addTo(panel)`; `*_Comp*` ids yield native split-buttons automatically — `Action.cpp:472-485`; skip nullptr ids — D-08), `void buildAutoDerived(const std::list<std::pair<std::string, std::list<std::string>>>& toolbarGroups);` consuming the value-type list returned by `Gui::Workbench::getToolbarItems()` (`Workbench.h:103`) — each pair's first (group name) → a panel, each command-id in the pair's second → a button via `getCommandByName`+`addTo`, and an id equal to the literal `"Separator"` (`Workbench.cpp:290`) → `panel->addSeparator()` instead of a button. Do NOT walk the transient `setupToolBars()` `ToolBarItem*` tree — `Workbench::activate()` consumes and deletes it, so it is not a live source at ribbon-build time (REVIEW concern 5). Also add `void setCurrentTab(const QString& tabName);` (find tab by text, set current index — exposed now so Plan 04's context switcher can call it). Apply UI-SPEC metrics on every panel `QToolBar`: `setToolButtonStyle(Qt::ToolButtonTextUnderIcon)`, `setIconSize(QSize(32,32))`, 1px separators between panels, labels word-wrap to 2 lines (no ellipsis). Color via `QPalette`/native `QStyle` ONLY — no `setStyleSheet`/hex (Phase 7 owns theming). Assign each panel `QToolBar` a unique `objectName` `Fw_RibbonPanel_<Tab>_<Panel>` (Pitfall 4). When a build yields zero tabs/panels, show the empty-state widget with the UI-SPEC empty-state copy. Add tests in the QTEST_MAIN Qt target `tests/src/Gui/FwRibbonWidget.cpp` (QWidget construction needs a QApplication — REVIEW concern 1): curated build yields 3 tabs; the flyout test resolves a `*_Comp*` command, calls `cmd->addTo(toolbar)`, finds the resulting `QToolButton` via `toolbar->findChildren<QToolButton*>()`, and `QVERIFY`s `popupMode()==QToolButton::MenuButtonPopup` and `menu()->actions().size() > 1` (the working-split-button proof); auto-derive from a synthesized `std::list<std::pair<std::string,std::list<std::string>>>` (including a `"Separator"` entry) yields the expected panel + separator count; panel objectNames are unique. No "SolidWorks" string (leak-grep).
  </action>
  <verify>
    <automated>ctest -R "Gui_tests_run|FwRibbonWidget_Tests_run" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/Gui/FreeWorks/FwRibbon.h` declares `buildFromCuratedMap`, `buildAutoDerived` (taking the `std::list<std::pair<std::string,std::list<std::string>>>` value type), and `setCurrentTab`.
    - `src/Gui/FreeWorks/FwRibbon.cpp` contains `getToolbarItems` consumption (or the value-list parameter walk), the `"Separator"` sentinel handling (`addSeparator`), `Fw_RibbonPanel_`, and `Qt::ToolButtonTextUnderIcon`.
    - `grep -c 'setStyleSheet' src/Gui/FreeWorks/FwRibbon.cpp` returns 0.
    - The flyout test in `tests/src/Gui/FwRibbonWidget.cpp` contains `MenuButtonPopup` and `findChildren<QToolButton` (inspects the real widget, not just metadata).
    - `ctest -R "Gui_tests_run|FwRibbonWidget_Tests_run" --output-on-failure` passes: curated-build-3-tabs, flyout-real-MenuButtonPopup-menu>1, auto-derive-panel+separator-count, unique-panel-objectName tests all green.
    - No "SolidWorks" identifier/string in `FwRibbon.{h,cpp}` or the test.
  </acceptance_criteria>
  <done>FwRibbon builds the curated 3-tab ribbon of large labeled buttons and working flyout split-buttons (verified by inspecting the real QToolButton's MenuButtonPopup + menu) from the registry, auto-derives non-curated workbenches from the live getToolbarItems() list (honoring the "Separator" sentinel), omits unresolved IDs, and all build/flyout/auto-derive tests pass under a QApplication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated map (developer constants) → CommandManager | Command-ID strings are compile-time developer constants resolved via `getCommandByName` (null-checked, D-08). No user-supplied input. |
| auto-derive reads workbench ToolBarItem tree | Read-only traversal of existing registry data; no mutation of App or registry. |
| FwRibbon Gui widget → App layer | Ribbon reads command metadata only; fires via existing `Gui::Command`; no App includes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Repudiation/Legal | FwRibbonMap.cpp, FwRibbon.cpp, test | mitigate | No "SolidWorks" identifier/string; `tools/fw-string-leak-grep.sh` gate; SW tab names are generic English nouns (Features/Sketch/Evaluate) sourced from the command registry, not proprietary art |
| T-02-03 | Info Disclosure | FwRibbon ↔ App separation | mitigate | No `App/`/`Mod/Sketcher` includes; command firing via `CommandManager` only; auto-derive reads `ToolBarItem` via Gui getters |
| T-02-04 | Input Validation | Curated/derived command-ID resolution | accept | IDs developer-authored or registry-sourced; `getCommandByName` null-checked (D-08); `leaf->command()==""` handled as separator — low risk |
| T-02-DC | Tampering | Ribbon firing a destructive command | accept | Buttons are thin triggers; D-08 places only real registered command IDs (no synthesized destructive actions); destructive commands keep their existing downstream confirmation — no new unconfirmed destructive path |
| T-02-02 | Tampering | src/Gui/FreeWorks/CMakeLists.txt | mitigate | Additive `target_sources` only; no shared-file edits |
</threat_model>

<verification>
- `ctest -R "Gui_tests_run|FwRibbonWidget_Tests_run" --output-on-failure` green (per-row resolution after module-GUI load in Gui_tests_run; 3-tab curated build, real-MenuButtonPopup flyout, auto-derive panel+separator count, unique panel objectNames in the QTEST_MAIN widget target).
- Auto-derive consumes `Workbench::getToolbarItems()` (live, value-type) — NOT the transient `setupToolBars()` tree (REVIEW concern 5); `"Separator"` literal handled as separator.
- Flyout proof inspects the real `QToolButton` (`MenuButtonPopup` + `menu()->actions().size()>1`), not `getGroupCommands()` metadata (REVIEW concern 8).
- Curated tab names are Features/Sketch/Evaluate; only real (pinned, verified) command IDs placed (D-08); `kKnownGaps` empty.
- The per-row resolution test asserts every curated row resolves non-null INCLUDING the Evaluate rows (`Std_Measure`/`Std_MassProperties`/`Part_CheckGeometry`/`Materials_Inspect*`), which only pass because Plan 01's bootstrap imports MeasureGui/MatGui (+PartGui via PartDesignGui) — REVIEW cycle-2 NEW HIGH 2.
- No `setStyleSheet`/hex (UI-SPEC § Color); no `Mod/Sketcher` include (Pitfall 2); no "SolidWorks" string.
</verification>

<success_criteria>
- RIBBON-01 SC1/SC2/SC3: tabbed ribbon of large labeled icons driven by the command registry, with working flyout split-buttons, firing real commands as thin triggers.
- D-05/D-06/D-08: curated declarative map of the core loop, omit-missing/no-extras honored.
- D-07: uncurated workbenches auto-derive a non-empty ribbon from the live `Workbench::getToolbarItems()` list (REVIEW concern 5).
- D-13: large icon-over-label buttons + flyouts using existing registry icons; no final theming.
</success_criteria>

<output>
Create `.planning/phases/02-commandmanager-ribbon/02-02-SUMMARY.md` when done. Record: the final curated tab/panel/command-ID roster (and any recorded gaps), the auto-derive behavior, and the FwRibbon build API (`buildFromCuratedMap`/`buildAutoDerived`/`setCurrentTab`) Plans 03-04 consume.
</output>
