---
phase: 02-commandmanager-ribbon
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/Gui/FreeWorks/FwLayout.h
  - src/Gui/FreeWorks/FwLayout.cpp
  - src/Gui/FreeWorks/FwWorkbench.h
  - src/Gui/FreeWorks/FwWorkbench.cpp
  - src/Gui/FreeWorks/FwRibbon.h
  - src/Gui/FreeWorks/FwRibbon.cpp
  - tests/src/Gui/FwRibbon.cpp
autonomous: true
requirements:
  - RIBBON-01
user_setup: []

must_haves:
  truths:
    - "Entering FreeWorks mode mounts the ribbon in the top toolbar area, replacing FreeCAD's menu bar and toolbars"
    - "Leaving FreeWorks mode reversibly restores the stock menu bar and toolbars so other workbenches keep their chrome"
    - "No command is stranded: with chrome hidden, keyboard shortcuts keep working and a 'More commands…' overflow affordance reaches uncurated commands (D-12)"
    - "Ribbon tab/panel layout persists across app restart via QMainWindow saveState/restoreState keyed by unique Fw_* objectNames (D-14)"
  artifacts:
    - path: "src/Gui/FreeWorks/FwLayout.cpp"
      provides: "Mount FwRibbon in TopToolBarArea; hide/restore stock chrome; persistence hook"
      contains: "TopToolBarArea"
    - path: "src/Gui/FreeWorks/FwWorkbench.cpp"
      provides: "activated() builds+mounts ribbon and hides chrome; deactivation restores chrome"
      contains: "activated"
    - path: "src/Gui/FreeWorks/FwRibbon.cpp"
      provides: "More-commands overflow button (D-12) + Fw_Ribbon objectName"
      contains: "Fw_Ribbon"
  key_links:
    - from: "src/Gui/FreeWorks/FwLayout.cpp"
      to: "Gui::ToolBarAreaWidget (TopToolBarArea)"
      via: "topArea->addWidget(fwRibbon)"
      pattern: "TopToolBarArea"
    - from: "src/Gui/FreeWorks/FwLayout.cpp"
      to: "Gui::ToolBarManager::setState + menuBar()->hide()"
      via: "reversible chrome hide/restore"
      pattern: "ForceHidden"
    - from: "src/Gui/FreeWorks/FwRibbon.cpp"
      to: "QMainWindow saveState/restoreState"
      via: "unique objectName Fw_Ribbon for layout round-trip"
      pattern: "Fw_Ribbon"
---

<objective>
Wire the ribbon into the live window as a single command surface: mount `FwRibbon` in the reserved `TopToolBarArea` from `FwLayout`/`FwWorkbench`, hide FreeCAD's menu bar + toolbars reversibly in FreeWorks mode (D-11), add the discoverability escape hatch so no command is stranded (D-12), and make the layout persist across restarts (D-14). After this plan, launching FreeWorks mode shows the ribbon *replacing* menus+toolbars, switching away cleanly restores stock chrome, and the ribbon's tab/panel state survives a restart — the end-to-end "ribbon is the app's command surface" slice. Context tab switching is Plan 04.

Purpose: RIBBON-01 Success Criteria 1 ("replacing FreeCAD's menus+toolbars") and 5 (layout persists; survives upstream sync as a self-contained widget); CONTEXT D-11/D-12/D-14.
Output: extended `FwLayout.{h,cpp}` (mount + chrome hide/restore + persistence hook), extended `FwWorkbench.{h,cpp}` (activate builds/mounts/hides, deactivate restores), `FwRibbon` overflow "More commands…" button + `Fw_Ribbon` objectName, GTest for chrome round-trip + persistence round-trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-commandmanager-ribbon/02-CONTEXT.md
@.planning/phases/02-commandmanager-ribbon/02-RESEARCH.md
@.planning/phases/02-commandmanager-ribbon/02-PATTERNS.md
@.planning/phases/02-commandmanager-ribbon/02-UI-SPEC.md
@.planning/phases/02-commandmanager-ribbon/02-02-SUMMARY.md
@src/Gui/FreeWorks/FwLayout.cpp
@src/Gui/FreeWorks/FwWorkbench.cpp
</context>

<artifacts_this_phase_produces>
- **This plan adds:** `FwLayout` gains `mountRibbon()` (build `FwRibbon`, set objectName `Fw_Ribbon`, `topArea->addWidget`), `hideStockChrome()` / `restoreStockChrome()` (reversible, FreeWorks-scoped); `FwWorkbench::activated()` calls them and a deactivation path restores chrome; `FwRibbon` gains a pinned "More commands…" overflow `QToolButton` (D-12) and the `Fw_Ribbon` objectName for persistence.
- **Pre-existing:** `FwRibbon` build API (Plan 02), `FwLayout::install` + `FwWorkbench` lifecycle (Phase 1).
- **Later (Plan 04):** `FwRibbonContext` subscribes to edit signals and calls `FwRibbon::setCurrentTab`.

> If SPIKE.md verdict was "SARibbon fallback", the mount/hide/persistence wiring is engine-agnostic (it operates on the ribbon widget + MainWindow chrome); only the ribbon-widget type changes.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mount the ribbon in TopToolBarArea + reversible stock-chrome hide/restore (D-11)</name>
  <files>src/Gui/FreeWorks/FwLayout.h, src/Gui/FreeWorks/FwLayout.cpp, src/Gui/FreeWorks/FwWorkbench.h, src/Gui/FreeWorks/FwWorkbench.cpp, tests/src/Gui/FwRibbon.cpp</files>
  <read_first>
    - src/Gui/FreeWorks/FwLayout.cpp (existing install(); observe-the-DOM note lines 77-87; placeholder/objectName discipline lines 43-59,90-105)
    - src/Gui/FreeWorks/FwWorkbench.cpp (existing activated() lines 44-57; setupDockWindows() top-area-free note line 96)
    - src/Gui/ToolBarAreaWidget.h lines 36-48 (enum `ToolBarArea::TopToolBarArea`; `addWidget()`)
    - src/Gui/ToolBarManager.h lines 154-158 (State `ForceHidden`/`RestoreDefault`), 174-175 (`setState`), 181 (`toolBarAreaWidget`)
    - src/Gui/MainWindow.h line 74 (MainWindow IS-A QMainWindow; `menuBar()` access via `Gui::getMainWindow()`)
    - .planning/phases/02-commandmanager-ribbon/02-PATTERNS.md § "FwLayout.{h,cpp} (EXTEND)" + § "FwWorkbench.{h,cpp} (EXTEND)" + § "Reversible, FreeWorks-scoped chrome hiding"
    - .planning/phases/02-commandmanager-ribbon/02-RESEARCH.md § "Mounting the ribbon in the reserved top area" + Pitfall 3 (non-reversible chrome hiding) + Pitfall 4 (objectName)
  </read_first>
  <action>
    Extend `FwLayout` with `mountRibbon()`: construct an `FwRibbon`, call `buildFromCuratedMap()` (or `buildAutoDerived(...)` when no curated map for the active workbench — Plan 02 API), set its `objectName` to `Fw_Ribbon` (Pitfall 4 / D-14), obtain the top `ToolBarAreaWidget` via `Gui::ToolBarManager::getInstance()->toolBarAreaWidget(...)` for `ToolBarArea::TopToolBarArea` (`ToolBarAreaWidget.h:41`), and `topArea->addWidget(fwRibbon)` (`ToolBarAreaWidget.h:48`). Add `hideStockChrome()` and `restoreStockChrome()`: hide with `Gui::ToolBarManager::getInstance()->setState(names, ToolBarManager::State::ForceHidden)` + `Gui::getMainWindow()->menuBar()->hide()`; restore with `State::RestoreDefault` + `menuBar()->show()` (reversible, FreeWorks-scoped — Pitfall 3). Operate only through public Gui singletons; do NOT edit `MainWindow.cpp` (observe-the-DOM); if any unavoidable shared touch arises mark it `// SW-FORK HOOK`. In `FwWorkbench::activated()` call `FwLayout::mountRibbon()` + `FwLayout::hideStockChrome()` after the existing nav/theme calls; add a `deactivated()` override (or the framework's deactivation hook) that calls `FwLayout::restoreStockChrome()` so stock workbenches keep their chrome. Keep `setupDockWindows()` unchanged (top area stays free). Add a headless GTest to `tests/src/Gui/FwRibbon.cpp` that toggles the hide/restore path (where reachable headless: assert the ribbon widget gets objectName `Fw_Ribbon`; the live menu-bar round-trip is the manual-checklist item per VALIDATION). No "SolidWorks" string (leak-grep).
  </action>
  <verify>
    <automated>ctest -R Gui_tests_run --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/Gui/FreeWorks/FwLayout.h` declares `mountRibbon`, `hideStockChrome`, `restoreStockChrome`.
    - `src/Gui/FreeWorks/FwLayout.cpp` contains `TopToolBarArea`, `ForceHidden`, `RestoreDefault`, and `Fw_Ribbon`.
    - `src/Gui/FreeWorks/FwWorkbench.cpp` activated() references `mountRibbon`/`hideStockChrome`; a deactivation path references `restoreStockChrome`.
    - `grep -rn 'MainWindow.cpp' src/Gui/FreeWorks/` shows no edit to MainWindow.cpp (additive discipline); any `// SW-FORK HOOK` is the only allowed shared touch.
    - `ctest -R Gui_tests_run --output-on-failure` passes (objectName + reachable hide/restore assertions).
    - No "SolidWorks" identifier/string in the modified files.
  </acceptance_criteria>
  <done>FreeWorks mode mounts the ribbon in the top area and hides stock menu bar/toolbars; deactivation restores them; the ribbon carries objectName Fw_Ribbon; headless tests green; no MainWindow.cpp edit.</done>
</task>

<task type="auto">
  <name>Task 2: Discoverability escape hatch (D-12) + layout persistence round-trip (D-14)</name>
  <files>src/Gui/FreeWorks/FwRibbon.h, src/Gui/FreeWorks/FwRibbon.cpp, tests/src/Gui/FwRibbon.cpp</files>
  <read_first>
    - src/Gui/FreeWorks/FwRibbon.cpp (Plan 02 build; add overflow button + ensure Fw_Ribbon objectName persistence)
    - src/Gui/Command.h lines 988-1005 (`getModuleCommands`, `getAllCommands`, `getGroupCommands`) — overflow menu source
    - src/Gui/DockWindowManager.h lines 113-114 (`saveState`/`loadState`); src/Gui/MainWindow.h line 74 (QMainWindow saveState/restoreState)
    - .planning/phases/02-commandmanager-ribbon/02-RESEARCH.md § "Discoverability Escape Hatch" (layered: shortcuts baseline + "More commands…" overflow button) + § "Don't Hand-Roll" (persistence row) + Pitfall 5 (stranded commands)
    - .planning/phases/02-commandmanager-ribbon/02-PATTERNS.md § "Unique objectName for persistence" + § "Command firing / metadata"
    - .planning/phases/02-commandmanager-ribbon/02-UI-SPEC.md § Copywriting ("More commands…" label) + § Spacing (overflow button is a small/secondary 16px icon)
    - .planning/phases/02-commandmanager-ribbon/02-VALIDATION.md § Manual-Only (true-restart persistence is manual; in-process round-trip is headless)
  </read_first>
  <action>
    Add the D-12 escape hatch to `FwRibbon`: a single pinned "More commands…" overflow `QToolButton` at the ribbon's right edge whose `QMenu` is built (lazily on show) from `Gui::Application::Instance->commandManager().getModuleCommands(activeModule)` / `getAllCommands()` grouped by module, each entry being the command's existing `QAction` (thin trigger — no new backend). Document in a comment that the baseline guarantee is that `QAction` shortcuts survive `menuBar()->hide()` (Pitfall 5 minimum). Label exactly "More commands…" (UI-SPEC copy); use a small/secondary icon per UI-SPEC spacing. Ensure the ribbon (`Fw_Ribbon`) and each persisted panel (`Fw_RibbonPanel_*` from Plan 02) have unique stable objectNames so `QMainWindow::saveState()/restoreState()` round-trips (Pitfall 4 / D-14); reuse FreeCAD's existing `DockWindowManager`/`QMainWindow` state save-restore rather than a bespoke format (Don't Hand-Roll). Add a headless GTest: build a ribbon, capture `QMainWindow`-style state (or the relevant `saveState` blob), restore into a fresh window/state, and assert the `Fw_Ribbon` objectName is present after restore (in-process round-trip — true app-restart persistence is the VALIDATION manual-checklist item). No "SolidWorks" string (leak-grep).
  </action>
  <verify>
    <automated>ctest -R Gui_tests_run --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/Gui/FreeWorks/FwRibbon.cpp` contains the literal "More commands…" and one of `getModuleCommands`/`getAllCommands`.
    - `src/Gui/FreeWorks/FwRibbon.cpp` sets `objectName` to `Fw_Ribbon` and persisted panels keep `Fw_RibbonPanel_` objectNames.
    - `grep -c 'setStyleSheet' src/Gui/FreeWorks/FwRibbon.cpp` returns 0.
    - `ctest -R Gui_tests_run --output-on-failure` passes, including the in-process save/restore round-trip asserting `Fw_Ribbon` survives.
    - No "SolidWorks" identifier/string in the modified files.
  </acceptance_criteria>
  <done>The ribbon has a working "More commands…" overflow reaching uncurated commands, keyboard shortcuts remain live with chrome hidden (no stranded commands), and the layout round-trips via QMainWindow state keyed by unique Fw_* objectNames; headless persistence test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FreeWorks workbench → shared MainWindow chrome | Hides/restores menu bar + toolbars via public Gui singletons; scoped to FreeWorks activation/deactivation; no MainWindow.cpp edit. |
| overflow menu → CommandManager | Overflow entries reuse existing `QAction`s; thin triggers; no new backend. |
| ribbon widget → QMainWindow persisted state | State keyed by unique `Fw_*` objectNames; Qt-native save/restore, no bespoke IO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service (usability) | Chrome hidden but commands stranded (D-11×D-06) | mitigate | D-12 escape hatch: keyboard shortcuts survive `menuBar()->hide()` (baseline) + "More commands…" overflow built from `getAllCommands` (Pitfall 5) |
| T-02-06 | Tampering | Non-reversible chrome hiding leaking into other workbenches | mitigate | Hide on activate, restore on deactivate via `RestoreDefault` + `menuBar()->show()`; FreeWorks-scoped (Pitfall 3); headless toggle assertion |
| T-02-03 | Info Disclosure | FwLayout/FwWorkbench ↔ App separation | mitigate | Operate via public Gui singletons (`ToolBarManager`, `getMainWindow`); no App includes; no MainWindow.cpp edit |
| T-02-02 | Tampering | Shared-file edit to host the ribbon | mitigate | Mount via `ToolBarAreaWidget`/`DockWindowManager` from FwLayout; any unavoidable shared touch gets `// SW-FORK HOOK` (none expected) |
| T-02-07 | Tampering | Persistence round-trip silently failing | mitigate | Unique stable `Fw_*` objectNames (Pitfall 4); reuse Qt-native `saveState/restoreState`; headless round-trip test |
| T-02-DC | Tampering | Overflow menu firing a destructive command | accept | Overflow entries are the commands' existing `QAction`s; any destructive command keeps its existing downstream confirmation — no new unconfirmed path |
</threat_model>

<verification>
- `ctest -R Gui_tests_run --output-on-failure` green (objectName mount, in-process persistence round-trip, reachable chrome hide/restore).
- Ribbon mounts in `TopToolBarArea`; chrome hidden in FreeWorks mode, restored on deactivation (Pitfall 3).
- "More commands…" overflow + live shortcuts ensure no stranded commands (Pitfall 5 / D-12).
- No `MainWindow.cpp` edit; no `setStyleSheet`/hex; no "SolidWorks" string.
- Manual-checklist items (true-restart persistence, live menu-bar round-trip, flyout popup) tracked per VALIDATION.md.
</verification>

<success_criteria>
- RIBBON-01 SC1: the ribbon replaces FreeCAD's menus+toolbars in FreeWorks mode, reversibly.
- RIBBON-01 SC5: tab/panel layout persists across restart; the ribbon is a self-contained widget that survives an upstream sync (additive module, no shared edits).
- D-11 reversible chrome hiding; D-12 no stranded commands; D-14 layout persistence via Qt-native state.
</success_criteria>

<output>
Create `.planning/phases/02-commandmanager-ribbon/02-03-SUMMARY.md` when done. Record: the mount seam used (FwLayout direct, per RESEARCH recommendation), the chrome hide/restore call sequence, the escape-hatch mechanism chosen ("More commands…" overflow + shortcuts), and the persistence objectName scheme.
</output>
