---
phase: 03-featuremanager-design-tree
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/Gui/FreeWorks/FwFeatureTree.h
  - src/Gui/FreeWorks/FwFeatureTree.cpp
  - src/Gui/FreeWorks/FwFeatureTreeDelegate.h
  - src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp
  - src/Gui/FreeWorks/FwLayout.cpp
  - src/Gui/FreeWorks/CMakeLists.txt
  - tests/src/Gui/FwFeatureTreeWidget.cpp
  - tests/src/Gui/FwFeatureTree.cpp
autonomous: true
requirements:
  - TREE-01
  - TREE-04
  - TREE-03  # F2 rename half ONLY — double-click-to-PropertyManager is Phase 4

must_haves:
  truths:
    - "The left Fw_FeatureManager dock shows the active Body's FeatureManager tree: the Phase-1 placeholder in FwLayout.cpp is REPLACED by a mounted FwFeatureTree via manager->registerDockWindow(\"Fw_FeatureManager\", tree), following the Phase-2 mountRibbon discipline (unique_ptr-until-adopt, idempotent find-or-reuse by objectName, observe-the-DOM only) — the dock objectName stays \"Fw_FeatureManager\" so saveState round-trips (TREE-01, D-01)"
    - "Tree structure reads as a SolidWorks FeatureManager with ZERO new structure code: the Origin node renders FIRST with its 3 planes + 3 axes + point (free via ViewProviderOriginGroupExtension::constructChildren), features render in creation order, and the profile sketch nests under its consuming feature (free via ViewProviderSketchBased::claimChildren) — the tree only observes the DOM these ViewProviders emit (TREE-01, D-07)"
    - "The active-Body scoping shows ONLY the active PartDesign::Body's subtree by HIDING the non-active Body subtrees via the 03-01 scopeToActiveBody(), which recurses from invisibleRootItem() down through the per-document Gui::DocumentItem (Bodies are CHILDREN of the DocumentItem, NOT top-level items — Tree.h:348, Tree.cpp:4562) and calls QTreeWidgetItem::setHidden(true) on the non-active Body's child subtree — the same API the base TreeWidget uses (Tree.cpp:4574/6070) — keeping the ancestor DocumentItem visible; scoping is driven by Gui signals (signalActivatedObject/signalInEdit/signalActiveDocument) + the \"PartDesign::Body\" type-name literal, with NO #include <Mod/PartDesign/...> and NO PartDesign link, and NEVER a Visibility/property write (D-07, RESEARCH Pitfall 1, D-06). When no Body is active the tree shows the 'No active Body' empty state (UI-SPEC Copywriting)"
    - "The three origin planes display under SolidWorks names via a QStyledItemDelegate that remaps DISPLAY TEXT ONLY (object Label unchanged): XY_Plane→'Front Plane', XZ_Plane→'Top Plane', YZ_Plane→'Right Plane'. The delegate obtains the underlying plane object + its PlaneRoles name through the STOCK tree item data path (the QModelIndex → the item's Gui::ViewProviderDocumentObject/App::DocumentObject via the public DocumentObjectItem::object() accessor, NOT a fabricated model and NOT DocumentObjectItem internals), keyed on the internal PlaneRoles name (Datums.h:204) — these tr() strings contain no 'SolidWorks' token and clear the leak-grep (D-08, RESEARCH A2 resolved; reviewer plane-delegate-access concern)"
    - "F2 inline rename works in the hosted tree with NO re-implementation: the inherited Gui::TreeWidget relabel action (Key_F2 / Key_Return on macOS → editItem → DocumentObjectItem::setData → transactioned Label.setValue, Tree.cpp:6536-6556) is active on the mounted FwFeatureTree (D-13, the F2 half of TREE-03)"
    - "Drag-to-reorder reuses the existing dependency-aware machinery verbatim by calling Gui::TreeWidget::dragMoveEvent(event) FIRST (which runs the full canDropObjectEx target validation and sets the accept/ignore state) and only DECORATING the already-ignored case with Qt::ForbiddenCursor + suppressing the insertion line — the subclass NEVER reimplements target validation. A VALID reorder commits through the inherited dropEvent/sortDroppedObjects Group PropertyLinkList in ONE transaction (clean undo); an INVALID reorder (child before parent) the base already ignored gets the no-line + forbidden-cursor affordance with NO transaction — allow-and-flag is never taken (TREE-04, D-10/D-11/D-12; reviewer DnD-override concern)"
    - "Below-tip greying (when present) and all color flow through the inherited QPalette / native QStyle ONLY — NO setStyleSheet, NO hex color, NEVER a Visibility/property write for presentation (D-06, UI-SPEC § Color, Pitfall 3). The 22px row height + 16px indent metrics are pinned; SW icon art / final colors are Phase 7"
  artifacts:
    - path: "src/Gui/FreeWorks/FwFeatureTreeDelegate.h"
      provides: "FwFeatureTreeDelegate : QStyledItemDelegate — plane display-name remap (Front/Top/Right) + below-tip greying paint hook (Gui-only)"
      contains: "class FwFeatureTreeDelegate"
    - path: "src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp"
      provides: "Delegate impl: XY/XZ/YZ → Front/Top/Right display-text remap keyed on PlaneRoles name resolved through the stock item-object data path; greying via QPalette::Disabled Text role"
      contains: "Front Plane"
    - path: "src/Gui/FreeWorks/FwFeatureTree.cpp"
      provides: "Full scoping (active Body subtree via scopeToActiveBody/recursive setHidden over the DocumentItem→Body topology), delegate install (setItemDelegate), F2-action confirmation, DnD validity wiring (base dragMoveEvent first, then forbidden cursor + no insertion line on the ignored case)"
      contains: "setItemDelegate"
    - path: "src/Gui/FreeWorks/FwLayout.cpp"
      provides: "Mount: replace the Fw_FeatureManager placeholder with a mounted FwFeatureTree (find-or-reuse, unique_ptr-until-adopt)"
      contains: "Fw_FeatureManager"
    - path: "tests/src/Gui/FwFeatureTreeWidget.cpp"
      provides: "QTEST: structure (Origin-first/creation-order/nested), delegate plane-label remap renders via the stock item path, F2 action present after mount, scoping recursive-setHidden render (non-active Body subtree hidden)"
      contains: "Front Plane"
  key_links:
    - from: "src/Gui/FreeWorks/FwLayout.cpp"
      to: "src/Gui/FreeWorks/FwFeatureTree.h"
      via: "registerDockWindow(\"Fw_FeatureManager\", new FwFeatureTree(\"FwFeatureManager\", parent)) replacing the placeholder"
      pattern: "FwFeatureTree"
    - from: "src/Gui/FreeWorks/FwFeatureTree.cpp"
      to: "src/Gui/FreeWorks/FwFeatureTreeDelegate.h"
      via: "setItemDelegate(new FwFeatureTreeDelegate(this))"
      pattern: "setItemDelegate"
    - from: "src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp"
      to: "App::Origin PlaneRoles"
      via: "remap display text keyed on the internal PlaneRoles name resolved through the stock tree item-object accessor (Datums.h:204)"
      pattern: "Plane"
---

<objective>
Deliver the first user-visible vertical slice of the FeatureManager: a SolidWorks user opens the left dock and SEES their active Body's design tree — Origin/planes node on top (planes labelled Front/Top/Right), features in creation order, the profile sketch nested under its feature — and can press F2 to rename and drag a feature up/down to reorder history with the existing validity feedback (invalid reorders BLOCKED). This replaces the Phase-1 `Fw_FeatureManager` placeholder with a mounted `FwFeatureTree`, installs the `FwFeatureTreeDelegate` plane-name remap, scopes the tree to the active `PartDesign::Body` via the 03-01 `scopeToActiveBody()` recursive-setHidden render (descends the `DocumentItem`→`Body` topology) driven by Gui signals + the type-name literal, and confirms the inherited F2 + DnD machinery is live (CONTEXT D-01/D-02/D-07/D-08/D-10/D-11/D-12/D-13).

Purpose: This is the "looks and behaves like a SolidWorks FeatureManager" slice (TREE-01 structure + TREE-04 reorder + the F2 half of TREE-03). The rollback bar (TREE-02) is the next slice (Plan 03-03) — but after this plan a real user can already see, rename, and reorder, which they could not after Plan 03-01.
Output: `FwFeatureTreeDelegate.{h,cpp}` (NEW), the full `FwFeatureTree` scoping/delegate-install/DnD-wiring (extends the 03-01 spike surface), the `FwLayout.cpp` mount swap, CMake additions (additive), and the QTEST structure/remap/F2 assertions authored to pass under CI.

> **Engine gate:** This plan assumes `03-01/SPIKE.md` recorded `reuse committed` (scoping rendered via `scopeToActiveBody()`/recursive setHidden over the DocumentItem→Body topology). If it recorded `subclass-fallback committed`, the same deliverables stand but `FwFeatureTree` is the CONCRETE projection-tree fallback (builds its own QTreeWidgetItems from the link-free `Group`, routes reorder/rename/Tip through the same reused command paths) per the SPIKE.md FAIL-branch — still reusing DnD/rename/transaction machinery, never editing `Tree.cpp`. ("Override more virtuals" is NOT the fallback; the item factory is non-virtual.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-featuremanager-design-tree/03-CONTEXT.md
@.planning/phases/03-featuremanager-design-tree/03-RESEARCH.md
@.planning/phases/03-featuremanager-design-tree/03-PATTERNS.md
@.planning/phases/03-featuremanager-design-tree/03-UI-SPEC.md
@.planning/phases/03-featuremanager-design-tree/03-VALIDATION.md
@.planning/phases/03-featuremanager-design-tree/SPIKE.md
@.planning/phases/03-featuremanager-design-tree/03-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- **This plan (03-02):** class `FwFeatureTreeDelegate` (namespace `FreeWorksGui`, `src/Gui/FreeWorks/FwFeatureTreeDelegate.{h,cpp}`) — a `QStyledItemDelegate` remapping the three origin-plane display names (Front/Top/Right) and providing the below-tip greying paint hook (Gui-only). Extends `FwFeatureTree` with: full active-Body subtree scoping (the 03-01 `scopeToActiveBody()` recursive-setHidden render over the `DocumentItem`→`Body` topology + Gui signals + `"PartDesign::Body"` literal + the 'No active Body' empty state), `setItemDelegate(...)` install, F2-action confirmation, and the DnD validity affordance (call base `dragMoveEvent` first; decorate the ignored case with forbidden cursor + no insertion line). Modifies `FwLayout.cpp` to replace the `Fw_FeatureManager` placeholder with a mounted `FwFeatureTree` (find-or-reuse, unique_ptr-until-adopt). CMake append of the delegate sources.
- **Plan 03-03 (later):** `FwRollbackBar` mechanism (the `drawRow` band + grab zone + drag→`Body.Tip` via the `"PartDesign_MoveTip"` command-ID fired under an RAII `FwSelectionGuard`, NO outer FreeWorks transaction), reversibility, insert-at-bar via the Python-only `Body.insertObject` doCommand with an explicit post-insert Tip policy, below-tip greying live-driven by Tip, and `SPIKE_LIVE_CHECKLIST.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FwFeatureTreeDelegate — Front/Top/Right plane display-name remap (display-text only, role resolved through the stock item path, Gui-only)</name>
  <files>src/Gui/FreeWorks/FwFeatureTreeDelegate.h, src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp, src/Gui/FreeWorks/CMakeLists.txt, tests/src/Gui/FwFeatureTreeWidget.cpp</files>
  <read_first>
    - src/Gui/FreeWorks/FwRibbon.h lines 1-23 (the SPDX + LGPL-2.1 copyright block to copy verbatim into the new delegate files)
    - src/Gui/Tree.h line 50 (`Gui::TreeWidgetItemDelegate` — the existing tree delegate, the role analog for a `QStyledItemDelegate` over tree rows) and line 495 (the public `DocumentObjectItem::object()` accessor — the stock path the delegate uses to reach the underlying object from a QModelIndex; the item is a `DocumentObjectItem` whose `object()` exposes the `Gui::ViewProviderDocumentObject` → `App::DocumentObject` — read it through the public item-object accessor, never `DocumentObjectItem` internals)
    - src/App/Datums.h lines 202-206 (`PlaneRoles`/`AxisRoles`/`PointRoles` — the internal role names the remap keys on: XY_Plane/XZ_Plane/YZ_Plane), and src/App/Datums.cpp lines 270-272 (the FreeCAD plane labels "XY-plane"/"XZ-plane"/"YZ-plane" the remap replaces in display text)
    - .planning/phases/03-featuremanager-design-tree/03-PATTERNS.md § "FwFeatureTreeDelegate" (the SW↔FreeCAD remap table; display-text-only rule; greying via QPalette::Disabled, never Visibility; no hex/setStyleSheet — references the 02-02-SUMMARY deviation #2 where a setStyleSheet comment alone tripped the acceptance grep)
    - .planning/phases/03-featuremanager-design-tree/03-UI-SPEC.md § Copywriting (the exact plane names "Front Plane"/"Top Plane"/"Right Plane"; the A1 correspondence flagged for the parity-user check) + § Color (palette roles only) + § Typography (9pt regular; tip row Semibold 600)
    - src/Gui/FreeWorks/CMakeLists.txt lines 17-37,48 (the additive source lists)
    - tests/src/Gui/FwRibbonWidget.cpp (QTEST analog for asserting rendered display text)
  </read_first>
  <behavior>
    - A `FwFeatureTreeDelegate : QStyledItemDelegate` remaps DISPLAY TEXT ONLY: an origin-plane item whose underlying object's `PlaneRoles` name is `XY_Plane` renders as "Front Plane", `XZ_Plane` as "Top Plane", `YZ_Plane` as "Right Plane"; the underlying `obj->Label` is NEVER written (display-only).
    - The role name is resolved through the STOCK tree item data path: from the painted `QModelIndex`, reach the inherited tree item and its public `DocumentObjectItem::object()`/ViewProvider→`App::DocumentObject` accessor (Tree.h:495), then read the plane's role-name string from that object — NOT a fabricated model, NOT `DocumentObjectItem` private internals (reviewer plane-delegate-access concern). If the role cannot be resolved through the stock path for a given index, the delegate falls back to the normal `Label` text (pass-through).
    - A non-plane row (feature/sketch/axis/point) renders its normal `Label` text unchanged (the delegate is a pass-through for everything except the three planes).
    - The below-tip greying paint hook uses the palette's disabled text role (`palette().color(QPalette::Disabled, QPalette::Text)` / `Qt::ForegroundRole`) — NO hex, NO `setStyleSheet`, NEVER a `Visibility`/property write (the actual below-tip set is driven by the rollback bar in Plan 03-03; this plan provides the hook + ensures it is palette-driven).
    - QTEST: a tree populated with an Origin's three planes (built so the item-object accessor resolves the real plane objects, not a stub model) renders the display strings "Front Plane"/"Top Plane"/"Right Plane" (not "XY-plane" etc.), and the underlying objects' `Label` values are unchanged after rendering.
  </behavior>
  <action>
    Create `src/Gui/FreeWorks/FwFeatureTreeDelegate.h`/`.cpp`, namespace `FreeWorksGui`, copying the `FwRibbon.h` license/copyright/pragma block verbatim. Declare `class FwFeatureTreeDelegate : public QStyledItemDelegate { Q_OBJECT public: explicit FwFeatureTreeDelegate(QObject* parent = nullptr); ... };`. Override the display-text path (e.g. `initStyleOption` or `displayText`) to remap the three origin planes keyed on the internal `PlaneRoles` name (`XY_Plane`→tr("Front Plane"), `XZ_Plane`→tr("Top Plane"), `YZ_Plane`→tr("Right Plane")). Resolve the underlying plane object + its role-name string through the STOCK tree item-object data path: from the `QModelIndex`, obtain the inherited tree item (`itemFromIndex` on the tree, or the item carried in the option) and read its public `DocumentObjectItem::object()`/ViewProvider→`App::DocumentObject` accessor (Tree.h:495), then key on the plane's role-name string via the generic `App::DocumentObject` accessor — WITHOUT a PartDesign/App-mutation include and WITHOUT touching `DocumentObjectItem` internals. The remap is DISPLAY-ONLY: never call `Label.setValue`. Add the greying paint hook: when the item is flagged below-tip (a Gui-only item flag / role the rollback bar will set in Plan 03-03), paint the text with `palette().color(QPalette::Disabled, QPalette::Text)` — provide the hook now, palette-driven, no hex, no `setStyleSheet`. Keep the tip-row Semibold (600) weight affordance per UI-SPEC Typography (palette/font role, not color). Append `FwFeatureTreeDelegate.cpp`/`.h` to `FreeWorks_CPP_SRCS`/`FreeWorks_HPP_SRCS`. Extend `tests/src/Gui/FwFeatureTreeWidget.cpp` with a QTEST slot that builds an Origin's three real planes in the tree (so the item-object accessor resolves them through the stock path) and `QCOMPARE`s the rendered display text to "Front Plane"/"Top Plane"/"Right Plane" and asserts the underlying `Label`s are unchanged. No "SolidWorks" string anywhere (these plane names contain no such token — leak-grep clean).
  </action>
  <verify>
    <automated>ctest -R "FwFeatureTreeWidget_Tests_run" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/Gui/FreeWorks/FwFeatureTreeDelegate.h` contains `class FwFeatureTreeDelegate` and `QStyledItemDelegate`.
    - `src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp` contains `Front Plane`, `Top Plane`, `Right Plane` and `QPalette::Disabled`, and resolves the object through the stock item-object accessor (references `object()`/`itemFromIndex` / the ViewProvider→object path, NOT a fabricated model).
    - `grep -c 'setStyleSheet' src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp` returns 0 AND no hex color literal is present (`grep -ciE '#[0-9a-f]{6}' src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp` returns 0).
    - `grep -c 'Label.setValue\|setLabel' src/Gui/FreeWorks/FwFeatureTreeDelegate.cpp` returns 0 (display-text remap never mutates the object — D-08).
    - `grep -v '^#' src/Gui/FreeWorks/CMakeLists.txt | grep -c 'FwFeatureTreeDelegate.cpp'` returns >= 1.
    - `ctest -R "FwFeatureTreeWidget_Tests_run" --output-on-failure`: authored, pending CI — turns green once a CI/build-tree run shows the three planes render Front/Top/Right (via the stock item path) and the underlying Labels unchanged.
    - `bash tools/fw-string-leak-grep.sh` clean.
  </acceptance_criteria>
  <done>A QStyledItemDelegate remaps the three origin planes to Front/Top/Right display names (display-only, object Labels untouched, role resolved through the stock tree item-object path via DocumentObjectItem::object()) and provides a palette-driven below-tip greying hook; the remap is authored to render correctly headless and clears the leak-grep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount FwFeatureTree in Fw_FeatureManager + active-Body scoping (scopeToActiveBody/recursive setHidden over DocumentItem→Body) + delegate install + F2 + DnD validity affordance (base dragMoveEvent first)</name>
  <files>src/Gui/FreeWorks/FwFeatureTree.h, src/Gui/FreeWorks/FwFeatureTree.cpp, src/Gui/FreeWorks/FwLayout.cpp, tests/src/Gui/FwFeatureTreeWidget.cpp, tests/src/Gui/FwFeatureTree.cpp</files>
  <read_first>
    - src/Gui/FreeWorks/FwLayout.cpp lines 95-103 (`ensureDock`/`registerDockWindow(name, widget)` — the placeholder registration to REPLACE for "Fw_FeatureManager"), 124-127 (the current `Fw_FeatureManager` placeholder call to swap), 138-235 (`mountRibbon()` — the swap discipline to mirror: WR-02 unique_ptr-until-adopt at 160-164, idempotent find-or-reuse by objectName at 149-153,182-198, observe-the-DOM via the singleton at 109-114,140-147)
    - src/Gui/Tree.h lines 60-65 (TreeWidget ctor), 147 (drawRow), 150-161 (the DnD overrides `startDrag`/`dropMimeData`/`dragMoveEvent`/`dropEvent` reused as-is), 82 (eventFilter + the inherited F2 path), 348 (`class DocumentItem: public QTreeWidgetItem` — the per-document top-level item scopeToActiveBody descends through), 495 (the public `DocumentObjectItem::object()` accessor the scope render resolves items with)
    - src/Gui/Tree.cpp lines 2348-2458 (`TreeWidget::dragMoveEvent` — it ALREADY calls `QTreeWidget::dragMoveEvent`, adjusts target info, and rejects invalid drops via `canDropObjectEx`; the subclass calls THIS first and only decorates the ignored case), 2546-2549 & 3162 (`dropEvent` transactioned mutation), 607-614 (the `relabelObjectAction` bound to `Key_F2`/`Key_Return` — confirm it is installed on the inherited tree), 3260-3273 (`sortDroppedObjects` Group reorder), 6536-6556 (the transactioned `Label.setValue` rename commit), 4562 (`parent->addChild(item)` — proves a Body item is a CHILD of the DocumentItem, so scopeToActiveBody descends rather than iterating topLevelItem(i)), 4574/6070 (`item->setHidden(...)` — the scoping render API reused by scopeToActiveBody)
    - src/Gui/ViewProvider.h lines 444-493 (`canDragObjectToTarget`/`canDropObjectEx` — the gates the BASE dragMoveEvent already consults; the subclass does NOT re-call them, it reads the base's accept/ignore result)
    - src/Gui/Application.h lines 130,142,154 (`signalActiveDocument`/`signalActivatedObject`/`signalInEdit` — connect for active-Body scoping; observe-the-DOM)
    - .planning/phases/03-featuremanager-design-tree/03-PATTERNS.md § "src/Gui/FreeWorks/FwLayout.cpp" (the mount swap discipline) + § "src/Gui/FreeWorks/FwFeatureTree.{h,cpp}" (the reusable virtual seams; the scoping snippet)
    - .planning/phases/03-featuremanager-design-tree/03-UI-SPEC.md § Interaction State Contract (selected/hover/below-tip rows; drag-to-reorder valid = 2px insertion line, invalid = NO line + ForbiddenCursor + event->ignore()) + § Spacing (setIndentation(16); 22px row) + § Copywriting (the 'No active Body' empty state + the blocked-reorder tooltip)
    - .planning/phases/03-featuremanager-design-tree/SPIKE.md (the engine verdict: thin subclass vs the projection-tree fallback — apply the recorded branch)
  </read_first>
  <behavior>
    - Mount: `FwLayout` replaces the `Fw_FeatureManager` placeholder with a mounted `FwFeatureTree` via `manager->registerDockWindow("Fw_FeatureManager", tree)` — using the `mountRibbon()` discipline (build into a `unique_ptr`, `release()` at the adopt site; find-or-reuse by the `Fw_FeatureManager` objectName so re-activation never adds a second tree; reach the manager only through `Gui::DockWindowManager::instance()`, never edit `MainWindow.cpp`). The dock objectName stays `Fw_FeatureManager` so `saveState`/`restoreState` round-trips.
    - Scoping: connecting `signalActivatedObject`/`signalInEdit`/`signalActiveDocument`, the tree calls the 03-01 `scopeToActiveBody()` which resolves the active `PartDesign::Body` (type-name literal) and, recursing from `invisibleRootItem()` down through the per-document `Gui::DocumentItem`, HIDES the NON-active Body subtrees via `QTreeWidgetItem::setHidden(true)` (Gui-only render, no property write) while keeping the ancestor `DocumentItem` visible — Bodies are children of the DocumentItem, NOT top-level items (Tree.h:348, Tree.cpp:4562). With no active Body, the tree shows the 'No active Body' empty state (UI-SPEC Copywriting). The Origin-first / creation-order / nested-sketch structure (free via the ViewProviders) survives the scoping.
    - Delegate: the tree installs `FwFeatureTreeDelegate` via `setItemDelegate(...)` and sets `setIndentation(16)` (UI-SPEC indent metric).
    - F2: the inherited relabel action (`Key_F2` / `Key_Return` on macOS) is confirmed active on the mounted tree — rename routes through the existing transactioned `Label.setValue` with NO re-implementation (D-13).
    - DnD validity (reviewer DnD-override concern): the subclass override `dragMoveEvent(event)` calls `Gui::TreeWidget::dragMoveEvent(event)` FIRST (which runs the full inherited target validation via `canDropObjectEx` and sets event accept/ignore). It then ONLY decorates the result: if the base left the event ignored (invalid target), set `Qt::ForbiddenCursor` and suppress the insertion line; if accepted, leave the inherited 2px insertion line and let the inherited `dropEvent`/`sortDroppedObjects` commit the Group reorder in its existing single transaction. The subclass NEVER reimplements target validation or reaches into private target info — allow-and-flag is never taken (D-10/D-11/D-12).
    - QTEST: after mount, the tree is-a `Gui::TreeWidget`, has the delegate installed (`itemDelegate()` is an `FwFeatureTreeDelegate`), `indentation()==16`, the F2 action is present, the empty state shows when no Body is active, and scopeToActiveBody hides the non-active Body's subtree (`isHidden()` true on the non-active Body item, resolved via the DocumentItem→Body descent + `object()`) while keeping the active body + ancestor DocumentItem visible. GTest (logic, extend 03-01): the BLOCK gate returns false for child-before-parent (already asserted in 03-01 — extend with the affordance contract that no transaction opens on BLOCK).
  </behavior>
  <action>
    In `src/Gui/FreeWorks/FwLayout.cpp`, replace the `Fw_FeatureManager` placeholder: instead of `ensureDock(... "Fw_FeatureManager" ...)` registering a placeholder, register a mounted `FwFeatureTree`. Mirror `mountRibbon()` exactly — build the tree into a `std::make_unique<FwFeatureTree>("FwFeatureManager", parent)`, find-or-reuse by the `Fw_FeatureManager` registered-dock lookup (`findRegisteredDockWindow`) so re-install never duplicates, `release()` the unique_ptr only at the `registerDockWindow` adopt site, and reach the manager via `Gui::DockWindowManager::instance()` only (no `MainWindow.cpp` edit). Keep the other two placeholders (`Fw_PropertyManager`, `Fw_TaskPane`) untouched. In `FwFeatureTree`, complete the scoping: connect the three Gui signals so they call `scopeToActiveBody()` (the 03-01 recursive-setHidden render that descends from `invisibleRootItem()` through the `DocumentItem` and hides the non-active Body subtrees; never a `Visibility` write, never `topLevelItem(i)`-treats-Body-as-top-level); render the 'No active Body' empty state when none is active. Install the delegate (`setItemDelegate(new FwFeatureTreeDelegate(this))`) and `setIndentation(16)`. Confirm the inherited F2 relabel action is active (it is installed by the base `TreeWidget`; assert presence, do not re-add). Wire the DnD validity affordance by OVERRIDING `dragMoveEvent(QDragMoveEvent* event)`: call `Gui::TreeWidget::dragMoveEvent(event)` FIRST to run the inherited `canDropObjectEx` validation + accept/ignore decision, THEN inspect `event->isAccepted()` — if the base ignored it, `setCursor(Qt::ForbiddenCursor)` and suppress the insertion-line drop indicator (no transaction); if accepted, leave the inherited behavior untouched so `dropEvent`/`sortDroppedObjects` commits the Group reorder in its existing single transaction. Do NOT reimplement target validation or read private target info. If `SPIKE.md` recorded `subclass-fallback committed`, `FwFeatureTree` is the projection-tree fallback (own QTreeWidgetItems from the link-free `Group`) — still NO `Tree.cpp` edit, still reusing DnD/rename/transaction. Use ONLY palette/`QStyle` for color (no `setStyleSheet`, no hex). NO `#include <Mod/PartDesign/...>`, NO PartDesign link. Extend the QTEST (`FwFeatureTreeWidget.cpp`) with: delegate-installed assertion, `indentation()==16`, empty-state-when-no-Body, F2-action-present-after-mount, scopeToActiveBody hides the non-active Body subtree (assert via the DocumentItem→Body descent + `object()`/`isHidden()`, not synthetic top-level items); extend the GTest (`FwFeatureTree.cpp`) confirming no transaction opens on a BLOCKed drop. No "SolidWorks" identifier/string (leak-grep).
  </action>
  <verify>
    <automated>ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/Gui/FreeWorks/FwLayout.cpp` registers an `FwFeatureTree` under `Fw_FeatureManager` (`grep -c 'FwFeatureTree' src/Gui/FreeWorks/FwLayout.cpp` >= 1) and the swap is idempotent (uses `findRegisteredDockWindow` find-or-reuse, mirrors `mountRibbon`).
    - `src/Gui/FreeWorks/FwFeatureTree.cpp` contains `setItemDelegate`, `setIndentation(16)`, the `"PartDesign::Body"` literal, `scopeToActiveBody`, `setHidden(`, `invisibleRootItem`, `object()` (the real DocumentItem→Body descent), at least one of the three Gui signal connections, a `dragMoveEvent` that calls `Gui::TreeWidget::dragMoveEvent`, `Qt::ForbiddenCursor`, and reads `event->isAccepted()` (the base-first DnD decoration).
    - `grep -c 'Gui::TreeWidget::dragMoveEvent' src/Gui/FreeWorks/FwFeatureTree.cpp` returns >= 1 (base validation runs first; reviewer DnD-override concern) AND `grep -cE 'canDropObjectEx|canDragObjectToTarget' src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0 (the subclass does NOT re-call the gates).
    - `grep -c '#include <Mod/PartDesign' src/Gui/FreeWorks/FwFeatureTree.cpp src/Gui/FreeWorks/FwLayout.cpp` returns 0 AND `grep -c 'PartDesignGui' src/Gui/FreeWorks/CMakeLists.txt` returns 0 (no module include/link).
    - `grep -c 'Visibility' src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0 (scoping/greying never writes Visibility — D-06/Pitfall 3).
    - `grep -c 'setStyleSheet' src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0.
    - `tests/src/Gui/FwFeatureTreeWidget.cpp` asserts the delegate is installed, `indentation()==16`, the F2 action is present, the 'No active Body' empty state, and scopeToActiveBody hides the non-active Body subtree (via the DocumentItem→Body descent + `object()`/`isHidden()`, not synthetic top-level items).
    - `ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure`: authored, pending CI — turns green once a CI/build-tree run executes structure/scoping/remap/F2/DnD-BLOCK; do NOT record green from authoring alone.
    - `bash tools/fw-string-leak-grep.sh` clean; no `MainWindow.cpp` edit (`git diff --name-only` shows none).
  </acceptance_criteria>
  <done>The left Fw_FeatureManager dock shows the active Body's SW-structured tree (Origin/Front-Top-Right planes on top, features in creation order, nested sketches), scoped via `scopeToActiveBody()`/recursive setHidden over the DocumentItem→Body topology driven by Gui signals + the type-name literal with no module link (Bodies hidden as child subtrees, not as top-level items), F2 rename and drag-reorder (valid commits in one transaction, invalid BLOCKED by the base `dragMoveEvent` then decorated with forbidden cursor) both work via the reused machinery, and the headless structure/remap/F2/DnD tests are authored to pass under CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FreeWorks Gui tree → App layer | The mounted tree observes the live document (SelectionObserver / ViewProvider signals) and resolves the active Body by a type-name literal; it never includes/mutates App headers and never links PartDesign. Scoping (recursive setHidden over the DocumentItem→Body topology) / greying (palette) are Gui-only, never a property write. |
| user drag/F2 input → document | Reorder and rename cross into the document only through the existing transactioned, dependency-gated machinery (the base `dragMoveEvent`→`canDropObjectEx` validation, `sortDroppedObjects`, `Label.setValue`) — the subclass adds NO new validation or mutation path, it only decorates the base's ignore result. |
| FreeWorks mount → shared chrome | The dock swap goes through `Gui::DockWindowManager::instance()` only; `MainWindow.cpp` is never edited; `Tree.cpp` bodies are never edited (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Repudiation/Legal | FwFeatureTreeDelegate plane names, all new source | mitigate | SW plane display names are `tr()` text containing no "SolidWorks" token; `tools/fw-string-leak-grep.sh` confirms (RESEARCH A2 resolved); no bare "SolidWorks" identifier anywhere |
| T-03-08 | Tampering (fork isolation) | FwFeatureTree scoping, FwLayout mount, CMakeLists.txt | mitigate | NO `#include <Mod/PartDesign/...>`, NO PartDesign link; reference Body by type-name literal + Gui signals; additive `target_sources` only; no `MainWindow.cpp`/`Tree.cpp` edit (D-02, Pitfall 1) |
| T-03-09 | Tampering (presentation leak into .FCStd) | scoping (recursive setHidden over DocumentItem→Body) / greying | mitigate | Scoping descends the real DocumentItem→Body topology and hides the non-active Body's child subtree via the public `QTreeWidgetItem::setHidden` API (keeping the ancestor DocumentItem visible); greying via `QPalette::Disabled` text role — NEVER `Visibility.setValue` or any property write for presentation (D-06, Pitfall 3); verified by the `grep -c Visibility == 0` gate |
| T-03-10 | Tampering (invalid reorder corrupts model) | DnD validity affordance | mitigate | Invalid reorder is rejected by the inherited `dragMoveEvent`→`canDropObjectEx` (the subclass calls the base FIRST, then only decorates the ignored case with forbidden cursor; no transaction); valid reorder commits via Group `PropertyLinkList` in ONE transaction → clean undo (D-10/D-11/D-12) |
| T-03-11 | Tampering (rename input) | F2 inline rename | accept | Rename reuses the existing transactioned `Label.setValue` path verbatim (`Property::setValue` validates); no new input-handling code (D-13) — low risk |
| T-03-12 | DoS (duplicate mount on re-activation) | FwLayout mount swap | mitigate | Idempotent find-or-reuse by the `Fw_FeatureManager` objectName (mirrors `mountRibbon`); never adds a second tree |
</threat_model>

<verification>
- `ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure`: structure (Origin-first/creation-order/nested), plane-name remap renders via the stock item path, delegate installed, `indentation()==16`, F2-action-present, empty-state-when-no-Body, scopeToActiveBody recursive-setHidden render (non-active Body subtree hidden via the DocumentItem→Body descent) (QTEST); BLOCK gate + no-transaction-on-block (GTest). Status in this no-build-tree env: **authored, not run locally, pending CI** — only marked green after an actual CI/build-tree run.
- The `Fw_FeatureManager` placeholder is replaced by a mounted `FwFeatureTree` (idempotent, observe-the-DOM); dock objectName unchanged so layout round-trips.
- No PartDesign include/link in `src/Gui/FreeWorks/`; the DnD override calls the base `dragMoveEvent` first and never re-calls `canDropObjectEx`; no `Visibility`/property write for presentation; no `setStyleSheet`/hex; no `MainWindow.cpp`/`Tree.cpp` edit.
- `tools/fw-string-leak-grep.sh` clean (plane display names carry no "SolidWorks" token).
</verification>

<success_criteria>
- A SolidWorks user opens the left dock and sees the active Body's FeatureManager tree: Origin node first (planes labelled Front/Top/Right via the display-only delegate, role resolved through the stock item path), features in creation order, the profile sketch nested under its feature — all from observing the ViewProvider-emitted DOM, scoped to the active `PartDesign::Body` by `scopeToActiveBody()`/recursive setHidden over the DocumentItem→Body topology driven by Gui signals + the type-name literal (no module link), with a 'No active Body' empty state.
- F2 renames a feature through the reused transactioned `Label.setValue` path; dragging a feature reorders history (valid → inherited 2px insertion line + one-transaction commit + clean undo; invalid child-before-parent → the base `dragMoveEvent` ignores it, the subclass decorates with no line + forbidden cursor + no transaction).
- All presentation is Gui-only (palette/`QStyle`, item visibility via recursive setHidden over the DocumentItem→Body topology / flags) — no property write, no `setStyleSheet`/hex, no shared-file edit; the headless structure/remap/F2/DnD tests are authored to pass under CI and the leak-grep is clean.
</success_criteria>

<output>
Create `.planning/phases/03-featuremanager-design-tree/03-02-SUMMARY.md` when done. Record: the mount approach (placeholder→FwFeatureTree swap, idempotency), the scoping mechanism (signals + `scopeToActiveBody()`/recursive setHidden over the DocumentItem→Body topology + type-name literal + empty state), the delegate remap (Front/Top/Right, display-only, role via the stock item path), the F2-active confirmation, the DnD validity affordance (base `dragMoveEvent` first → forbidden cursor + no line + no transaction on the ignored case), and the A1 plane-correspondence obligation for the parity-user check.
</output>
</content>
</invoke>
