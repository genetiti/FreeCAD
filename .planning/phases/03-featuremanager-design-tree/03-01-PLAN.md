---
phase: 03-featuremanager-design-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/src/Gui/FwFeatureTree.cpp
  - tests/src/Gui/FwFeatureTreeWidget.cpp
  - tests/src/Gui/CMakeLists.txt
  - src/Gui/FreeWorks/FwFeatureTree.h
  - src/Gui/FreeWorks/FwFeatureTree.cpp
  - src/Gui/FreeWorks/CMakeLists.txt
  - .planning/phases/03-featuremanager-design-tree/SPIKE.md
autonomous: false
requirements:
  - TREE-01
  - TREE-02
  - TREE-04
user_setup: []

must_haves:
  truths:
    - "The Wave 0 test scaffold reuses the EXISTING tests/src/Gui/FwTestGuiBootstrap.h verbatim (no edit): tests::ensureGuiTestBootstrap() already creates the Gui::Application singleton then imports PartDesignGui/SketcherGui/MeasureGui/MatGui, so PartDesign::Body, the feature types, and the PartDesign_MoveTip command all resolve headlessly (RESEARCH Wave-0 / 03-PATTERNS § headless bootstrap)"
    - "Active-Body scoping resolves a Body purely by the type-name STRING literal \"PartDesign::Body\" (obj->getTypeId().getName() comparison) — there is NO #include <Mod/PartDesign/...> and NO target_link_libraries(... PartDesignGui) anywhere in src/Gui/FreeWorks/ (D-07, RESEARCH Pitfall 1: the clean PartDesignGui::getBody accessor is in the wrong layer for FreeWorks)"
    - "A GTest proves the rollback logic with the REAL engine: building a Body + solid features, setting Body.Tip to an earlier feature, makes Tip.isTouched() true and Body::mustExecute() return 1 (the true suppress-below trigger, D-04; Body.cpp:94-100). NO new persisted property is introduced (D-05) and NO Visibility/property write performs greying (D-06, Pitfall 3)"
    - "A GTest proves Body::insertObject(feature, target, after) places the feature in the Group without disturbing Tip (insert-at-bar, D-04; Body.cpp:275-321) and that a Group PropertyLinkList reorder inside one openTransaction/commitTransaction is undo-reversible (D-12)"
    - "A GTest proves the dependency-aware DnD gate (canDragObjectToTarget/canDropObjectEx) returns false for a child-before-parent reorder so the drop is BLOCKED (D-10/D-11; ViewProvider.h:450,483 with ViewProviderBody overrides) — allow-and-flag is never exercised"
    - "FwFeatureTree is a thin subclass of Gui::TreeWidget (NOT a from-scratch tree, NOT an edit to Tree.cpp bodies): the public TreeWidget(const char* name, QWidget* parent) ctor is reused and the only overridable presentation seams touched are drawRow / setItemDelegate / DnD event overrides — never the non-virtual DocumentItem::createNewItem/populateItem (D-01/D-02, RESEARCH Pitfall 2)"
    - "A QTEST_MAIN (offscreen) test constructs an FwFeatureTree under a real QApplication and asserts it is-a Gui::TreeWidget with the inherited F2 rename action present (D-13) — construction cannot run in the bare Gui_tests_run target (no QApplication), mirroring the Phase-2 FwRibbonWidget split"
    - "The D-03 tree-reuse spike (Spike A) is recorded in SPIKE.md with an explicit PASS/FAIL line for EACH of the 7 PASS-checklist items (RESEARCH § Spike Plan) and a VERDICT using the literal token 'reuse committed' (PASS) or 'subclass-fallback committed' (FAIL); Spike B (Body.Tip path, research-CONFIRMED) is recorded with its confirmed items plus the live-deferred items routed to SPIKE_LIVE_CHECKLIST"
    - "tools/fw-string-leak-grep.sh is clean over src/Gui/FreeWorks/ — no bare 'SolidWorks' token in any new source (only Gui::SolidWorksNavigationStyle is allow-listed; the SW plane display names belong to Plan 03-02 and contain no 'SolidWorks' token anyway)"
  artifacts:
    - path: "src/Gui/FreeWorks/FwFeatureTree.h"
      provides: "FwFeatureTree class declaration: thin subclass of Gui::TreeWidget; the spike surface (scoping helper + presentation seam declarations)"
      contains: "class FwFeatureTree"
    - path: "src/Gui/FreeWorks/FwFeatureTree.cpp"
      provides: "Minimal-but-real FwFeatureTree: construct + mount-able + active-Body scope resolution by \"PartDesign::Body\" type-name literal; the spike implementation"
      contains: "PartDesign::Body"
    - path: "tests/src/Gui/FwFeatureTree.cpp"
      provides: "GTest (Gui_tests_run): active-Body type-name scoping, Tip-move/mustExecute logic, insertObject placement, reorder+undo, DnD gate BLOCK"
      contains: "TEST_F"
      min_lines: 60
    - path: "tests/src/Gui/FwFeatureTreeWidget.cpp"
      provides: "QTEST_MAIN (offscreen) test: FwFeatureTree construction under a real QApplication + inherited F2 action present"
      contains: "QTEST_MAIN"
    - path: ".planning/phases/03-featuremanager-design-tree/SPIKE.md"
      provides: "Spike A (D-03 tree reuse, 7-item PASS checklist) + Spike B (D-04 Body.Tip path) verdict; the literal token 'reuse committed' or 'subclass-fallback committed'"
      contains: "Verdict"
  key_links:
    - from: "tests/src/Gui/FwFeatureTree.cpp"
      to: "tests/src/Gui/FwTestGuiBootstrap.h"
      via: "SetUpTestSuite calls tests::ensureGuiTestBootstrap() so PartDesign::Body + PartDesign_MoveTip resolve before the asserts"
      pattern: "ensureGuiTestBootstrap"
    - from: "tests/src/Gui/CMakeLists.txt"
      to: "tests/src/Gui/FwFeatureTreeWidget.cpp"
      via: "setup_qt_test(FwFeatureTreeWidget) so a QApplication exists for QWidget construction"
      pattern: "FwFeatureTreeWidget"
    - from: "src/Gui/FreeWorks/FwFeatureTree.h"
      to: "Gui::TreeWidget"
      via: "public subclass reusing the (const char* name, QWidget* parent) ctor"
      pattern: "public Gui::TreeWidget"
---

<objective>
Stand up the Wave 0 test scaffold AND run the time-boxed tree-reuse spike (Spike A, D-03) that resolves the keystone foundation decision for this phase: can FreeCAD's existing `Gui::TreeWidget` be scoped to the active Body, shown Origin-on-top, and restyled SW-like via a thin additive `FwFeatureTree` subclass WITHOUT editing `Tree.cpp` bodies (CONTEXT D-01/D-02/D-03; RESEARCH § Spike Plan)? This is the first vertical slice: a thin, real `FwFeatureTree : Gui::TreeWidget` that constructs, mounts, and resolves the active Body by the `"PartDesign::Body"` type-name string literal — plus a headless GTest proving the rollback/reorder logic against the REAL `Body.Tip` engine and the dependency-aware DnD gate, and a QTEST_MAIN widget-construction test. The spike's exit criteria make the reuse-vs-fallback choice explicit: PASS → SPIKE.md records `reuse committed` and the thin subclass is the committed engine for Plans 03-02/03-03; FAIL → record `subclass-fallback committed` (override more `TreeWidget` virtuals, still reusing DnD/rename/transaction machinery — from-scratch is last resort only).

Purpose: De-risk the one genuinely uncertain part of an otherwise ~90%-reuse phase (RESEARCH Pitfall 2: per-item construction in `DocumentItem`/`DocumentObjectItem` is non-virtual and `TreeWidget`-friended, so the spike must confirm scoping + greying are reachable from `drawRow`/delegate/palette seams alone), and establish the headless test harness Plans 03-02/03-03 sample against (mirrors the Phase-2 native-ribbon spike-gate structure).
Output: `FwFeatureTree.{h,cpp}` (minimal but real), GTest logic tests in `Gui_tests_run` (`FwFeatureTree.cpp`), a QTEST_MAIN construction test (`FwFeatureTreeWidget.cpp`, offscreen) wired via `setup_qt_test`, CMake additions (FreeWorks + tests, both additive), and `SPIKE.md` recording the Spike A verdict (`reuse committed`/`subclass-fallback committed`) plus the research-CONFIRMED Spike B status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-featuremanager-design-tree/03-CONTEXT.md
@.planning/phases/03-featuremanager-design-tree/03-RESEARCH.md
@.planning/phases/03-featuremanager-design-tree/03-PATTERNS.md
@.planning/phases/03-featuremanager-design-tree/03-UI-SPEC.md
@.planning/phases/03-featuremanager-design-tree/03-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols/files created across Phase 3 (this plan creates the first set):

- **This plan (03-01):** class `FwFeatureTree` (namespace `FreeWorksGui`, `src/Gui/FreeWorks/FwFeatureTree.{h,cpp}`) — thin `Gui::TreeWidget` subclass; spike surface with the active-Body scope resolver keyed on the `"PartDesign::Body"` type-name literal and the presentation-seam stubs (`drawRow`/`setItemDelegate`). `tests/src/Gui/FwFeatureTree.cpp` (GTest, added to `Gui_tests_run`): scoping/Tip-logic/insertObject/reorder-undo/DnD-gate-BLOCK. `tests/src/Gui/FwFeatureTreeWidget.cpp` (QTEST_MAIN, offscreen, registered via `setup_qt_test`): construction + F2 action present. `SPIKE.md` (Spike A + B verdict). CMake edits to `src/Gui/FreeWorks/CMakeLists.txt` (`FreeWorks_CPP_SRCS`/`FreeWorks_HPP_SRCS`) and `tests/src/Gui/CMakeLists.txt` (`Gui_tests_run` list + `setup_qt_test(FwFeatureTreeWidget)`).
- **Plan 03-02 (later):** `FwFeatureTreeDelegate.{h,cpp}` (plane label remap Front/Top/Right + below-tip greying paint); full `FwFeatureTree` scoping + delegate install + F2-active confirmation; `FwLayout.cpp` mount swap (replace the `Fw_FeatureManager` placeholder); dock objectName stays `Fw_FeatureManager`.
- **Plan 03-03 (later):** `FwRollbackBar` mechanism (the `drawRow` band + grab zone + drag→`Body.Tip` fire via the `"PartDesign_MoveTip"` command-ID / Python `doCommand` string), reversibility ("Roll to End"), insert-at-bar via `Body::insertObject`, and `SPIKE_LIVE_CHECKLIST.md` (live 3D suppress-below feel + insertion-line cursor obligations).

> **Downstream gate:** Plans 03-02/03-03 require this plan's `SPIKE.md` to record the Spike-A verdict. If it reads `reuse committed`, the thin-subclass engine is committed; if it reads `subclass-fallback committed`, those plans build on the heavier (still additive) subclass approach (override more `TreeWidget` virtuals) — they must NOT proceed on the thin surface merely because 03-01 ran.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — GTest logic (scoping + Tip + reorder + DnD gate) and QTEST construction, split across targets</name>
  <files>tests/src/Gui/FwFeatureTree.cpp, tests/src/Gui/FwFeatureTreeWidget.cpp, tests/src/Gui/CMakeLists.txt</files>
  <read_first>
    - tests/src/Gui/FwRibbon.cpp (GTest analog: `SetUpTestSuite()` calls `tests::ensureGuiTestBootstrap()` at lines 43-58; `constexpr const char*` ID/type constants; the intentionally-empty allow-list guard; the `kSketchVpTypeName` type-name-literal precedent at line 163 — mirror it for `kBodyTypeName = "PartDesign::Body"`)
    - tests/src/Gui/FwRibbonWidget.cpp (EXACT QTEST analog: `class testFwRibbonWidget : public QObject` with ctor calling `ensureGuiTestBootstrap()`, `private Q_SLOTS`, the `ensureRealMainWindow()` leaked-singleton helper at lines 37-43, `QTEST_MAIN(...)` + `#include "FwRibbonWidget.moc"` at EOF)
    - tests/src/Gui/FwTestGuiBootstrap.h (REUSE VERBATIM, no edit: `tests::ensureGuiTestBootstrap()` creates `Gui::Application(false)` then imports PartDesignGui/SketcherGui/MeasureGui/MatGui — confirm lines 54-105 load PartDesignGui so `PartDesign::Body`, the feature types, and `PartDesign_MoveTip` resolve)
    - tests/src/Gui/CMakeLists.txt (the `Gui_tests_run` add_executable list lines 7-18 — add `FwFeatureTree.cpp` after `FwRibbon.cpp` line 18; the `setup_qt_test(FwRibbonWidget)` call line 23 — add `setup_qt_test(FwFeatureTreeWidget)` next to it)
    - tests/CMakeLists.txt lines 13-47 (`function(setup_qt_test)` — the QApplication-providing harness: QTEST_MAIN + links FreeCADApp/FreeCADGui/QtTest + `QT_QPA_PLATFORM=offscreen`)
    - src/Mod/PartDesign/App/Body.cpp lines 94-100 (`mustExecute()` returns 1 on `Tip.isTouched()` — the suppress-below trigger to assert), 248 (`Tip.setValue` on addObject), 275-321 (`insertObject(feature, target, after)` placement), 176-194 (`isSolidFeature` false for datums/sketches — Pitfall 5)
    - src/Mod/Part/App/BodyBase.h line 54 (`App::PropertyLink Tip;` — the property to set in the logic test)
    - src/Gui/ViewProvider.h lines 444-493 (`canDragObjectToTarget`/`canDropObjectEx` declarations — the DnD gate to assert returns false for child-before-parent)
    - src/Gui/Tree.cpp lines 3260-3273 (`sortDroppedObjects` Group `PropertyLinkList::setValue` reorder — the path the reorder+undo test mirrors)
    - .planning/phases/03-featuremanager-design-tree/03-PATTERNS.md § "tests/src/Gui/FwFeatureTree.cpp" + § "tests/src/Gui/FwFeatureTreeWidget.cpp" (the exact test lists + QTEST class skeleton)
    - .planning/phases/03-featuremanager-design-tree/03-VALIDATION.md § "Per-Task Verification Map" + § "Wave 0 Requirements" (wire each test to its Req row)
  </read_first>
  <behavior>
    - GTest (Gui_tests_run, FwFeatureTree.cpp), after `ensureGuiTestBootstrap()`:
      - Scoping: a freshly created `PartDesign::Body` reports `getTypeId().getName()` exactly equal to the `kBodyTypeName = "PartDesign::Body"` literal, and a non-Body object does not — proving active-Body identification needs NO module link (TREE-01 scoping, D-07).
      - Rollback logic (TREE-02, D-04): build a Body with at least two solid features; set `Body.Tip` to the earlier feature; assert `Tip.isTouched()` is true and `Body::mustExecute()` returns 1 (the real suppress-below trigger). Then set Tip to the last solid feature ("Roll to End", Pitfall 4) and assert it is accepted.
      - Insert-at-bar (TREE-02): `Body::insertObject(newFeature, targetFeature, /*after=*/true)` places `newFeature` immediately after `targetFeature` in the `Group` order and does NOT change `Tip` (insertObject "doesn't modify the Tip unlike addObject").
      - Reorder + undo (TREE-04, D-12): inside one `doc->openTransaction("Reorder")` / `commitTransaction()`, reorder the body's `Group` `PropertyLinkList`; assert the new order; `doc->undo()` restores the original order (clean undo, no `.FCStd` pollution).
      - DnD gate BLOCK (TREE-04, D-10/D-11): construct a parent→child dependency and assert the ViewProvider gate (`canDragObjectToTarget` or `canDropObjectEx`) returns false for moving the child before its parent — the drop is BLOCKED, no transaction opens.
    - QTEST (FwFeatureTreeWidget.cpp, QTEST_MAIN, offscreen): under the QApplication QTEST_MAIN provides, an `FwFeatureTree` constructs (`FwFeatureTree("FwFeatureManager", nullptr)`), is-a `Gui::TreeWidget` (dynamic_cast non-null), and the inherited F2 rename action is present (the action whose shortcut is `Qt::Key_F2`, or `Qt::Key_Return` on macOS — D-13) — proving F2 rename is wired in the hosted tree without re-implementation.
  </behavior>
  <action>
    Create `tests/src/Gui/FwFeatureTree.cpp` mirroring `FwRibbon.cpp` structure: include `"FwTestGuiBootstrap.h"`, declare a `FwFeatureTreeTest : ::testing::Test` whose `SetUpTestSuite()` calls `tests::ensureGuiTestBootstrap()` (NOT a bare `initApplication()` — the GUI singleton + PartDesignGui import are required so `PartDesign::Body` resolves). Declare `constexpr const char* kBodyTypeName = "PartDesign::Body";` (mirror the Phase-2 `kSketchVpTypeName` literal precedent). Build the Body/features for the logic tests via the running Python interpreter (`Base::Interpreter().runString(...)` to create a document + Body + two solid features) OR via the App factory — whichever needs NO `#include <Mod/PartDesign/...>` and NO link (resolve types by name through `App::Document`/`Base::Type`, exactly the no-module-link rule). Author the five `TEST_F` cases from the behavior block: type-name scoping match/non-match; set-Tip-earlier → `Tip.isTouched()` + `mustExecute()==1`; `insertObject` placement (Tip unchanged); Group reorder in one transaction + `undo()` restores; DnD gate returns false for child-before-parent. Reach `Tip`/`Group`/`mustExecute` through `App::Document`/`App::DocumentObject` generic accessors (`getPropertyByName("Tip")`, `getPropertyByName("Group")`, `mustExecute()` is on `DocumentObject`) so no PartDesign header is needed. Add `FwFeatureTree.cpp` to the `Gui_tests_run` `add_executable` list in `tests/src/Gui/CMakeLists.txt` (after `FwRibbon.cpp`). Create `tests/src/Gui/FwFeatureTreeWidget.cpp` mirroring `FwRibbonWidget.cpp`: `#include <QTest>`, `#include "FwTestGuiBootstrap.h"`, a `testFwFeatureTreeWidget : QObject` whose ctor calls `ensureGuiTestBootstrap()` (reuse the `ensureRealMainWindow()` leaked-singleton helper if the tree needs a registered main window), a `private Q_SLOTS` test that constructs an `FwFeatureTree`, `QVERIFY`s the `dynamic_cast<Gui::TreeWidget*>` is non-null, and `QVERIFY`s the F2 action is present (scan `actions()` for one whose `shortcut()` is `Qt::Key_F2`/`Qt::Key_Return`); end with `QTEST_MAIN(testFwFeatureTreeWidget)` + `#include "FwFeatureTreeWidget.moc"`. Register it via `setup_qt_test(FwFeatureTreeWidget)` next to `setup_qt_test(FwRibbonWidget)`. No "SolidWorks" string in any identifier or literal (CI leak-grep). These tests will go RED until Task 2 lands `FwFeatureTree`.
  </action>
  <verify>
    <automated>ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/src/Gui/FwFeatureTree.cpp` contains `TEST_F`, `ensureGuiTestBootstrap`, the `"PartDesign::Body"` type-name literal, `Tip`, `mustExecute`, `insertObject`, `openTransaction`, and `undo` — and contains NO `#include <Mod/PartDesign` and NO `FwFeatureTree` widget construction (logic-only).
    - `grep -c 'mustExecute' tests/src/Gui/FwFeatureTree.cpp` returns >= 1 (the suppress-below trigger is asserted, TREE-02).
    - `tests/src/Gui/FwFeatureTreeWidget.cpp` contains `QTEST_MAIN`, constructs an `FwFeatureTree`, and asserts both `Gui::TreeWidget` (dynamic_cast) and a `Qt::Key_F2`/`Qt::Key_Return` action present.
    - `grep -c 'FwFeatureTree.cpp' tests/src/Gui/CMakeLists.txt` returns >= 1 AND `grep -c 'setup_qt_test(FwFeatureTreeWidget)' tests/src/Gui/CMakeLists.txt` returns 1.
    - `ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure` passes once Task 2 lands (logic asserts + construction assert green) — in this env the run rides CI; authored compile-intended.
    - `grep -ri solidworks tests/src/Gui/FwFeatureTree.cpp tests/src/Gui/FwFeatureTreeWidget.cpp` finds nothing.
  </acceptance_criteria>
  <done>The Wave 0 scaffold exists and reuses the existing bootstrap verbatim: rollback/reorder/scoping logic is asserted against the real `Body.Tip`/`Group`/DnD-gate engine in `Gui_tests_run`, widget construction + F2-action-present runs under an offscreen QApplication in a QTEST target, both registered in CMake, no module link, clean of trademark strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimal native FwFeatureTree (thin Gui::TreeWidget subclass) — construct, mount-able, active-Body scope by type-name literal</name>
  <files>src/Gui/FreeWorks/FwFeatureTree.h, src/Gui/FreeWorks/FwFeatureTree.cpp, src/Gui/FreeWorks/CMakeLists.txt</files>
  <read_first>
    - src/Gui/FreeWorks/FwRibbon.h lines 1-56 (copy the SPDX line 1 + the 23-line LGPL-2.1 copyright block 3-23 + `#pragma once` + `#include "PreCompiled.h"` + `namespace FreeWorksGui` + the doc-comment framing "Compiled directly into FreeCADGui ... no cross-library export macro is required (mirrors FwWorkbench)")
    - src/Gui/Tree.h lines 60-65 (`class TreeWidget : public QTreeWidget`, the public ctor `TreeWidget(const char* name, QWidget* parent = nullptr)` — the reused base ctor), 147 (`void drawRow(...) const override;` — the protected-virtual presentation seam), 82 (`eventFilter` — the F2 path, do NOT re-implement), 328-331 & 456-482 (the NON-virtual `DocumentItem`/`DocumentObjectItem` `TreeWidget`-friended item construction — DO NOT touch; wanting to is the D-03 FAIL signal)
    - tests/src/Gui/FwRibbon.cpp line 163 (the `kSketchVpTypeName` type-name-literal precedent — mirror it for `"PartDesign::Body"`)
    - src/Gui/Application.h lines 130,142,154 (`signalActiveDocument`/`signalActivatedObject`/`signalInEdit` — the Gui signals the scope resolver observes; observe-the-DOM, no App-layer link)
    - src/Gui/FreeWorks/FwLayout.cpp lines 95-103 (`ensureDock` / `registerDockWindow(name, widget)` — the mount seam Plan 03-02 uses; this plan only makes the widget mount-able)
    - src/Gui/FreeWorks/CMakeLists.txt lines 17-26 (`FreeWorks_CPP_SRCS`), 28-37 (`FreeWorks_HPP_SRCS`), 48 (`target_sources(FreeCADGui PRIVATE ...)`) — append `FwFeatureTree.cpp`/`FwFeatureTree.h`; NO `target_link_libraries` to PartDesign
    - .planning/phases/03-featuremanager-design-tree/03-PATTERNS.md § "src/Gui/FreeWorks/FwFeatureTree.{h,cpp}" (class decl + reusable virtual seams list + the active-Body scoping snippet)
    - .planning/phases/03-featuremanager-design-tree/03-UI-SPEC.md § Color (no `setStyleSheet`/hex; palette/QStyle only) + § Spacing Scale (22px row height, 16px indent reserved — pin metrics, art deferred)
  </read_first>
  <behavior>
    - `FwFeatureTree("FwFeatureManager", parent)` constructs as a `Gui::TreeWidget` subclass (reuses the public base ctor) — no crash, no QApplication-less construction (construction is exercised by the QTEST target).
    - A pure-logic scope resolver `bool isBody(const App::DocumentObject*) const` (or a free helper) returns true exactly when `obj->getTypeId().getName()` equals the `"PartDesign::Body"` literal — keep it pure so the GTest exercises it without a widget (mirror the `FwRibbonContext` decide-then-act split: Gui-only decision, no App mutation).
    - The class declares (but does not yet fully theme) the presentation seams it will own: `drawRow(...)` override (Plan 03-03 paints the rollback band) and a `setItemDelegate` install point (Plan 03-02 installs the plane-remap/greying delegate). This spike build wires the seams as no-op/pass-through overrides so the subclass compiles and the inherited rendering is unchanged.
    - NO `setStyleSheet`, NO hex color anywhere (UI-SPEC § Color; Phase 7 owns theming). NO `#include <Mod/PartDesign/...>`, NO PartDesign link (reference Body by the type-name literal only).
  </behavior>
  <action>
    Create `src/Gui/FreeWorks/FwFeatureTree.h`/`.cpp`, namespace `FreeWorksGui`, copying the `FwRibbon.h` license/copyright/pragma block verbatim and reusing its doc-comment framing. Declare `class FwFeatureTree : public Gui::TreeWidget { Q_OBJECT public: explicit FwFeatureTree(const char* name, QWidget* parent = nullptr); ... };` — the ctor forwards to the base `Gui::TreeWidget(name, parent)`. Add the pure scope resolver keyed on `constexpr const char* kBodyTypeName = "PartDesign::Body";` comparing `obj->getTypeId().getName()` (the GTest's scoping assertion targets this; mirror the Phase-2 `kSketchVpTypeName` literal). Declare/define a pass-through `void drawRow(QPainter*, const QStyleOptionViewItem&, const QModelIndex&) const override;` that calls `Gui::TreeWidget::drawRow(...)` (the rollback band paint is Plan 03-03) and reserve a `setItemDelegate` install hook (the delegate is Plan 03-02) — do NOT touch the non-virtual `DocumentItem`/`DocumentObjectItem` item construction (Pitfall 2; wanting to is the FAIL signal). Observe active-Body changes via the Gui signals (`Gui::Application::Instance->signalActivatedObject`/`signalInEdit`/`signalActiveDocument`) — connect them so the active-Body scope can be resolved, but keep the observed-Body resolution as the pure `isBody`/type-name path (no PartDesign include). Explicitly `#include` the Qt headers used (`<QTreeWidget>` comes via the `Gui::TreeWidget` base; add `<QPainter>`/`<QStyleOptionViewItem>` for the `drawRow` signature) — do not rely on PCH. Use ONLY palette/native `QStyle` for any color (no `setStyleSheet`, no hex). Append `FwFeatureTree.cpp`/`FwFeatureTree.h` to `FreeWorks_CPP_SRCS`/`FreeWorks_HPP_SRCS` in `src/Gui/FreeWorks/CMakeLists.txt` (additive). No "SolidWorks" identifier/string anywhere (leak-grep). Note: the plane-remap delegate, below-tip greying, the rollback bar, and the dock mount are Plans 03-02/03-03 — keep this minimal but real and mount-able.
  </action>
  <verify>
    <automated>ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/Gui/FreeWorks/FwFeatureTree.h` contains `class FwFeatureTree` and `public Gui::TreeWidget` and a `drawRow` override declaration.
    - `src/Gui/FreeWorks/FwFeatureTree.cpp` contains the `"PartDesign::Body"` type-name literal and connects at least one of `signalActivatedObject`/`signalInEdit`/`signalActiveDocument`.
    - `grep -c 'setStyleSheet' src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0 (no inline styling; UI-SPEC § Color).
    - `grep -c '#include <Mod/PartDesign' src/Gui/FreeWorks/FwFeatureTree.h src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0 AND `grep -c 'PartDesignGui' src/Gui/FreeWorks/CMakeLists.txt` returns 0 (no module include, no module link — D-07/Pitfall 1).
    - `grep -v '^#' src/Gui/FreeWorks/CMakeLists.txt | grep -c 'FwFeatureTree.cpp'` returns >= 1.
    - `ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure` passes (Task 1 logic asserts + the QTEST construction/F2 assert now green against the real `FwFeatureTree`).
    - `bash tools/fw-string-leak-grep.sh` clean (no bare "SolidWorks" token in `src/Gui/FreeWorks/`).
  </acceptance_criteria>
  <done>FwFeatureTree compiles into FreeCADGui as a thin `Gui::TreeWidget` subclass, constructs under an offscreen QApplication, resolves the active Body by the `"PartDesign::Body"` type-name literal (no module link), reserves the `drawRow`/delegate seams without touching non-virtual item construction, and the headless Task 1 tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tree-reuse spike gate — Spike A (D-03) 7-item PASS checklist + Spike B (Body.Tip) status & verdict</name>
  <files>.planning/phases/03-featuremanager-design-tree/SPIKE.md</files>
  <read_first>
    - .planning/phases/03-featuremanager-design-tree/03-RESEARCH.md § "Spike Plan" (Spike A: the 7-item D-03 PASS checklist + the `reuse committed`/`subclass-fallback committed` verdict tokens + the FAIL branch; Spike B: the CONFIRMED Body.Tip items + the live-deferred open items)
    - .planning/phases/03-featuremanager-design-tree/03-CONTEXT.md D-01..D-06 (reuse-first; spike-before-build; no `Tree.cpp` edits; the rollback engine; Gui-only greying)
    - .planning/phases/02-commandmanager-ribbon/SPIKE.md (the Phase-2 spike-verdict format to mirror: per-item PASS/FAIL + an explicit literal verdict token + the deferral routing to SPIKE_LIVE_CHECKLIST)
    - .planning/phases/02-commandmanager-ribbon/02-01-SUMMARY.md (the verification-deferral pattern: blocking human-verify cleared by doc-approval + a source-tree live checklist artifact carrying the on-hardware obligation — no build tree/GUI/CI here)
    - src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md (the Phase-2 live-checklist artifact format to mirror for the deferred items)
  </read_first>
  <what-built>
    A minimal-but-real thin `FwFeatureTree : Gui::TreeWidget` subclass (constructs, mounts-able, resolves the active Body by the `"PartDesign::Body"` type-name literal with NO module link) plus a headless GTest proving the rollback logic against the real `Body.Tip` engine (set-Tip-earlier → `mustExecute()==1`), `insertObject` placement, Group reorder + undo, and the DnD gate BLOCKing a child-before-parent reorder; and a QTEST construction/F2-present test. This task is the human verification of whether reusing-and-wrapping `Gui::TreeWidget` reaches the SW FeatureManager parity bar (D-03) WITHOUT editing `Tree.cpp` bodies — so the reuse-vs-fallback decision (D-01/D-02/D-03) is made explicitly before the full build.
  </what-built>
  <how-to-verify>
    Build a THROWAWAY spike harness (an `FwFeatureTree` mounted in the `Fw_FeatureManager` dock for the spike only — the production mount is Plan 03-02) against a live document with an active `PartDesign::Body` carrying an Origin + a couple of solid features + a sketch. Walk the 7-item Spike-A PASS checklist from RESEARCH § "Spike Plan", recording an explicit PASS or FAIL line for EACH item in SPIKE.md. Items split into two evidence classes so the gate is an engineering decision, not subjective theater (mirrors the Phase-2 spike gate):

    **A. Reuse primitives the spike code / research directly establishes — demonstrate, do not assert by eye:**
    1. `FwFeatureTree` subclass constructs and mounts in `Fw_FeatureManager` (the `TreeWidget(name, parent)` ctor is public — confirmed by Task 2 + the QTEST construction test).
    2. The tree can be SCOPED to the active Body's subtree from the FreeWorks layer using ONLY Gui signals + the `"PartDesign::Body"` type-name literal (NO PartDesign/Sketcher link/include) — demonstrate the non-Body items are excluded/hidden purely in the Gui.
    3. Origin node renders FIRST with its 3 planes + 3 axes + point (free via `ViewProviderOriginGroupExtension::constructChildren` — confirm it survives scoping).
    4. Features render in creation order; the profile sketch nests under its consuming feature (free via `claimChildren` — confirm).
    5. Front/Top/Right plane label remap is REACHABLE via a `QStyledItemDelegate` (display text only; object `Label` unchanged) — demonstrate at least a prototype remap renders, or confirm the delegate seam accepts the remap (the full delegate is Plan 03-02).
    6. Below-tip greying is REACHABLE via delegate / `QPalette::Disabled` / item flags with NO `Visibility`/property mutation (Pitfall 3, D-06) — demonstrate a prototype grey, or confirm the seam.
    7. `tools/fw-string-leak-grep.sh` clean (only `Gui::SolidWorksNavigationStyle` allow-listed).

    **B. Spike B — Body.Tip rollback path (research-CONFIRMED HIGH; record the confirmed items + route the live items to the checklist):**
    Record the research-confirmed items as CONFIRMED (Body.Tip is a persisted PropertyLink; set-Tip+recompute = suppress-below via `mustExecute` on `Tip.isTouched`; the `CmdPartDesignMoveTip` set-tip sequence exists; `insertObject` exists; the 3D tip display exists) — all backed by Task 1's passing GTest for the logic half. Route the LIVE-only open items to SPIKE_LIVE_CHECKLIST (created in Plan 03-03): firing via `PartDesign_MoveTip` command-ID / Python `doCommand` from FreeWorks with no module link (A3); drag-down/"Roll to End" reversibility on hardware (Pitfall 4); 3D shows the rolled-back shape + greyed rows + SC5 feel (parity-user track).

    Then record the VERDICT in SPIKE.md: PASS (all class-A items 1-4 + 7 PASS, and items 5-6 are confirmed REACHABLE) = write the literal phrase `reuse committed`; the thin subclass is the committed engine for Plans 03-02/03-03. FAIL (any structural override needs the non-virtual `DocumentItem`/`DocumentObjectItem` surgery of Pitfall 2, OR scoping/greying cannot be done from the `drawRow`/delegate/palette seams, OR the time-box is exceeded) = write `subclass-fallback committed`; Plans 03-02/03-03 build on the heavier-but-additive subclass (override more `TreeWidget` virtuals, still reusing DnD/rename/transaction machinery — from-scratch is the last resort only if even that fails). In THIS environment there is no build tree / live GUI: clear the gate by DOCUMENTATION APPROVAL (Phase 1/2 precedent), recording the research-grounded PASS/FAIL judgement per item and deferring the live demonstrations (scoping render, prototype remap/grey on hardware, 3D rollback feel) to SPIKE_LIVE_CHECKLIST — NOT fabricated.
  </how-to-verify>
  <action>
    Author `.planning/phases/03-featuremanager-design-tree/SPIKE.md` recording: (1) Spike A — an explicit PASS or FAIL mark for EACH of the 7 D-03 PASS-checklist items under the A evidence class, citing the Task 1/Task 2 artifact or the research seam that establishes each (e.g. item 3 cites `ViewProviderOriginGroupExtension.cpp:57`); (2) Spike B — the research-CONFIRMED Body.Tip items (backed by Task 1's passing logic GTest) plus the explicit list of LIVE-only items routed to SPIKE_LIVE_CHECKLIST; (3) an explicit VERDICT using the literal token `reuse committed` (PASS) or `subclass-fallback committed` (FAIL). On PASS, record that Plans 03-02/03-03 proceed on the thin-subclass engine. On FAIL, record the FAIL-branch next step (heavier subclass: which additional `TreeWidget` virtuals to override; still reusing DnD/rename/transaction; from-scratch only as last resort) and flag that Plans 03-02/03-03 actions must be re-scoped to the heavier surface before execution. Because this env has no build tree / live GUI, mark the gate cleared by documentation approval (Phase 1/2 precedent) and list the deferred live demonstrations (active-Body scoping render, prototype plane-remap + below-tip grey on hardware, the Spike-B live fire/reversibility/3D items) as open obligations to be created in SPIKE_LIVE_CHECKLIST.md in Plan 03-03 — never fabricated as observed. No "SolidWorks" string in any SPIKE.md code identifier (descriptive prose naming the reference CAD product in the verdict rationale is acceptable in planning docs; the leak-grep covers source files, not `.planning/`).
  </action>
  <verify>
    <human-check>SPIKE.md records a PASS/FAIL line for all 7 Spike-A D-03 items, the Spike-B CONFIRMED-items + live-deferred list, and an explicit verdict using the literal `reuse committed` or `subclass-fallback committed`, signed off by the developer; `grep -c "PASS\|FAIL" .planning/phases/03-featuremanager-design-tree/SPIKE.md` returns >= 7</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-featuremanager-design-tree/SPIKE.md` exists and contains the word `Verdict`.
    - All 7 Spike-A checklist items are individually recorded: `grep -c "PASS\|FAIL" .planning/phases/03-featuremanager-design-tree/SPIKE.md` returns >= 7 (one PASS/FAIL per item).
    - The verdict states either the literal `reuse committed` (proceed to Plan 03-02 on the thin subclass) OR `subclass-fallback committed` (re-scope Plans 03-02/03-03 to the heavier subclass) with the corresponding next-step.
    - Spike B records the Body.Tip items as research-CONFIRMED (backed by Task 1's passing logic GTest) and lists the LIVE-only open items routed to SPIKE_LIVE_CHECKLIST (fire via command-ID/Python with no link; reversibility; 3D feel).
    - If FAIL: SPIKE.md lists which additional `TreeWidget` virtuals the heavier subclass overrides and confirms from-scratch is last resort only.
  </acceptance_criteria>
  <done>SPIKE.md records an individual PASS/FAIL for all 7 Spike-A D-03 items (grep count >= 7), the Spike-B Body.Tip CONFIRMED items + live-deferred list, and an explicit verdict using the literal `reuse committed` or `subclass-fallback committed`; the developer has signed off so Plans 03-02/03-03 know which engine to build on.</done>
  <resume-signal>Type "approved: reuse" to proceed to Plan 03-02 on the thin-subclass engine (SPIKE.md must read `reuse committed`), or "approved: subclass-fallback" to build Plans 03-02/03-03 on the heavier `TreeWidget`-subclass surface</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FreeWorks Gui tree → App layer | The tree observes the live `App::Document` via SelectionObserver / ViewProvider signals and resolves the active Body by a type-name STRING literal; it never includes or mutates App-layer headers and never links PartDesign. |
| developer-authored type-name literal → Body identification | The `"PartDesign::Body"` constant is a developer string compared against `getTypeId().getName()`; no user-supplied input is parsed this phase. |
| fork code ↔ upstream shared files | Only additive `target_sources`/test-list edits; `Tree.cpp` bodies are NOT edited (D-02); any unavoidable shared touch gets `// SW-FORK HOOK`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Repudiation/Legal | New source/test files, SPIKE.md | mitigate | No bare "SolidWorks" identifier/string in any new source file; `tools/fw-string-leak-grep.sh` CI gate (only `Gui::SolidWorksNavigationStyle` allow-listed); behavior described as reference-CAD-style |
| T-03-02 | Tampering (fork isolation) | FwFeatureTree, CMakeLists.txt | mitigate | NO `#include <Mod/PartDesign/...>`, NO `target_link_libraries(... PartDesignGui)`; reference Body by type-name literal + Gui signals only (RESEARCH Pitfall 1, Phase 2 precedent); additive `target_sources`/test-list edits only; no `Tree.cpp`/`MainWindow.cpp` edit |
| T-03-03 | Tampering (model integrity) | Body.Tip / Group reorder logic test | mitigate | All App-state mutation (set Tip, reorder) goes through `Document::openTransaction`/`commitTransaction` (clean undo asserted); no new persisted property (D-05); the rollback engine is the existing transactional, recompute-aware `Body.Tip` (D-04) |
| T-03-04 | Tampering (presentation leak) | greying / scoping (verified reachable in spike) | mitigate | Greying/scoping is Gui-only (delegate/`QPalette::Disabled`/item flags); NEVER `Visibility.setValue` or any property write for presentation (D-06, Pitfall 3) — asserted by the spike checklist item 6 |
| T-03-05 | Spoofing/Input Validation | type-name literal resolution | accept | The `"PartDesign::Body"` literal is a developer constant compared against the live type system; no user input — low risk |
| T-03-06 | Tampering (invalid reorder corrupts model) | DnD gate test | mitigate | The DnD gate (`canDragObjectToTarget`/`canDropObjectEx`) BLOCKs child-before-parent (asserted false in GTest); only valid reorders commit, in one transaction (D-10/D-11/D-12) |
</threat_model>

<verification>
- `ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure` green: rollback/reorder/scoping logic (Gui_tests_run, after the existing module-GUI bootstrap) AND FwFeatureTree construction + F2-action-present (FwFeatureTreeWidget Qt test, offscreen QApplication). In this env the run rides CI; authored compile-intended (Phase 1/2 precedent).
- `SPIKE.md` records the Spike-A verdict (all 7 D-03 items individually PASS/FAIL) and the reuse-vs-fallback decision (`reuse committed` or `subclass-fallback committed`), plus the Spike-B CONFIRMED items + live-deferred list.
- No bare "SolidWorks" string in any new source file (`tools/fw-string-leak-grep.sh`).
- No PartDesign include/link in `src/Gui/FreeWorks/`; only additive CMake/test-list edits; no `Tree.cpp` edits.
</verification>

<success_criteria>
- The Wave 0 scaffold reuses the existing `FwTestGuiBootstrap.h` verbatim so `PartDesign::Body` + `PartDesign_MoveTip` resolve headlessly; rollback (`mustExecute` on Tip), insert-at-bar (`insertObject`), reorder+undo (Group transaction), and the DnD BLOCK gate are all asserted against the REAL engine in `Gui_tests_run`; construction + F2-present run under an offscreen QApplication in a QTEST target.
- A minimal real `FwFeatureTree : Gui::TreeWidget` proves the thin-subclass reuse surface compiles, mounts, and scopes the active Body by the `"PartDesign::Body"` type-name literal with NO module link.
- The reuse-vs-fallback decision (D-01/D-02/D-03) is recorded with an explicit per-item PASS/FAIL verdict (all 7 Spike-A items) and an actionable next step using the literal `reuse committed`/`subclass-fallback committed`; the Spike-B Body.Tip path is recorded research-CONFIRMED with live items routed to SPIKE_LIVE_CHECKLIST (created in Plan 03-03).
</success_criteria>

<output>
Create `.planning/phases/03-featuremanager-design-tree/03-01-SUMMARY.md` when done. Record: the Spike-A verdict (`reuse committed` vs `subclass-fallback committed`), the FwFeatureTree spike API surface (ctor, scope resolver, reserved `drawRow`/delegate seams), the Spike-B CONFIRMED Body.Tip items + the live obligations routed to SPIKE_LIVE_CHECKLIST, and whether Plans 03-02/03-03 proceed on the thin subclass or must re-scope to the heavier subclass.
</output>
