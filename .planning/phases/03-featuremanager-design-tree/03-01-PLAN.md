---
phase: 03-featuremanager-design-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/src/Gui/FwFeatureTree.cpp
  - tests/src/Gui/FwFeatureTreeWidget.cpp
  - tests/src/Gui/CMakeLists.txt
  - src/Gui/FreeWorks/FwFeatureTree.h
  - src/Gui/FreeWorks/FwFeatureTree.cpp
  - src/Gui/FreeWorks/CMakeLists.txt
  - .planning/phases/03-featuremanager-design-tree/SPIKE.md
autonomous: false
requirements:
  - TREE-01
  - TREE-02
  - TREE-04
user_setup: []

must_haves:
  truths:
    - "The Wave 0 test scaffold reuses the EXISTING tests/src/Gui/FwTestGuiBootstrap.h verbatim (no edit): tests::ensureGuiTestBootstrap() already creates the Gui::Application singleton then imports PartDesignGui/SketcherGui/MeasureGui/MatGui, so PartDesign::Body, the feature types, and the PartDesign_MoveTip command all resolve headlessly (RESEARCH Wave-0 / 03-PATTERNS § headless bootstrap)"
    - "Active-Body scoping resolves a Body purely by the type-name STRING literal \"PartDesign::Body\" (obj->getTypeId().getName() comparison) — there is NO #include <Mod/PartDesign/...> and NO target_link_libraries(... PartDesignGui) anywhere in src/Gui/FreeWorks/ (D-07, RESEARCH Pitfall 1: the clean PartDesignGui::getBody accessor is in the wrong layer for FreeWorks)"
    - "The CONCRETE scoping RENDER mechanism is row HIDING, not painting, and it traverses the REAL nested tree topology: in the stock Gui::TreeWidget the per-document Gui::DocumentItem (Tree.h:348) is the TOP-LEVEL QTreeWidgetItem and a PartDesign::Body is a CHILD nested under it (parent->addChild(item), Tree.cpp:4562) — Bodies are NOT top-level items. Therefore scopeToActiveBody() recurses from invisibleRootItem() (descending DocumentItem → its children), resolves each DocumentObjectItem's underlying object via the public DocumentObjectItem::object() accessor (Tree.h:495), keeps the ancestor DocumentItem visible, and HIDES the non-active PartDesign::Body subtrees with QTreeWidgetItem::setHidden(true) — the SAME public-item API Gui::TreeWidget itself uses (Tree.cpp:3458/3598/3986/4574/6070). drawRow / a QStyledItemDelegate can only PAINT, never remove or hide rows; therefore scoping is driven by recursive setHidden over the real DocumentItem→Body topology, NOT by topLevelItem(i) iteration (Bodies are not top-level) and NOT by the non-virtual DocumentItem::createNewItem/populateItem (D-03 scoping render; reviewer HIGH-A)"
    - "The active-Body feature ordering / solid detection uses ONLY link-free means: the Body's Group is read generically as App::PropertyLinkList via getPropertyByName(\"Group\") (GroupExtension::Group, GroupExtension.h:142) and solidness is decided by type-name STRING checks on each member object — the C++-only Body::getPrevSolidFeature / Body::isSolidFeature (Body.cpp:102/176, ABSENT from Body.pyi) are NEVER referenced from src/Gui/FreeWorks/ because referencing them forces a PartDesign include/link (reviewer concern 7; D-07/Pitfall 1)"
    - "A GTest proves the rollback logic with the REAL engine: building a Body + solid features, setting Body.Tip to an earlier feature, makes Tip.isTouched() true and Body::mustExecute() return 1 (the true suppress-below trigger, D-04; Body.cpp:94-100). NO new persisted property is introduced (D-05) and NO Visibility/property write performs greying (D-06, Pitfall 3)"
    - "A GTest proves Body::insertObject(feature, target, after) places the feature in the Group without disturbing Tip (insert-at-bar, D-04; Body.cpp:275-321) and that a Group PropertyLinkList reorder inside one openTransaction/commitTransaction is undo-reversible (D-12)"
    - "A GTest proves the dependency-aware DnD gate (canDragObjectToTarget/canDropObjectEx) returns false for a child-before-parent reorder so the drop is BLOCKED (D-10/D-11; ViewProvider.h:450,483 with ViewProviderBody overrides) — allow-and-flag is never exercised"
    - "FwFeatureTree is a thin subclass of Gui::TreeWidget (NOT a from-scratch tree, NOT an edit to Tree.cpp bodies): the public TreeWidget(const char* name, QWidget* parent) ctor is reused and the only seams touched are recursive setHidden over the public item surface (scoping), drawRow / setItemDelegate (presentation), and DnD event overrides — never the non-virtual DocumentItem::createNewItem/populateItem (D-01/D-02, RESEARCH Pitfall 2)"
    - "A QTEST_MAIN (offscreen) test constructs an FwFeatureTree under a real QApplication and asserts it is-a Gui::TreeWidget with the inherited F2 rename action present (D-13) — construction cannot run in the bare Gui_tests_run target (no QApplication), mirroring the Phase-2 FwRibbonWidget split"
    - "The D-03 tree-reuse spike (Spike A) is recorded in SPIKE.md with an explicit PASS/FAIL line for EACH of the 7 PASS-checklist items (RESEARCH § Spike Plan); the active-Body scoping RENDER item (item 2) PASSes ONLY on a REAL two-Body-document QTEST (build a doc with TWO PartDesign::Body objects nested under the DocumentItem, activate one, assert ONLY the active Body's subtree is unhidden with its Origin first and the non-active Body subtree is hidden) or a live demonstration — a synthetic-top-level-item QTEST or a documentation-only 'reachable' note is NOT a PASS for item 2. The VERDICT uses the literal token 'reuse committed' (PASS) or 'subclass-fallback committed' (FAIL); Spike B (Body.Tip path, research-CONFIRMED) is recorded with its confirmed items plus the live-deferred items routed to SPIKE_LIVE_CHECKLIST"
    - "tools/fw-string-leak-grep.sh is clean over src/Gui/FreeWorks/ — no bare 'SolidWorks' token in any new source (only Gui::SolidWorksNavigationStyle is allow-listed; the SW plane display names belong to Plan 03-02 and contain no 'SolidWorks' token anyway)"
  artifacts:
    - path: "src/Gui/FreeWorks/FwFeatureTree.h"
      provides: "FwFeatureTree class declaration: thin subclass of Gui::TreeWidget; the spike surface (recursive setHidden-based scoping helper that descends DocumentItem→Body + presentation seam declarations)"
      contains: "class FwFeatureTree"
    - path: "src/Gui/FreeWorks/FwFeatureTree.cpp"
      provides: "Minimal-but-real FwFeatureTree: construct + mount-able + active-Body scope resolution by \"PartDesign::Body\" type-name literal + scoping render via recursive QTreeWidgetItem::setHidden from invisibleRootItem() over the real DocumentItem→Body topology (DocumentObjectItem::object() accessor); the spike implementation"
      contains: "invisibleRootItem"
    - path: "tests/src/Gui/FwFeatureTree.cpp"
      provides: "GTest (Gui_tests_run): active-Body type-name scoping, link-free Group/solid resolution, Tip-move/mustExecute logic, insertObject placement, reorder+undo, DnD gate BLOCK"
      contains: "TEST_F"
      min_lines: 60
    - path: "tests/src/Gui/FwFeatureTreeWidget.cpp"
      provides: "QTEST_MAIN (offscreen) test: FwFeatureTree construction under a real QApplication + inherited F2 action present + REAL two-Body-document scoping render (non-active Body subtree hidden, active Body's Origin first among visible rows; a root-level non-Body sibling stays VISIBLE to pin that ONLY the non-active Body subtree is hidden — reviewer cycle-3 MEDIUM)"
      contains: "QTEST_MAIN"
    - path: ".planning/phases/03-featuremanager-design-tree/SPIKE.md"
      provides: "Spike A (D-03 tree reuse, 7-item PASS checklist; item 2 scoping render is proven by the REAL two-Body QTEST, not synthetic top-level items) + Spike B (D-04 Body.Tip path) verdict; the literal token 'reuse committed' or 'subclass-fallback committed'"
      contains: "Verdict"
  key_links:
    - from: "tests/src/Gui/FwFeatureTree.cpp"
      to: "tests/src/Gui/FwTestGuiBootstrap.h"
      via: "SetUpTestSuite calls tests::ensureGuiTestBootstrap() so PartDesign::Body + PartDesign_MoveTip resolve before the asserts"
      pattern: "ensureGuiTestBootstrap"
    - from: "tests/src/Gui/CMakeLists.txt"
      to: "tests/src/Gui/FwFeatureTreeWidget.cpp"
      via: "setup_qt_test(FwFeatureTreeWidget) so a QApplication exists for QWidget construction"
      pattern: "FwFeatureTreeWidget"
    - from: "src/Gui/FreeWorks/FwFeatureTree.h"
      to: "Gui::TreeWidget"
      via: "public subclass reusing the (const char* name, QWidget* parent) ctor"
      pattern: "public Gui::TreeWidget"
---

<objective>
Stand up the Wave 0 test scaffold AND run the time-boxed tree-reuse spike (Spike A, D-03) that resolves the keystone foundation decision for this phase: can FreeCAD's existing `Gui::TreeWidget` be scoped to the active Body (non-active-Body subtrees HIDDEN via `QTreeWidgetItem::setHidden(true)`, traversing the REAL nested `DocumentItem`→`Body` topology from `invisibleRootItem()`), shown Origin-on-top, and restyled SW-like via a thin additive `FwFeatureTree` subclass WITHOUT editing `Tree.cpp` bodies (CONTEXT D-01/D-02/D-03; RESEARCH § Spike Plan)? This is the first vertical slice: a thin, real `FwFeatureTree : Gui::TreeWidget` that constructs, mounts, resolves the active Body by the `"PartDesign::Body"` type-name string literal, and recurses from `invisibleRootItem()` to hide non-active-Body subtrees (Bodies are children of the per-document `DocumentItem`, NOT top-level items — Tree.h:348, Tree.cpp:4562) while keeping their ancestor `DocumentItem` visible — plus a headless GTest proving the rollback/reorder logic against the REAL `Body.Tip` engine and the dependency-aware DnD gate, and a QTEST_MAIN widget-construction + REAL-two-Body-document scoping-render test. The spike's exit criteria make the reuse-vs-fallback choice explicit: PASS → SPIKE.md records `reuse committed` and the thin subclass is the committed engine for Plans 03-02/03-03; FAIL → record `subclass-fallback committed` and commit to the CONCRETE fallback (a FreeWorks projection tree that reuses the command/DnD/rename paths but builds its OWN QTreeWidgetItems instead of the friended `DocumentItem` surface — see the fallback note below).

Purpose: De-risk the one genuinely uncertain part of an otherwise ~90%-reuse phase (RESEARCH Pitfall 2: per-item construction in `DocumentItem`/`DocumentObjectItem` is non-virtual and `TreeWidget`-friended, so the spike must confirm that scoping is achievable by HIDING already-constructed items (recursive setHidden over the real `DocumentItem`→`Body` topology) and greying is achievable from `drawRow`/delegate/palette seams — NOT by editing the item factory), and establish the headless test harness Plans 03-02/03-03 sample against (mirrors the Phase-2 native-ribbon spike-gate structure).
Output: `FwFeatureTree.{h,cpp}` (minimal but real — ctor, type-name scope resolver, recursive setHidden-based scoping render from `invisibleRootItem()`, link-free Group reader), GTest logic tests in `Gui_tests_run` (`FwFeatureTree.cpp`), a QTEST_MAIN construction + REAL-two-Body scoping-render test (`FwFeatureTreeWidget.cpp`, offscreen) wired via `setup_qt_test`, CMake additions (FreeWorks + tests, both additive), and `SPIKE.md` recording the Spike A verdict (`reuse committed`/`subclass-fallback committed`) plus the research-CONFIRMED Spike B status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-featuremanager-design-tree/03-CONTEXT.md
@.planning/phases/03-featuremanager-design-tree/03-RESEARCH.md
@.planning/phases/03-featuremanager-design-tree/03-PATTERNS.md
@.planning/phases/03-featuremanager-design-tree/03-UI-SPEC.md
@.planning/phases/03-featuremanager-design-tree/03-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols/files created across Phase 3 (this plan creates the first set):

- **This plan (03-01):** class `FwFeatureTree` (namespace `FreeWorksGui`, `src/Gui/FreeWorks/FwFeatureTree.{h,cpp}`) — thin `Gui::TreeWidget` subclass; spike surface with the active-Body scope resolver keyed on the `"PartDesign::Body"` type-name literal, the recursive `setHidden`-based scoping-render helper that descends `DocumentItem`→`Body` from `invisibleRootItem()` (resolving each `DocumentObjectItem`'s object via the public `object()` accessor), the link-free `Group` reader (`getPropertyByName("Group")` → `App::PropertyLinkList` + type-name solid checks), and the presentation-seam stubs (`drawRow`/`setItemDelegate`). `tests/src/Gui/FwFeatureTree.cpp` (GTest, added to `Gui_tests_run`): scoping/link-free-Group/Tip-logic/insertObject/reorder-undo/DnD-gate-BLOCK. `tests/src/Gui/FwFeatureTreeWidget.cpp` (QTEST_MAIN, offscreen, registered via `setup_qt_test`): construction + F2 action present + REAL two-Body-document recursive-setHidden scoping render. `SPIKE.md` (Spike A + B verdict). CMake edits to `src/Gui/FreeWorks/CMakeLists.txt` (`FreeWorks_CPP_SRCS`/`FreeWorks_HPP_SRCS`) and `tests/src/Gui/CMakeLists.txt` (`Gui_tests_run` list + `setup_qt_test(FwFeatureTreeWidget)`).
- **Plan 03-02 (later):** `FwFeatureTreeDelegate.{h,cpp}` (plane label remap Front/Top/Right + below-tip greying paint); full `FwFeatureTree` scoping + delegate install + F2-active confirmation; `FwLayout.cpp` mount swap (replace the `Fw_FeatureManager` placeholder); dock objectName stays `Fw_FeatureManager`.
- **Plan 03-03 (later):** `FwRollbackBar` mechanism (the `drawRow` band + grab zone + drag→`Body.Tip` fire via the `"PartDesign_MoveTip"` command-ID fired under an RAII Gui::Selection guard, with NO outer FreeWorks transaction), a `FwSelectionGuard` RAII helper (snapshot/clear/select-target/restore selection AND preselection), reversibility ("Roll to End"), insert-at-bar via the Python-only `Body.insertObject` doCommand with an explicit post-insert Tip policy, and `SPIKE_LIVE_CHECKLIST.md` (live 3D suppress-below feel + insertion-line cursor obligations).

> **Downstream gate:** Plans 03-02/03-03 require this plan's `SPIKE.md` to record the Spike-A verdict. If it reads `reuse committed`, the thin-subclass engine is committed; if it reads `subclass-fallback committed`, those plans build on the CONCRETE fallback (a FreeWorks projection tree — see the fallback note in Task 3 — NOT "override more TreeWidget virtuals", since the item-construction surface is non-virtual). They must NOT proceed on the thin surface merely because 03-01 ran.

> **Concrete fallback (committed if Spike A FAILs):** because `DocumentItem::createNewItem`/`populateItem` and the item ctors are NON-virtual and `TreeWidget`-friended (Tree.cpp:351/370/424/492; friends 328-331/479-481/576-577), the FAIL fallback is NOT "override more virtuals". It is a **FreeWorks projection tree**: a plain `QTreeWidget`-derived `FwFeatureTree` that builds its OWN lightweight `QTreeWidgetItem`s from the active Body's `Group` (read link-free via `getPropertyByName("Group")`), and routes reorder/rename/Tip through the SAME reused command paths (`sortDroppedObjects`-equivalent Group `PropertyLinkList` transaction, F2 `Label.setValue` doCommand, `PartDesign_MoveTip` command-ID). Last resort only if even the projection tree cannot reach parity; a one-line `// SW-FORK HOOK` upstream edit is the final escalation and must be flagged for developer approval.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — GTest logic (scoping + link-free Group + Tip + reorder + DnD gate) and QTEST construction/REAL-two-Body-scoping-render, split across targets</name>
  <files>tests/src/Gui/FwFeatureTree.cpp, tests/src/Gui/FwFeatureTreeWidget.cpp, tests/src/Gui/CMakeLists.txt</files>
  <read_first>
    - tests/src/Gui/FwRibbon.cpp (GTest analog: `SetUpTestSuite()` calls `tests::ensureGuiTestBootstrap()` at lines 43-58; `constexpr const char*` ID/type constants; the intentionally-empty allow-list guard; the `kSketchVpTypeName` type-name-literal precedent at line 163 — mirror it for `kBodyTypeName = "PartDesign::Body"`)
    - tests/src/Gui/FwRibbonWidget.cpp (EXACT QTEST analog: `class testFwRibbonWidget : public QObject` with ctor calling `ensureGuiTestBootstrap()`, `private Q_SLOTS`, the `ensureRealMainWindow()` leaked-singleton helper at lines 37-43, `QTEST_MAIN(...)` + `#include "FwRibbonWidget.moc"` at EOF)
    - tests/src/Gui/FwTestGuiBootstrap.h (REUSE VERBATIM, no edit: `tests::ensureGuiTestBootstrap()` creates `Gui::Application(false)` then imports PartDesignGui/SketcherGui/MeasureGui/MatGui — confirm lines 54-105 load PartDesignGui so `PartDesign::Body`, the feature types, and `PartDesign_MoveTip` resolve)
    - tests/src/Gui/CMakeLists.txt (the `Gui_tests_run` add_executable list lines 7-18 — add `FwFeatureTree.cpp` after `FwRibbon.cpp` line 18; the `setup_qt_test(FwRibbonWidget)` call line 23 — add `setup_qt_test(FwFeatureTreeWidget)` next to it)
    - tests/CMakeLists.txt lines 13-47 (`function(setup_qt_test)` — the QApplication-providing harness: QTEST_MAIN + links FreeCADApp/FreeCADGui/QtTest + `QT_QPA_PLATFORM=offscreen`)
    - src/App/GroupExtension.h line 142 (`PropertyLinkList Group;` — the link-free Group the scoping/ordering reads via `getPropertyByName("Group")` cast to `App::PropertyLinkList`)
    - src/Gui/Tree.h lines 348 (`class DocumentItem: public QTreeWidgetItem` — the per-document TOP-LEVEL item), 489-495 (`class DocumentObjectItem: public QTreeWidgetItem`; the PUBLIC `Gui::ViewProviderDocumentObject* object() const;` accessor at :495 used to resolve an item's underlying App object) — Bodies are CHILDREN under the DocumentItem, NOT top-level
    - src/Gui/Tree.cpp line 4562 (`parent->addChild(item)` — proves a Body item is nested as a CHILD of the DocumentItem, so a real-DOM scoping QTEST must descend the tree, not iterate topLevelItem(i))
    - src/Mod/PartDesign/App/Body.cpp lines 94-100 (`mustExecute()` returns 1 on `Tip.isTouched()` — the suppress-below trigger to assert), 248 (`Tip.setValue` on addObject), 275-321 (`insertObject(feature, target, after)` placement), 176-194 (`isSolidFeature` keys on `PartDesign::Feature`/`PartDesign::Transformed` — C++-only; the test resolves solidness by TYPE-NAME STRING instead, never calling this helper from FreeWorks)
    - src/Mod/Part/App/BodyBase.h line 54 (`App::PropertyLink Tip;` — the property to set in the logic test, read via getPropertyByName)
    - src/Gui/ViewProvider.h lines 444-493 (`canDragObjectToTarget`/`canDropObjectEx` declarations — the DnD gate to assert returns false for child-before-parent)
    - src/Gui/Tree.cpp lines 3260-3273 (`sortDroppedObjects` Group `PropertyLinkList::setValue` reorder — the path the reorder+undo test mirrors), 4574/6070 (`item->setHidden(...)` — the public-item hide API the QTEST scoping-render assertion mirrors)
    - .planning/phases/03-featuremanager-design-tree/03-PATTERNS.md § "tests/src/Gui/FwFeatureTree.cpp" + § "tests/src/Gui/FwFeatureTreeWidget.cpp" (the exact test lists + QTEST class skeleton)
    - .planning/phases/03-featuremanager-design-tree/03-VALIDATION.md § "Per-Task Verification Map" + § "Wave 0 Requirements" (wire each test to its Req row)
  </read_first>
  <behavior>
    - GTest (Gui_tests_run, FwFeatureTree.cpp), after `ensureGuiTestBootstrap()`:
      - Scoping identification: a freshly created `PartDesign::Body` reports `getTypeId().getName()` exactly equal to the `kBodyTypeName = "PartDesign::Body"` literal, and a non-Body object does not — proving active-Body identification needs NO module link (TREE-01 scoping, D-07).
      - Link-free Group/solid resolution (reviewer concern 7): the Body's ordered features are read via `getPropertyByName("Group")` cast to `App::PropertyLinkList` (GroupExtension.h:142); a pure helper decides solidness by the member's `getTypeId().getName()` type-name STRING (solid PartDesign feature type names vs datum/sketch type names) — assert it picks the same preceding-solid a sketch row should snap to, WITHOUT calling `Body::getPrevSolidFeature`/`isSolidFeature` and WITHOUT any `#include <Mod/PartDesign/...>`.
      - Rollback logic (TREE-02, D-04): build a Body with at least two solid features; set `Body.Tip` to the earlier feature; assert `Tip.isTouched()` is true and `Body::mustExecute()` returns 1 (the real suppress-below trigger). Then set Tip to the last solid feature ("Roll to End", Pitfall 4) and assert it is accepted.
      - Insert-at-bar (TREE-02): `Body::insertObject(newFeature, targetFeature, /*after=*/true)` places `newFeature` immediately after `targetFeature` in the `Group` order and does NOT change `Tip` (insertObject "doesn't modify the Tip unlike addObject" — Body.pyi:34).
      - Reorder + undo (TREE-04, D-12): inside one `doc->openTransaction("Reorder")` / `commitTransaction()`, reorder the body's `Group` `PropertyLinkList`; assert the new order; `doc->undo()` restores the original order (clean undo, no `.FCStd` pollution).
      - DnD gate BLOCK (TREE-04, D-10/D-11): construct a parent→child dependency and assert the ViewProvider gate (`canDragObjectToTarget` or `canDropObjectEx`) returns false for moving the child before its parent — the drop is BLOCKED, no transaction opens.
    - QTEST (FwFeatureTreeWidget.cpp, QTEST_MAIN, offscreen): under the QApplication QTEST_MAIN provides, an `FwFeatureTree` constructs (`FwFeatureTree("FwFeatureManager", nullptr)`), is-a `Gui::TreeWidget` (dynamic_cast non-null), the inherited F2 rename action is present (the action whose shortcut is `Qt::Key_F2`, or `Qt::Key_Return` on macOS — D-13), and the scoping-render contract holds against the REAL stock-tree topology: build a real FreeCAD document containing TWO `PartDesign::Body` objects (each with an Origin + a feature), make the FwFeatureTree show that document so the stock `Gui::DocumentItem` is the top-level item and the two Bodies are nested CHILDREN under it (Tree.cpp:4562), activate ONE Body, call `scopeToActiveBody()`, then assert: the ancestor DocumentItem `isHidden() == false`, the active Body's item `isHidden() == false`, the NON-active Body's item `isHidden() == true`, and the active Body's Origin item is the FIRST visible (non-hidden) child under the active Body — proving scoping is a recursive setHidden render over the real DocumentItem→Body topology, not a paint and not a synthetic top-level-item shortcut (reviewer HIGH-A). If a fully populated stock document cannot be built headlessly, the test must still descend a real DocumentItem-rooted item tree (DocumentObjectItem children resolved via `object()`), NOT two manually-added synthetic top-level QTreeWidgetItems — synthetic top-level items do not prove the nested topology and are an explicit non-PASS for this assertion.
  </behavior>
  <action>
    Create `tests/src/Gui/FwFeatureTree.cpp` mirroring `FwRibbon.cpp` structure: include `"FwTestGuiBootstrap.h"`, declare a `FwFeatureTreeTest : ::testing::Test` whose `SetUpTestSuite()` calls `tests::ensureGuiTestBootstrap()` (NOT a bare `initApplication()` — the GUI singleton + PartDesignGui import are required so `PartDesign::Body` resolves). Declare `constexpr const char* kBodyTypeName = "PartDesign::Body";` (mirror the Phase-2 `kSketchVpTypeName` literal precedent). Build the Body/features for the logic tests via the running Python interpreter (`Base::Interpreter().runString(...)` to create a document + Body + two solid features + a sketch) OR via the App factory — whichever needs NO `#include <Mod/PartDesign/...>` and NO link (resolve types by name through `App::Document`/`Base::Type`, exactly the no-module-link rule). Author the six `TEST_F` cases from the behavior block: type-name scoping match/non-match; link-free Group read + type-name solid resolution picking the right preceding solid (NO `getPrevSolidFeature`/`isSolidFeature` call, NO PartDesign include); set-Tip-earlier → `Tip.isTouched()` + `mustExecute()==1`; `insertObject` placement (Tip unchanged); Group reorder in one transaction + `undo()` restores; DnD gate returns false for child-before-parent. Reach `Tip`/`Group`/`mustExecute` through `App::Document`/`App::DocumentObject` generic accessors (`getPropertyByName("Tip")`, `getPropertyByName("Group")` cast to `App::PropertyLinkList`, `mustExecute()` is on `DocumentObject`) so no PartDesign header is needed. Add `FwFeatureTree.cpp` to the `Gui_tests_run` `add_executable` list in `tests/src/Gui/CMakeLists.txt` (after `FwRibbon.cpp`). Create `tests/src/Gui/FwFeatureTreeWidget.cpp` mirroring `FwRibbonWidget.cpp`: `#include <QTest>`, `#include "FwTestGuiBootstrap.h"`, a `testFwFeatureTreeWidget : QObject` whose ctor calls `ensureGuiTestBootstrap()` (reuse the `ensureRealMainWindow()` leaked-singleton helper if the tree needs a registered main window), a `private Q_SLOTS` test that constructs an `FwFeatureTree`, `QVERIFY`s the `dynamic_cast<Gui::TreeWidget*>` is non-null, `QVERIFY`s the F2 action is present (scan `actions()` for one whose `shortcut()` is `Qt::Key_F2`/`Qt::Key_Return`), and a REAL-two-Body scoping-render slot: build a real document via the interpreter containing TWO `PartDesign::Body` objects each carrying an Origin + a feature, make the FwFeatureTree display that document (so the stock `Gui::DocumentItem` is the top-level item and the Bodies are nested children — Tree.cpp:4562), activate one Body, call `scopeToActiveBody()`, then `QVERIFY` the ancestor DocumentItem is NOT hidden, the active Body's item is NOT hidden, the NON-active Body's item IS hidden (`isHidden()==true`), and the active Body's Origin is the first non-hidden child under the active Body (descend the item tree via the public child API + `DocumentObjectItem::object()` to identify the Origin/Body objects by type-name). Do NOT add two synthetic top-level `QTreeWidgetItem`s as a stand-in — that does not exercise the nested topology and is an explicit non-PASS for the scoping assertion. End with `QTEST_MAIN(testFwFeatureTreeWidget)` + `#include "FwFeatureTreeWidget.moc"`. Register it via `setup_qt_test(FwFeatureTreeWidget)` next to `setup_qt_test(FwRibbonWidget)`. No "SolidWorks" string in any identifier or literal (CI leak-grep). These tests will go RED until Task 2 lands `FwFeatureTree`.
  </action>
  <verify>
    <automated>ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/src/Gui/FwFeatureTree.cpp` contains `TEST_F`, `ensureGuiTestBootstrap`, the `"PartDesign::Body"` type-name literal, `getPropertyByName`, `Tip`, `mustExecute`, `insertObject`, `openTransaction`, and `undo` — and contains NO `#include <Mod/PartDesign`, NO `getPrevSolidFeature`, NO `isSolidFeature`, and NO `FwFeatureTree` widget construction (logic-only).
    - `grep -c 'mustExecute' tests/src/Gui/FwFeatureTree.cpp` returns >= 1 (the suppress-below trigger is asserted, TREE-02).
    - `grep -cE 'getPrevSolidFeature|isSolidFeature' tests/src/Gui/FwFeatureTree.cpp` returns 0 (link-free solid resolution by type-name string, reviewer concern 7).
    - `tests/src/Gui/FwFeatureTreeWidget.cpp` contains `QTEST_MAIN`, constructs an `FwFeatureTree`, asserts both `Gui::TreeWidget` (dynamic_cast) and a `Qt::Key_F2`/`Qt::Key_Return` action present, AND asserts REAL-two-Body scoping: it builds a document with TWO `PartDesign::Body` objects, references `scopeToActiveBody`, descends the DocumentItem-rooted item tree (uses `object()` / `DocumentObjectItem`), and asserts the non-active Body item `isHidden()==true` while the active Body item + its Origin are not hidden (Origin first).
    - `grep -cE 'topLevelItem' tests/src/Gui/FwFeatureTreeWidget.cpp` does NOT drive the scoping assertion — the scoping slot must reference `object()`/`invisibleRootItem`/`DocumentObjectItem` (real nested topology), NOT two synthetic top-level items as the scoping proof (reviewer HIGH-A).
    - `grep -c 'FwFeatureTree.cpp' tests/src/Gui/CMakeLists.txt` returns >= 1 AND `grep -c 'setup_qt_test(FwFeatureTreeWidget)' tests/src/Gui/CMakeLists.txt` returns 1.
    - `ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure` status is `authored, not run locally, pending CI` in this no-build-tree env (it turns green once Task 2 lands and a CI/build-tree run executes it) — do NOT record green/pass from authoring alone.
    - `grep -ri solidworks tests/src/Gui/FwFeatureTree.cpp tests/src/Gui/FwFeatureTreeWidget.cpp` finds nothing.
  </acceptance_criteria>
  <done>The Wave 0 scaffold exists and reuses the existing bootstrap verbatim: rollback/reorder/scoping/link-free-Group logic is asserted against the real `Body.Tip`/`Group`/DnD-gate engine in `Gui_tests_run`, widget construction + F2-action-present + REAL-two-Body recursive-setHidden scoping render (non-active Body subtree hidden, active Body Origin first) runs under an offscreen QApplication in a QTEST target, both registered in CMake, no module link, no C++-only Body helper, clean of trademark strings. Status: authored, pending CI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimal native FwFeatureTree (thin Gui::TreeWidget subclass) — construct, mount-able, active-Body scope by type-name literal, scoping RENDER via recursive setHidden over the DocumentItem→Body topology, link-free Group reader</name>
  <files>src/Gui/FreeWorks/FwFeatureTree.h, src/Gui/FreeWorks/FwFeatureTree.cpp, src/Gui/FreeWorks/CMakeLists.txt</files>
  <read_first>
    - src/Gui/FreeWorks/FwRibbon.h lines 1-56 (copy the SPDX line 1 + the 23-line LGPL-2.1 copyright block 3-23 + `#pragma once` + `#include "PreCompiled.h"` + `namespace FreeWorksGui` + the doc-comment framing "Compiled directly into FreeCADGui ... no cross-library export macro is required (mirrors FwWorkbench)")
    - src/Gui/Tree.h lines 60-65 (`class TreeWidget : public QTreeWidget`, the public ctor `TreeWidget(const char* name, QWidget* parent = nullptr)` — the reused base ctor), 147 (`void drawRow(...) const override;` — the protected-virtual presentation seam), 82 (`eventFilter` — the F2 path, do NOT re-implement), 348 (`class DocumentItem: public QTreeWidgetItem` — the per-document TOP-LEVEL item the scope render descends through), 495 (`Gui::ViewProviderDocumentObject* object() const;` — the PUBLIC DocumentObjectItem accessor the scope render uses to resolve each item's App object), 328-331 & 456-482 (the NON-virtual `DocumentItem`/`DocumentObjectItem` `TreeWidget`-friended item construction — DO NOT touch; wanting to is the D-03 FAIL signal)
    - src/Gui/Tree.cpp line 4562 (`parent->addChild(item)` — proves Body items are nested CHILDREN of the DocumentItem; the scope render must descend, not iterate topLevelItem(i)), lines 3458/3598/3986/4574/6070 (`item->setHidden(...)` — the EXACT public-item hide API the FwFeatureTree scoping render reuses to hide non-active-Body subtrees; this is how scoping is rendered, NOT by editing item construction)
    - src/App/GroupExtension.h line 142 (`PropertyLinkList Group;` — the link-free Group the scope resolver reads via `getPropertyByName("Group")` cast to `App::PropertyLinkList`)
    - tests/src/Gui/FwRibbon.cpp line 163 (the `kSketchVpTypeName` type-name-literal precedent — mirror it for `"PartDesign::Body"`)
    - src/Gui/Application.h lines 130,142,154 (`signalActiveDocument`/`signalActivatedObject`/`signalInEdit` — the Gui signals the scope resolver observes; observe-the-DOM, no App-layer link)
    - src/Gui/FreeWorks/FwLayout.cpp lines 95-103 (`ensureDock` / `registerDockWindow(name, widget)` — the mount seam Plan 03-02 uses; this plan only makes the widget mount-able)
    - src/Gui/FreeWorks/CMakeLists.txt lines 17-26 (`FreeWorks_CPP_SRCS`), 28-37 (`FreeWorks_HPP_SRCS`), 48 (`target_sources(FreeCADGui PRIVATE ...)`) — append `FwFeatureTree.cpp`/`FwFeatureTree.h`; NO `target_link_libraries` to PartDesign
    - .planning/phases/03-featuremanager-design-tree/03-PATTERNS.md § "src/Gui/FreeWorks/FwFeatureTree.{h,cpp}" (class decl + reusable virtual seams list + the active-Body scoping snippet)
    - .planning/phases/03-featuremanager-design-tree/03-UI-SPEC.md § Color (no `setStyleSheet`/hex; palette/QStyle only) + § Spacing Scale (22px row height, 16px indent reserved — pin metrics, art deferred)
  </read_first>
  <behavior>
    - `FwFeatureTree("FwFeatureManager", parent)` constructs as a `Gui::TreeWidget` subclass (reuses the public base ctor) — no crash, no QApplication-less construction (construction is exercised by the QTEST target).
    - A pure-logic scope resolver `bool isBody(const App::DocumentObject*) const` (or a free helper) returns true exactly when `obj->getTypeId().getName()` equals the `"PartDesign::Body"` literal — keep it pure so the GTest exercises it without a widget (mirror the `FwRibbonContext` decide-then-act split: Gui-only decision, no App mutation).
    - A scoping-RENDER method `scopeToActiveBody()` traverses the REAL nested tree topology and HIDES non-active-Body subtrees via `QTreeWidgetItem::setHidden(true)`: it descends RECURSIVELY from `invisibleRootItem()` (or from each top-level `Gui::DocumentItem` down through its children), resolves each `Gui::DocumentObjectItem`'s underlying `App::DocumentObject` via the public `DocumentObjectItem::object()` accessor (Tree.h:495), KEEPS the ancestor `DocumentItem` visible, hides the subtrees of every `PartDesign::Body` that is NOT the active Body (`setHidden(true)`), and un-hides the active Body's subtree (`setHidden(false)`). Bodies are CHILDREN of the per-document `DocumentItem` (Tree.h:348, Tree.cpp:4562) — they are NOT top-level items, so the render must NOT use `topLevelItem(i)` to find Bodies. It uses the SAME `setHidden` API the base `TreeWidget` already uses (Tree.cpp:4574/6070). It NEVER touches the non-virtual `DocumentItem`/`DocumentObjectItem` construction and NEVER writes a `Visibility`/property value (scoping is a Gui-render, not a model change). This is the concrete, topology-correct answer to "how does scoping actually render" (reviewer HIGH-A).
    - The active-Body feature ordering / solid detection reads the Body's `Group` link-free via `getPropertyByName("Group")` cast to `App::PropertyLinkList` and classifies solidness by member type-name STRING — it does NOT call `Body::getPrevSolidFeature`/`isSolidFeature` (C++-only, would force a PartDesign link — reviewer concern 7).
    - The class declares (but does not yet fully theme) the presentation seams it will own: `drawRow(...)` override (Plan 03-03 paints the rollback band) and a `setItemDelegate` install point (Plan 03-02 installs the plane-remap/greying delegate). This spike build wires the seams as no-op/pass-through overrides so the subclass compiles and the inherited rendering is unchanged.
    - NO `setStyleSheet`, NO hex color anywhere (UI-SPEC § Color; Phase 7 owns theming). NO `#include <Mod/PartDesign/...>`, NO PartDesign link (reference Body by the type-name literal only).
  </behavior>
  <action>
    Create `src/Gui/FreeWorks/FwFeatureTree.h`/`.cpp`, namespace `FreeWorksGui`, copying the `FwRibbon.h` license/copyright/pragma block verbatim and reusing its doc-comment framing. Declare `class FwFeatureTree : public Gui::TreeWidget { Q_OBJECT public: explicit FwFeatureTree(const char* name, QWidget* parent = nullptr); void scopeToActiveBody(); ... };` — the ctor forwards to the base `Gui::TreeWidget(name, parent)`. Add the pure scope resolver keyed on `constexpr const char* kBodyTypeName = "PartDesign::Body";` comparing `obj->getTypeId().getName()` (the GTest's scoping assertion targets this; mirror the Phase-2 `kSketchVpTypeName` literal). Implement `scopeToActiveBody()` as the topology-correct scoping RENDER: traverse RECURSIVELY starting at `invisibleRootItem()` (the parent of the top-level `Gui::DocumentItem`s), descend each `DocumentItem` into its children, and for each item that is a `Gui::DocumentObjectItem` resolve its underlying `App::DocumentObject` through the public `DocumentObjectItem::object()->getObject()` accessor (Tree.h:495); identify the `PartDesign::Body` subtrees by the `"PartDesign::Body"` type-name literal; call `setHidden(true)` on the subtree root of every NON-active Body (and `setHidden(false)` on the active Body's subtree root), while leaving the ancestor `DocumentItem` visible — reusing the EXACT `setHidden` mechanism the base `TreeWidget` uses (Tree.cpp:4574/6070). Do NOT iterate `topLevelItem(i)` to find Bodies (Bodies are children of the DocumentItem, not top-level — Tree.cpp:4562); NEVER edit item construction; NEVER write `Visibility`. Read the active Body's ordered features link-free via `getPropertyByName("Group")` cast to `App::PropertyLinkList` and classify solidness by type-name string (no `getPrevSolidFeature`/`isSolidFeature` call). Declare/define a pass-through `void drawRow(QPainter*, const QStyleOptionViewItem&, const QModelIndex&) const override;` that calls `Gui::TreeWidget::drawRow(...)` (the rollback band paint is Plan 03-03) and reserve a `setItemDelegate` install hook (the delegate is Plan 03-02) — do NOT touch the non-virtual `DocumentItem`/`DocumentObjectItem` item construction (Pitfall 2; wanting to is the FAIL signal). Observe active-Body changes via the Gui signals (`Gui::Application::Instance->signalActivatedObject`/`signalInEdit`/`signalActiveDocument`) — connect them so `scopeToActiveBody()` re-runs on active-Body change, but keep the observed-Body resolution as the pure `isBody`/type-name path (no PartDesign include). To reach `Gui::DocumentItem`/`Gui::DocumentObjectItem`/`object()`, `#include "Tree.h"` (the FreeWorks tree is compiled into FreeCADGui, so the Gui Tree header is available with no extra link). Explicitly `#include` the Qt headers used (`<QTreeWidget>` comes via the `Gui::TreeWidget` base; add `<QPainter>`/`<QStyleOptionViewItem>` for the `drawRow` signature) — do not rely on PCH. Use ONLY palette/native `QStyle` for any color (no `setStyleSheet`, no hex). Append `FwFeatureTree.cpp`/`FwFeatureTree.h` to `FreeWorks_CPP_SRCS`/`FreeWorks_HPP_SRCS` in `src/Gui/FreeWorks/CMakeLists.txt` (additive). No "SolidWorks" identifier/string anywhere (leak-grep). Note: the plane-remap delegate, below-tip greying, the rollback bar, and the dock mount are Plans 03-02/03-03 — keep this minimal but real and mount-able.
  </action>
  <verify>
    <automated>ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/Gui/FreeWorks/FwFeatureTree.h` contains `class FwFeatureTree`, `public Gui::TreeWidget`, a `scopeToActiveBody` declaration, and a `drawRow` override declaration.
    - `src/Gui/FreeWorks/FwFeatureTree.cpp` contains the `"PartDesign::Body"` type-name literal, `invisibleRootItem` (the recursive descent root), `setHidden(` (the scoping render), `object()` (resolving each DocumentObjectItem's App object — real nested topology), `getPropertyByName("Group")` (link-free Group read), and connects at least one of `signalActivatedObject`/`signalInEdit`/`signalActiveDocument`.
    - `grep -c 'topLevelItem' src/Gui/FreeWorks/FwFeatureTree.cpp` does NOT drive Body discovery — Body subtrees are found by descending from `invisibleRootItem()` through the `DocumentItem` (Bodies are children, not top-level — reviewer HIGH-A); if `topLevelItem` appears it must be only to reach the DocumentItem layer, not to treat a Body as top-level.
    - `grep -cE 'getPrevSolidFeature|isSolidFeature' src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0 (link-free solid resolution; reviewer concern 7).
    - `grep -c 'setStyleSheet' src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0 (no inline styling; UI-SPEC § Color).
    - `grep -c '#include <Mod/PartDesign' src/Gui/FreeWorks/FwFeatureTree.h src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0 AND `grep -c 'PartDesignGui' src/Gui/FreeWorks/CMakeLists.txt` returns 0 (no module include, no module link — D-07/Pitfall 1).
    - `grep -c 'Visibility' src/Gui/FreeWorks/FwFeatureTree.cpp` returns 0 (scoping hides via setHidden, never a property write — D-06/Pitfall 3).
    - `grep -v '^#' src/Gui/FreeWorks/CMakeLists.txt | grep -c 'FwFeatureTree.cpp'` returns >= 1.
    - `ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure`: authored, pending CI — turns green once a build-tree/CI run executes the Task 1 logic asserts + the QTEST construction/F2/REAL-two-Body-scoping assert against the real `FwFeatureTree`.
    - `bash tools/fw-string-leak-grep.sh` clean (no bare "SolidWorks" token in `src/Gui/FreeWorks/`).
  </acceptance_criteria>
  <done>FwFeatureTree compiles into FreeCADGui as a thin `Gui::TreeWidget` subclass, constructs under an offscreen QApplication, resolves the active Body by the `"PartDesign::Body"` type-name literal (no module link), RENDERS scoping by recursively descending from `invisibleRootItem()` through the per-document `DocumentItem` and hiding non-active-Body child subtrees via the public `setHidden` API (no item-construction touch, no Visibility write, no topLevelItem-treats-Body-as-top-level mistake), reads the Body's ordered features link-free via `getPropertyByName("Group")`, reserves the `drawRow`/delegate seams, and the headless Task 1 tests are authored to pass under CI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tree-reuse spike gate — Spike A (D-03) 7-item PASS checklist (item 2 scoping render proven by the REAL two-Body QTEST) + Spike B (Body.Tip) status & verdict</name>
  <files>.planning/phases/03-featuremanager-design-tree/SPIKE.md</files>
  <read_first>
    - .planning/phases/03-featuremanager-design-tree/03-RESEARCH.md § "Spike Plan" (Spike A: the 7-item D-03 PASS checklist + the `reuse committed`/`subclass-fallback committed` verdict tokens + the FAIL branch; Spike B: the CONFIRMED Body.Tip items + the live-deferred open items)
    - .planning/phases/03-featuremanager-design-tree/03-CONTEXT.md D-01..D-06 (reuse-first; spike-before-build; no `Tree.cpp` edits; the rollback engine; Gui-only greying)
    - .planning/phases/02-commandmanager-ribbon/SPIKE.md (the Phase-2 spike-verdict format to mirror: per-item PASS/FAIL + an explicit literal verdict token + the deferral routing to SPIKE_LIVE_CHECKLIST)
    - .planning/phases/02-commandmanager-ribbon/02-01-SUMMARY.md (the verification-deferral pattern: blocking human-verify cleared by doc-approval + a source-tree live checklist artifact carrying the on-hardware obligation — no build tree/GUI/CI here)
    - src/Gui/FreeWorks/SPIKE_LIVE_CHECKLIST.md (the Phase-2 live-checklist artifact format to mirror for the deferred items — note it phrases reference-CAD behavior WITHOUT the bare "SolidWorks" token)
    - src/Gui/Tree.cpp line 4562 (`parent->addChild(item)` — the nested topology item 2 must scope across) + lines 4574/6070 (`item->setHidden(...)` — the public-item hide API item 2's scoping render demonstrates)
    - src/Gui/Tree.h lines 348/495 (DocumentItem top-level + the public `object()` accessor item 2's real-DOM proof relies on)
  </read_first>
  <what-built>
    A minimal-but-real thin `FwFeatureTree : Gui::TreeWidget` subclass (constructs, mounts-able, resolves the active Body by the `"PartDesign::Body"` type-name literal with NO module link, and RENDERS scoping by recursively descending from `invisibleRootItem()` through the per-document `Gui::DocumentItem` and hiding non-active-Body CHILD subtrees via the public `QTreeWidgetItem::setHidden(true)` API — proven by the REAL two-Body-document QTEST that activates one Body and asserts the non-active Body subtree is hidden while the active Body's Origin renders first) plus a headless GTest proving the rollback logic against the real `Body.Tip` engine (set-Tip-earlier → `mustExecute()==1`), the link-free Group/solid resolution (no `getPrevSolidFeature`/`isSolidFeature`), `insertObject` placement, Group reorder + undo, and the DnD gate BLOCKing a child-before-parent reorder; and a QTEST construction/F2-present test. This task is the human verification of whether reusing-and-wrapping `Gui::TreeWidget` reaches the reference-CAD FeatureManager parity bar (D-03) WITHOUT editing `Tree.cpp` bodies — so the reuse-vs-fallback decision (D-01/D-02/D-03) is made explicitly before the full build.
  </what-built>
  <how-to-verify>
    Build a THROWAWAY spike harness (an `FwFeatureTree` mounted in the `Fw_FeatureManager` dock for the spike only — the production mount is Plan 03-02) against a live document with TWO active-able `PartDesign::Body` objects, each carrying an Origin + a couple of solid features + a sketch. Walk the 7-item Spike-A PASS checklist from RESEARCH § "Spike Plan", recording an explicit PASS or FAIL line for EACH item in SPIKE.md. Items split into two evidence classes so the gate is an engineering decision, not subjective theater (mirrors the Phase-2 spike gate):

    **A. Reuse primitives — for the scoping RENDER item, demonstrate the actual render against the REAL nested topology (code-evidenced by the REAL two-Body QTEST or live-demonstrated); do NOT pass item 2 by an "it is reachable" note OR by a synthetic-top-level-item QTEST:**
    1. `FwFeatureTree` subclass constructs and mounts in `Fw_FeatureManager` (the `TreeWidget(name, parent)` ctor is public — confirmed by Task 2 + the QTEST construction test).
    2. **Scoping RENDER (the precise unknown):** the tree is SCOPED to the active Body's subtree by recursively descending from `invisibleRootItem()` through the per-document `Gui::DocumentItem` and HIDING the NON-active Body's child subtree with `QTreeWidgetItem::setHidden(true)` (the same API the base tree uses, Tree.cpp:4574/6070; Bodies are children of the DocumentItem, NOT top-level — Tree.cpp:4562) — and the active Body's Origin renders FIRST among the visible rows. PASS requires CODE EVIDENCE from the REAL two-Body-document QTEST (`FwFeatureTreeWidget.cpp` builds a doc with TWO `PartDesign::Body` objects nested under the DocumentItem, activates one, and asserts the non-active Body item `isHidden()==true`, the active Body item + ancestor DocumentItem not hidden, and the active Body's Origin first among visible rows) OR a LIVE demonstration (non-active-Body rows visibly removed, active-Body Origin on top). A documentation-only "reachable" note OR a synthetic-two-top-level-item QTEST is NOT a PASS for this item — if neither real-DOM code evidence nor a live demonstration shows the recursive setHidden scoping the active Body with Origin-first, mark item 2 FAIL/UNKNOWN.
    3. Origin node renders FIRST with its 3 planes + 3 axes + point (free via `ViewProviderOriginGroupExtension::constructChildren` — confirm it survives the setHidden scoping).
    4. Features render in creation order; the profile sketch nests under its consuming feature (free via `claimChildren` — confirm).
    5. Front/Top/Right plane label remap is REACHABLE via a `QStyledItemDelegate` (display text only; object `Label` unchanged) — demonstrate at least a prototype remap renders, or confirm the delegate seam accepts the remap (the full delegate is Plan 03-02).
    6. Below-tip greying is REACHABLE via delegate / `QPalette::Disabled` / item flags with NO `Visibility`/property mutation (Pitfall 3, D-06) — demonstrate a prototype grey, or confirm the seam.
    7. `tools/fw-string-leak-grep.sh` clean (only `Gui::SolidWorksNavigationStyle` allow-listed).

    **B. Spike B — Body.Tip rollback path (research-CONFIRMED HIGH; record the confirmed items + route the live items to the checklist):**
    Record the research-confirmed items as CONFIRMED (Body.Tip is a persisted PropertyLink; set-Tip+recompute = suppress-below via `mustExecute` on `Tip.isTouched`; the `CmdPartDesignMoveTip` set-tip sequence exists; `insertObject` exists as a Python-only path; the 3D tip display exists) — all backed by Task 1's passing GTest for the logic half. Route the LIVE-only open items to SPIKE_LIVE_CHECKLIST (created in Plan 03-03): firing via the `PartDesign_MoveTip` command-ID under an RAII Gui::Selection guard from FreeWorks with no module link (A3); drag-down/"Roll to End" reversibility on hardware (Pitfall 4); 3D shows the rolled-back shape + greyed rows + SC5 feel (parity-user track).

    Then record the VERDICT in SPIKE.md: PASS (items 1-4 + 7 PASS — with item 2 passed on the REAL two-Body-document QTEST code evidence (or a LIVE demonstration), NOT a doc-only note and NOT a synthetic-top-level-item QTEST — and items 5-6 confirmed REACHABLE) = write the literal phrase `reuse committed`; the thin subclass is the committed engine for Plans 03-02/03-03. FAIL (item 2's recursive setHidden scoping render cannot be shown against a real two-Body document, OR any structural change needs the non-virtual `DocumentItem`/`DocumentObjectItem` surgery of Pitfall 2, OR scoping/greying cannot be done from the setHidden + `drawRow`/delegate/palette seams, OR the time-box is exceeded) = write `subclass-fallback committed`; Plans 03-02/03-03 build on the CONCRETE fallback: a FreeWorks PROJECTION tree that builds its own `QTreeWidgetItem`s from the link-free `Group` and routes reorder/rename/Tip through the same reused command paths (NOT "override more TreeWidget virtuals" — the item factory is non-virtual). In THIS environment there is no build tree / live GUI: clear the gate by DOCUMENTATION APPROVAL (Phase 1/2 precedent), recording the research-grounded PASS/FAIL judgement per item AND the code evidence for item 2 (the REAL two-Body QTEST setHidden assertion), and deferring the remaining live demonstrations (live scoping feel, prototype remap/grey on hardware, 3D rollback feel) to SPIKE_LIVE_CHECKLIST — NOT fabricated.
  </how-to-verify>
  <action>
    Author `.planning/phases/03-featuremanager-design-tree/SPIKE.md` recording: (1) Spike A — an explicit PASS or FAIL mark for EACH of the 7 D-03 PASS-checklist items under the A evidence class, citing the Task 1/Task 2 artifact or the research seam that establishes each. For item 2 (scoping render), the PASS citation MUST be the REAL two-Body-document QTEST code evidence (the `FwFeatureTreeWidget.cpp` test that builds a doc with TWO `PartDesign::Body` objects nested under the `DocumentItem`, activates one, and asserts via `object()`/`isHidden()` that the non-active Body subtree is hidden while the active Body's Origin renders first — recursive `setHidden` from `invisibleRootItem()`, NOT `topLevelItem(i)` over synthetic items) and/or a live demonstration — explicitly NOT a "reachable" note and NOT a synthetic-top-level-item test; if neither exists, mark item 2 FAIL/UNKNOWN and verdict FAIL. (2) Spike B — the research-CONFIRMED Body.Tip items (backed by Task 1's passing logic GTest) plus the explicit list of LIVE-only items routed to SPIKE_LIVE_CHECKLIST. (3) an explicit VERDICT using the literal token `reuse committed` (PASS) or `subclass-fallback committed` (FAIL). On PASS, record that Plans 03-02/03-03 proceed on the thin-subclass engine (scoping rendered via recursive setHidden over the DocumentItem→Body topology). On FAIL, record the FAIL-branch next step (the CONCRETE FreeWorks projection-tree fallback: build own QTreeWidgetItems from the link-free Group; reuse reorder/rename/Tip command paths; `// SW-FORK HOOK` only as last-resort escalation) and flag that Plans 03-02/03-03 actions must be re-scoped to the projection-tree surface before execution. Because this env has no build tree / live GUI, mark the gate cleared by documentation approval (Phase 1/2 precedent) and list the deferred live demonstrations (live active-Body scoping feel, prototype plane-remap + below-tip grey on hardware, the Spike-B live fire/reversibility/3D items) as open obligations to be created in SPIKE_LIVE_CHECKLIST.md in Plan 03-03 — never fabricated as observed. SPIKE.md lives under `.planning/` (NOT scanned by the leak-grep), so descriptive prose is fine here; but use no "SolidWorks" string in any code identifier you cite.
  </action>
  <verify>
    <human-check>SPIKE.md records a PASS/FAIL line for all 7 Spike-A D-03 items (item 2 cited to the REAL two-Body-document QTEST setHidden code evidence — non-active Body subtree hidden, active Body Origin first — NOT a doc-only "reachable" note and NOT a synthetic-top-level-item test), the Spike-B CONFIRMED-items + live-deferred list, and an explicit verdict using the literal `reuse committed` or `subclass-fallback committed`, signed off by the developer; `grep -c "PASS\|FAIL" .planning/phases/03-featuremanager-design-tree/SPIKE.md` returns >= 7</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-featuremanager-design-tree/SPIKE.md` exists and contains the word `Verdict`.
    - All 7 Spike-A checklist items are individually recorded: `grep -c "PASS\|FAIL" .planning/phases/03-featuremanager-design-tree/SPIKE.md` returns >= 7 (one PASS/FAIL per item).
    - Item 2 (scoping render) cites the REAL two-Body-document QTEST code evidence (the `setHidden`/`isHidden`/`object()` assertion over a doc with TWO Bodies nested under the DocumentItem) or a LIVE demonstration — SPIKE.md references `setHidden`, the two-Body QTEST, and the nested DocumentItem→Body topology as the item-2 evidence, NOT a bare "reachable" claim and NOT a synthetic-top-level-item test.
    - The verdict states either the literal `reuse committed` (proceed to Plan 03-02 on the thin subclass, scoping via recursive setHidden) OR `subclass-fallback committed` (re-scope Plans 03-02/03-03 to the FreeWorks projection-tree fallback) with the corresponding next-step.
    - Spike B records the Body.Tip items as research-CONFIRMED (backed by Task 1's passing logic GTest) and lists the LIVE-only open items routed to SPIKE_LIVE_CHECKLIST (fire via command-ID under a Selection guard with no link; reversibility; 3D feel).
    - If FAIL: SPIKE.md describes the CONCRETE projection-tree fallback (own QTreeWidgetItems from the link-free Group, reused command paths) and confirms "override more virtuals" is NOT the fallback (item factory is non-virtual) and `// SW-FORK HOOK` is last-resort only.
  </acceptance_criteria>
  <done>SPIKE.md records an individual PASS/FAIL for all 7 Spike-A D-03 items (grep count >= 7), with item 2's scoping render passed on the REAL two-Body-document QTEST setHidden code evidence (non-active Body subtree hidden, active Body Origin first; recursive descent from invisibleRootItem(), not synthetic top-level items) or a live demo rather than a doc-only note, the Spike-B Body.Tip CONFIRMED items + live-deferred list, and an explicit verdict using the literal `reuse committed` or `subclass-fallback committed`; the developer has signed off so Plans 03-02/03-03 know which engine (thin subclass via recursive setHidden, or projection-tree fallback) to build on.</done>
  <resume-signal>Type "approved: reuse" to proceed to Plan 03-02 on the thin-subclass engine (SPIKE.md must read `reuse committed` with item-2 REAL-two-Body-QTEST setHidden evidence), or "approved: subclass-fallback" to build Plans 03-02/03-03 on the FreeWorks projection-tree fallback surface</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FreeWorks Gui tree → App layer | The tree observes the live `App::Document` via SelectionObserver / ViewProvider signals, resolves the active Body by a type-name STRING literal, and reads the Body's `Group` link-free via `getPropertyByName("Group")`; it never includes or mutates App-layer headers and never links PartDesign, and never calls the C++-only `Body::getPrevSolidFeature`/`isSolidFeature`. |
| developer-authored type-name literal → Body identification | The `"PartDesign::Body"` constant is a developer string compared against `getTypeId().getName()`; no user-supplied input is parsed this phase. |
| scoping render (recursive setHidden) → presentation only | Scoping descends the real `DocumentItem`→`Body` topology and hides non-active Body subtrees via the public `QTreeWidgetItem::setHidden` API; it never writes a `Visibility`/property value, so the `.FCStd` document is never mutated for presentation. |
| fork code ↔ upstream shared files | Only additive `target_sources`/test-list edits; `Tree.cpp` bodies are NOT edited (D-02); any unavoidable shared touch gets `// SW-FORK HOOK`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Repudiation/Legal | New source/test files, SPIKE.md | mitigate | No bare "SolidWorks" identifier/string in any new source file; `tools/fw-string-leak-grep.sh` CI gate (only `Gui::SolidWorksNavigationStyle` allow-listed); behavior described as reference-CAD-style |
| T-03-02 | Tampering (fork isolation) | FwFeatureTree, CMakeLists.txt | mitigate | NO `#include <Mod/PartDesign/...>`, NO `target_link_libraries(... PartDesignGui)`, NO C++-only `getPrevSolidFeature`/`isSolidFeature` call; reference Body by type-name literal + Gui signals + link-free `getPropertyByName("Group")` only (RESEARCH Pitfall 1, Phase 2 precedent); additive `target_sources`/test-list edits only; no `Tree.cpp`/`MainWindow.cpp` edit |
| T-03-03 | Tampering (model integrity) | Body.Tip / Group reorder logic test | mitigate | All App-state mutation (set Tip, reorder) goes through `Document::openTransaction`/`commitTransaction` (clean undo asserted); no new persisted property (D-05); the rollback engine is the existing transactional, recompute-aware `Body.Tip` (D-04) |
| T-03-04 | Tampering (presentation leak) | scoping (recursive setHidden) / greying | mitigate | Scoping descends the real DocumentItem→Body topology and HIDES non-active Body subtrees via the public `setHidden` API; greying is Gui-only (delegate/`QPalette::Disabled`/item flags); NEVER `Visibility.setValue` or any property write for presentation (D-06, Pitfall 3) — asserted by the REAL two-Body QTEST `isHidden()` render check + the `grep -c Visibility == 0` gate |
| T-03-05 | Spoofing/Input Validation | type-name literal resolution | accept | The `"PartDesign::Body"` literal is a developer constant compared against the live type system; no user input — low risk |
| T-03-06 | Tampering (invalid reorder corrupts model) | DnD gate test | mitigate | The DnD gate (`canDragObjectToTarget`/`canDropObjectEx`) BLOCKs child-before-parent (asserted false in GTest); only valid reorders commit, in one transaction (D-10/D-11/D-12) |
</threat_model>

<verification>
- `ctest -R "Gui_tests_run|FwFeatureTreeWidget_Tests_run" --output-on-failure`: rollback/reorder/scoping/link-free-Group logic (Gui_tests_run, after the existing module-GUI bootstrap) AND FwFeatureTree construction + F2-action-present + REAL two-Body recursive-setHidden scoping render (FwFeatureTreeWidget Qt test, offscreen QApplication). Status in this no-build-tree env: **authored, not run locally, pending CI** (Phase 1/2 precedent) — only marked green after an actual CI/build-tree run.
- `SPIKE.md` records the Spike-A verdict (all 7 D-03 items individually PASS/FAIL; item 2 scoping render on the REAL two-Body-document QTEST code evidence or a live demo, NOT a doc-only note and NOT a synthetic-top-level-item test) and the reuse-vs-fallback decision (`reuse committed` or `subclass-fallback committed`), plus the Spike-B CONFIRMED items + live-deferred list.
- No bare "SolidWorks" string in any new source file (`tools/fw-string-leak-grep.sh`).
- No PartDesign include/link in `src/Gui/FreeWorks/`, no C++-only `getPrevSolidFeature`/`isSolidFeature` call; only additive CMake/test-list edits; no `Tree.cpp` edits.
</verification>

<success_criteria>
- The Wave 0 scaffold reuses the existing `FwTestGuiBootstrap.h` verbatim so `PartDesign::Body` + `PartDesign_MoveTip` resolve headlessly; rollback (`mustExecute` on Tip), link-free Group/solid resolution, insert-at-bar (`insertObject`), reorder+undo (Group transaction), and the DnD BLOCK gate are all asserted against the REAL engine in `Gui_tests_run`; construction + F2-present + REAL two-Body recursive-setHidden scoping render run under an offscreen QApplication in a QTEST target.
- A minimal real `FwFeatureTree : Gui::TreeWidget` proves the thin-subclass reuse surface compiles, mounts, scopes the active Body by the `"PartDesign::Body"` type-name literal with NO module link, and RENDERS scoping by recursively descending from `invisibleRootItem()` through the per-document `DocumentItem` and hiding non-active-Body child subtrees via the public `setHidden` API (no item-construction touch, no Visibility write, no topLevelItem-treats-Body-as-top-level mistake).
- The reuse-vs-fallback decision (D-01/D-02/D-03) is recorded with an explicit per-item PASS/FAIL verdict (all 7 Spike-A items; item 2 on REAL two-Body-document code/live evidence) and an actionable next step using the literal `reuse committed`/`subclass-fallback committed` (FAIL → the concrete projection-tree fallback); the Spike-B Body.Tip path is recorded research-CONFIRMED with live items routed to SPIKE_LIVE_CHECKLIST (created in Plan 03-03).
</success_criteria>

<output>
Create `.planning/phases/03-featuremanager-design-tree/03-01-SUMMARY.md` when done. Record: the Spike-A verdict (`reuse committed` vs `subclass-fallback committed`), the FwFeatureTree spike API surface (ctor, type-name scope resolver, `scopeToActiveBody()` recursive-setHidden render over the DocumentItem→Body topology, link-free `getPropertyByName("Group")` reader, reserved `drawRow`/delegate seams), the Spike-B CONFIRMED Body.Tip items + the live obligations routed to SPIKE_LIVE_CHECKLIST, and whether Plans 03-02/03-03 proceed on the thin subclass or must re-scope to the projection-tree fallback.

Once the Wave-0 tests are authored, set `03-VALIDATION.md` frontmatter `nyquist_compliant: true` and leave `wave_0_complete` reflecting actual run state: mark `wave_0_complete: true` ONLY after an actual CI/build-tree run shows the tests green — in this no-build-tree env record the tests as `authored, pending CI` and do NOT set `wave_0_complete: true` from authoring alone (reviewer MEDIUM: no-build verification honesty).
</output>
</content>
</invoke>
